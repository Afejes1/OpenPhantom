# normal_from_three_points

Form two binary32 edge vectors next-current and previous-current before either callback, normalize first then second, store their ordered cross product XYZ, normalize the output, then call snap(output, binary32 1e-6). Return void. All three normalize calls return float and their values are discarded from ST0. Full214-byte extent ends immediately at next function0047c686; four call operands map to normalize00486245 at offsets97/111/184 and snap0047c892 at203. Caller rdModel3_CalcFaceNormals passes output,current,next,previous in that order.

The authored fixture covers five owned vector placements, independent/output-current/output-next/output-previous aliases, distinct or all-identical input points, three literal normalized-vector/cross-product oracles, callback mutation of all source points after both edges are captured, discarded normalization return values, complete preserved local payloads and final snap call/threshold. The normalize and snap algorithms remain authored observers; the separately capped snap reconstruction is not promoted or retried. A fixed PC53 round-nearest environment is saved/restored. No arbitrary partial overlap, heap, real geometry corpus or dependency algorithm claim.
