# Start b3d sound placement

Activate an inactive placement and initialize its deadline with optional random span.

**Complete 123-byte relocation-adjusted match.** Final independent native/Docker verification passes
32 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed independent initial and compound guards, active/time stores before random call, noarg random ABI, verified float reciprocal, equal/unordered span skip and late reloads. Replaced mirrored test formula with independently derived expected float bits before execution; candidate unchanged.

Finite selected random values and default pinned x87 behavior are covered. Signaling NaNs, alternate FPU modes, original random sequence and concurrent mutation are outside the fixture.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
