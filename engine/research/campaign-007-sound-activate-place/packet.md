# Prepared target: sound_activate_place

Team-not-done bp/bapsound.c. Full interval0x00417711..0x0041778C: 123 body/full bytes, no alignment. Full original body/instructions and boundary reviewed. All PE relocations, call destinations and float constants independently verified. Pinned VC5 C /Od /MT; immutable target.

void op_sound_activate_place(op_sound_placement *placement); int op_sound_random(void);
World partial float time+0x54. Placement stride60: float random_span+0x20,float deadline+0x28,int active+0x30. Initialized==0 independently exits. Then placement null OR active!=0 OR world null exits. Set active=1, copy world time into deadline with exact bits. If random_span compares equal OR unordered to0.0 under pinned VC5 x87, skip random adjustment. Otherwise call random() once, convert signed int result to x87, multiply by float reciprocal32767 constant, multiply CURRENT placement random_span then add CURRENT deadline, finally round/storefloat deadline. Random callee0049A580 is noarg C runtime state update yielding0..32767; do not rename it or reconstruct it here. Caller control004177AE pushes only placement and pops4, despite older thiscall inference. Full body, both caller references and random callee inspected. Natural expression with (float)op_sound_random() * (1.0f/32767.0f) is appropriate; preserve observed arithmetic order and no extra clamp.
Fixture null/initialization/active/world gates; exact signedzero and quietNaN initial time copies on zero/unordered span; arbitrary nonzero init; random0/mid/max with finite bounded spans and accurate explicit expected results; callback sees active1 and initial deadline, can mutate span/deadline to prove late reloads (world pointer change should not be dereferenced after callback). Keep unrelated fields and neighbors unchanged. No signaling NaNs, alternate FPU modes, real random sequence or invalid pointers. Do not reinterpret unsigned return as negative; actual random range0..32767.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only; readable multiline fixtures. Same15min/10candidate compile/five nonimprovement cap, freeze/handoff immediately. No shared tools/target/Ghidra/Git changes; native compile only, authored fixtures only in locked Docker. No original execution, exclusions, raw instructions or warnings. Worker stop05:07:17 UTC; publication05:17:17 UTC. Parent independently reviews and verifies final pairs.
