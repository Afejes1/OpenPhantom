# Play named sound

Set category1 and start a named nonpositional sound.

**Complete 60-byte relocation-adjusted match.** Final independent native/Docker verification passes
20 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed initialized guard, mode1 write and exact five argument callback order; saved signed result and callback global mutations preserved. Caller assembly proves two source arguments despite stale auto-this metadata.

Focused authored callback fixture; active null-name input verifies wrapper forwarding only, not downstream name validity. Full60-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
