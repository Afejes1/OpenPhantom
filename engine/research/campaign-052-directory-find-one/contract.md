# directory_find_one

Call open(path,mode,extension); null returns0. Otherwise capture read(state,output), always close the same state, and return the captured result. No callers were found. Complete target85 includes eight observed CC alignment bytes after77-byte body; no shrinking or synthetic padding is permitted. Authored callbacks cover null/success, signed results and mutations.

No original program or real file enumeration/close/heap operation is executed. Strings, records and callbacks are authored. Full snapshots and explicit pre/post-callback expectations support the bounded behavior claims. No byte exclusions or warning waivers; individual functions remain distinct from shared acceptance and whole-executable identity.
