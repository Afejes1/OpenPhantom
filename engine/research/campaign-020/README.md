# Campaign020: world lifecycle and controls

Six team-reported unfinished functions cover 856 complete bytes and 27 verified
address operands. One Sol medium worker implements the queue; the coordinator
independently reviews source and fixtures and verifies native/Docker byte identity.

The unchanged limits are 15 minutes and ten compiles per function, five attempts
without improvement, and 45 minutes for the batch with the final ten reserved for
review and publication. Exact means zero differences across the complete original
span with every operand verified. A <=5% near result is research only.

Only authored fixtures execute in locked Docker. Keep compiler binaries, original
game data and generated code private. Full regression is reserved for integration;
this feature publication uses focused checks. Final executable placement remains
separate work. See campaign.json for fixed deadlines and each contract for scope.
