# Acceptance002: shared colormap tracking

Integrate the frozen Campaign015 load/free sources under one canonical shared header,
preserving function bodies, signatures, flags, full targets and all bindings. Use
the actual reconstructed functions together in one synthetic fixture: repeated load,
capacity/failure, ordered free, live callback mutation, retained slots and count reset.
Only after a clean committed registered build, strict full comparison and Docker
behavior pass may the existing recorder append acceptance. Run the deliberate
integration regression checkpoint, then return to focused reconstruction cadence.
No changed matcher, excluded bytes or duplicate fixture main functions.
