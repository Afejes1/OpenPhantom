# Campaign 075

Three complete first-attempt matches: random table next53 bytes, shuffle position25, and indexed lookup38. Total116 bytes and nine verified operands. Their final native/Docker pairs agree and215,880 assertions pass. Shared checkpoint pending in [Acceptance054](../acceptance-054/result.json).

Two research cases stopped after six attempts each and five consecutive nonimprovements. Initializer retains seven differences over its complete82-byte span (8.5366%) and1,007,700 passing checks. Resource handle retains30 compiled bytes against31 original bytes and3,696 passing checks; the unequal extent gets no percentage or body-exact claim. Each first best candidate is restored. Typed aggregate and byte-storage accessor experiments were abandoned; the final fixture retains34 typed slots, with slot33 an owned trailing sentinel. Neither function is registered or accepted.

Initialization fixtures observe callback ordering, prior/current/future table mutations and captured seed return. Table lookup retains the strictly-greater-than1117 condition, including equal1117 reading an explicitly owned adjacent element. Shuffle covers unsigned interpretation of crafted negative RNG returns and preserves incidental EAX as an unclaimed value. The synthetic table capacity does not establish original data extent. No original game data or execution, byte exclusions or warning waivers.
