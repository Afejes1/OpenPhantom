# task_module_proc

Full original interval0x004755C0..0x0047563D; 125bytes, no tail. Team-manifest not_done; independent inventory and parent disassembly confirm ABI/extents/operands. Record0x2C has20-byte opaque prefix and update pointer at14. Event/out/data slots remain opaque unless later recovered.

Module event1 clears the complete64-record table and count/current/cursor, sets sentinel-1 and returns0. Event5 clears target/simulation but retains default return2. Other events preserve all state. Fixture checks all registry/pending bytes and six globals across signed event boundaries. Register chooses first zero update slot; fulltable returnsFFFFFFFF without changes. It clears selected44bytes, installs even a nullcallback, copies and clears20pendingbytes, increments unsigned count modulo32bits and returns exact entry. Fixture covers all65 first-free/full positions, extra later holes, three callback identities includingNULL, six counts including wrap, four bytepatterns and preservation of every other record/global/token. No callbacks are invoked and no unknown pointer fields are dereferenced.

No original execution, byte padding/exclusions or warning waivers.
