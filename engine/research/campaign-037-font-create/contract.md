# font_create

Find first null-font record or return-1. Capture row, acquire/store sprite; failure returns-1. Preserve observed local sprite snapshot at EBP-C, reload row sprite for material/load. Failed font load releases captured sprite slot. Success calls nine default setters in fixed order; return original index despite callback selection changes. The real observed unused sprite local is not synthetic padding.

Team not_done; static TPM/WMAIN body, callers/callees, full extents and operand inventory independently reviewed. Pinned VC5/Od/MT; zero differences across complete spans/all operands; no exclusions. Native compilation only; authored fixtures execute isolated Docker. Fifteen-minute/ten-attempt cap, stop after five non-improving attempts. Original executable never executed.
