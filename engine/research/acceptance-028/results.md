# Acceptance028 results

Three material-state functions are accepted, bringing the shared baseline150 to153. Their239 complete bytes match with7 verified operands and no exclusions or warning waivers. The clean build `20260913-214608-8f3d9767` at `6cc811760e7d01d59e465577c19b4c6039588297` passes all153 comparisons and shared authored fixtures. [Event51](../../evidence/runs/000051-d1e328a9f622a628.json) preserves the source snapshot. [Full supplemental CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34797219187) succeeds on that same source; independent review found no material issue.

The4213 material checks exercise actual unload-hook setter/destructor dispatch, embedded palette publication after callback mutation, and bounded cache invalidation over every0..3 inner-count pattern. The canonical180-byte material view preserves frame pointer type/offset176; palette48 and cache_count172 split the former opaque prefix. Cache invalidation views the same frame pointer through a typed988-byte row cast. All previous font, ripple and sprite fixtures still pass with this shared type.

These three exacts were researched before the ten-hour extension and are counted as carried-in promotions, not new extension matches. Color packing remains research because its170-byte raw-matching body does not cover its14-byte tail. Whole-executable identity is pending.
