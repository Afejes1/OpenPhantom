# puppet_init

Store paused0 and supplied thing before the loop. For four tracks, zero frame+288 and previous+292 immediately before remove callback, then advance by332. Do not reclear callback mutations. Return literal1 in EAX (current Ghidra void signature is stale). Callback results ignored; callbacks may change paused, thing and later/current track fields. Fixture uses observer remove; actual remove integration remains pending.

All unresolved record bytes are opaque. Four tracks established by original initialization/removal callers, not an inferred unbounded pool. Full span includes native compiler alignment. Team manifest not_done. Pinned VC5/O2/MT, no byte exclusions or artificial padding, no original execution. Ten attempts/fifteen minutes; stop after five non-improving attempts.
