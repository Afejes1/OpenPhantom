# Dispatch sound resource event

dispatch sound resource load, free and size events.

**Research candidate: non-relocation bytes differ; target 288 bytes, candidate 288 bytes.** Final independent native/Docker verification passes
108 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed all three event branches against static control flow and verified file API ABI. Parent strengthened close-time name and type reloads, null forwarding, and unsigned size boundary fixtures. Nine remaining differences are local frame displacements; no known semantic defect.

Near match only: 9 of 288 bytes differ after all 10 operands resolve. Parked within the 5 percent research threshold; not exact and not accepted.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own valid listing; no native-listing fallback was used.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-014/review.json).
