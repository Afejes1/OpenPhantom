# Acceptance009 results

Nine shield lifecycle functions (1,000 complete bytes) are promoted. All 52 registered functions pass the full original-toolchain comparison, and all 17,209 new shield checks pass alongside every prior behavior suite. Tooling tests: 100 passed; focused runner tests: 19 passed. The full supplemental modern CI checkpoint passed with warnings treated as errors.

The cleanup fixture links actual destroy-all, destructor, free helper and heap route; the object fixture links actual shield-stop. It covers NULL suppression, every cleanup field, live callback changes, forward-only traversal, shared accessor layout, count updates and guarded actor state. The canonical first record word is corrected from a provisional owner pointer to integer active. All four earlier accessor comparisons still match.

VC5 retains the original texture strcpy intrinsic. The supplemental modern compiler uses an equivalent explicit NUL-copy loop without an invented capacity or warning waiver. Both original-toolchain builds passed; the final committed source was rebuilt after this compatibility change.

Registered history now contains 52 functions and 30 immutable events. Event `000030-c3503c5cc0bb5f3c.json` records source `830ea0328c1754634f2a11e2910ca2c726834e83`. All 93 input hashes were verified against the worktree and committed Git blobs. No original game execution, byte exclusions or new warning waivers. Final linked executable identity remains pending.
