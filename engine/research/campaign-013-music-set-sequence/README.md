# Request interactive music sequence

request interactive music sequence.

**Research candidate: non-relocation bytes differ; target 190 bytes, candidate 190 bytes.** Final independent native/Docker verification passes
168 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed initialized and same-sequence guards before all callbacks; unsigned nonzero mode gate, count/threshold comparisons and complete player/radius ABI independently agree with original. Cache-before-lock, live depth/unlock replacement, return preservation, unsigned extremes, mode0/3, zero backend result and mutated preconditions are covered. Three natural variants retained, final unsigned gate improves9 to8 differences.

Complete190-byte candidate and all12 operands compare reliably:8 differences (4.2105%), a reviewed near match only. Counter/backend behavior is stubbed; signed-depth overflow and alternate FPU behavior remain outside fixture domain.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing without terminators. Complete COFF inventory
and native/Docker code/fixup agreement were retained; no fallback was used.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-013/review.json).
