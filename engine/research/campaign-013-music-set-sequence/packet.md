# Prepared target: music_set_sequence

Team not done bp/bapmusic.c; complete unpadded0x0041060E..0x004106CC span190bytes. Every instruction and address operand independently checked against owned PE before source. Pinned VC5 C /Od /MT.

int op_music_set_sequence(int sequence,unsigned int mode,unsigned int threshold,float radius). Reject op_music_initialized==0 and sequence==op_music_cached_sequence with0 before callbacks. For every nonzero unsigned mode call cdecl void* op_effective_player(void), return0 if null; call cdecl unsigned int op_count_nearby(void* player,float radius). Mode1 rejects count<threshold; mode2 rejects count>threshold; other unsigned modes still count without threshold gate. Cache sequence BEFORE lock; call live op_music_lock(stdcall void(void)); increment live signed op_music_lock_depth; call cdecl int op_backend_sequence(int sequence); call live op_music_unlock(stdcall void(void)); decrement reloaded live depth; return saved backend result. Real complete get-player/count callees independently checked, float radius stack argument confirmed; backend thunk and caller pop prove cdecl one-int ABI. All globals are int; mode/count/threshold comparisons are unsigned. No signed-depth overflow domain claim.

Fixture explicit branch table: initialized/same-cache no-calls, null player, mode0 no count, modes1/2 below/equal/above threshold, unsigned extreme values, other modes including UINT_MAX still count. Verify exact radius bits, player pointer and sequence forwarding; callback cache/flags/depth/pointer mutations and late unlock replacement; cache update occurs only after preconditions and before lock; backend return saved across unlock. Complete globals and callback order, with nonzero failure and success returns. Use owned synthetic pointers, explicit expected table and memcpy float bit oracles.

Worker owns candidate/api/behavior/README/worker-log only. Parent owns fixed targets/Git/sharedfiles/metadata. 15min/10compiles/five nonimprovements maximum per case, early park encouraged. Hard workerstop08:28UTC. Native compileonly, parent fixture execution locked Docker. Print checks/failures; ordinary check function avoids VC5 C4127. No rawbytes/inlineassembly/fakeframes/padding/waivers/exclusions. Send complete per-case handoff immediately.
