# Music detach wrapper

The candidate gates only on uninitialized state, optionally resumes, resets the
two playback IDs, performs shutdown callbacks under the live depth counter, and
clears initialized only after unlocking. Its first native compile matches the
complete 128-byte target with all fifteen bindings verified.

Worker native report: `engine/build/campaign-001-music_detach-ee694df4cbb3/research-result.json`.
The fixture covers the zero gate, non-one initialization, optional resume, exact
sentinels and positive-zero controls, callback order, ignored results, live depth
mutations, initialized timing, and paused changes only through stubs. Coordinator final verification is complete. The original was not executed.
Final parent review and fresh native/Docker verification confirm the complete 128-byte relocation-adjusted match with 58 focused checks passing.
See [result.json](result.json) for source, toolchain, object, fixture and report hashes.
The verified native-listing fallback was used after code/fixup equality; no bytes
were excluded. Final executable layout and accepted-history promotion remain pending.
