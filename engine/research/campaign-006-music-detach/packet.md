# Prepared target: music_detach

Team-not-done bp/bapmusic.c. Full interval0x0041046C..0x004104EC, 128 body/full bytes; no alignment. Coordinator reviewed full body/instructions, next function, callers and callees. Original identity, complete PE relocation inventory and direct call targets independently verified. Pinned VC5 C /Od /MT; immutable target.

Observed API (cdecl except explicitly typed lock slots):
`void op_music_detach(void); extern int op_music_initialized, op_music_paused, op_music_lock_depth; void op_music_resume(void); int op_music_set_state(int state); int op_music_set_sequence(int sequence, unsigned mode, unsigned threshold, float radius); typedef void (__stdcall *op_music_lock_fn)(void); extern op_music_lock_fn op_music_lock, op_music_unlock; void op_music_stop_all(void); void op_music_terminate_script(void); typedef void (__cdecl *op_music_shutdown_fn)(void); extern op_music_shutdown_fn op_music_shutdown;`

If initialized==0 return without mutation/callbacks; any nonzero value enters. If paused!=0, call resume(). Then call set_state(1000), set_sequence(2000,0,0,0.0f), lock(), increment current depth, stop_all(), terminate_script(), shutdown(), unlock(), decrement current depth and finally initialized=0. Ignore state/sequence callback return values and do not recheck initialized after any callback. All counter accesses remain live across callbacks; callbacks see initialized before the final clearing store. No direct paused write, new guard or rollback belongs here. Resume0x410825 has no parameters; set-state one-int cdecl and sequence four-stack-argument cdecl ABIs independently checked in prior/current full instruction views. StopAll0x49A2DE and Terminate0x49A2F0 are no-argument import thunks. Shutdown is the observed no-argument function-pointer slot0x8A01EC; fixture uses an authored cdecl stub. All pointer declarations represent observed slots, not a claim of original header spelling.

Fixture gate with zero and non-1 initialized values, paused0/nonzero optional resume, exact callbacks and sentinels/positive-zero bits, order, ignored return failures, entry-gate mutation inside callbacks, safe depth mutations and visible counter values, final initialized store after unlock, and paused changes only through stubs. No signed overflow, no original imports, no device/script lifecycle execution. Caller evidence includes exact reconstructed disable wrapper, menu and dispatcher.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. No target/case/shared tooling/Git/Ghidra writes. Per function15min/10candidate compiles/five successive nonimprovements; freeze when exact. Record all input hashes and attempts including failures. Native compile only, authored fixtures locked Docker only. No exclusions, warning waivers, raw bytes, artificial frames or original execution. Parent final pair/review is separate and must fit campaign deadline04:50:17; worker stop04:40:17.
