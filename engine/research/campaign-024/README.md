# Campaign024: effects control

Six fog/fade helpers and two shield siblings are selected from untouched team-not_done entries. Parent implements and Sol reviews independently. Each function retains a15-minute/10-implementation-compile cap and five consecutive non-improving attempts stop tuning. The last ten minutes of the45-minute campaign are reserved for evidence/publication.

Only zero-difference full spans with every address operand verified qualify as exact. Near cases remain separate research. Original VC5 native compilation is followed by pinned Docker function/fixup equality and an authored isolated fixture. Do not run the original game, waive warnings, exclude bytes, change the matcher, or reopen frozen cases. Shared integration and final executable placement remain separate.
