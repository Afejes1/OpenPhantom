# colormap_free_entry

Nonnull844-byte map. Release live first handle+334 then clear it after callback. Reload flags+20 and release live second handle+33c only if bit0 set and handle nonnull; clear after callback. First callback can activate/clear/replace second handle or flags; replacement of current handle is overwritten.

Team not_done. Full original spans/all operands statically reviewed. Pinned VC5/Od/MT, no exclusions. Fifteen-minute/ten-attempt function cap; stop after five non-improving attempts. Native compile only; authored fixtures execute isolated Docker. Original executable never run.
