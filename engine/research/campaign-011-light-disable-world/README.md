# Disable world dynamic light

guard and deactivate indexed dynamic world light.

**Complete 83-byte relocation-adjusted match.** Final independent native/Docker verification passes
49 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Independent review confirms signed guards, exact callback ABI and no postcallback writes. Parent expanded full-world/full-record mutation oracle, including neighboring records.

Complete83-byte relocation-adjusted match with sole relative operand verified. Valid active indices require owned records; original missing records-null guard is preserved. Fixtures cover callback leave/clear/replace behavior.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
