# Static contract

Masked x87 negative/unordered inputs skip. Nonnegative values and either zero sign call renderer then overwrite cached start with original argument bits. One dispatcher caller.

Complete 42-byte span at `0x00439023`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
