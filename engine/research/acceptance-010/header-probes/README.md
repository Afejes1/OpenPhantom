# Header dependency probes

These compile-only cases pin the four accepted functions affected by the active-world header migration. Inputs point to canonical headers; candidate files copy the unchanged canonical bodies. No fixture or original game is executed here. Final acceptance still requires the complete registered build and real authored behavior suite.

Including the entire B3D header in every baseline translation unit changed three old VC5 function bodies. A forward declaration and compatibility alias retain the single canonical world type while limiting complete-definition inclusion to frame setup and the shared fixture. All four focused probes then match completely. See result.json for exact source/report hashes.
