# material_invalidate_cache

Capture table+ B0; unsigned outer loop against count+AC, inner against row+18. Clear words row+100+i*EC and+108+i*EC; row stride3DC. No callbacks or globals. Authored disjoint rows have0..3 entries within explicitly represented row storage; no concurrent count mutation or alias self-modification claim. Existing canonical material frame+176 is the same table-pointer slot.

Team not_done. Static full spans and all operands reviewed. Pinned VC5/Od/MT; strict full-span exact acceptance. Fifteen-minute/ten-attempt function cap, five non-improving attempts stop. Native compile only; authored fixtures execute isolated Docker. Original executable never run.
