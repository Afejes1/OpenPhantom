# Rotate current transform axis

rotate current transform axis.

**Complete 67-byte relocation-adjusted match.** Final independent native/Docker verification passes
73 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed complete wrapper and both real callee ABIs. Natural C zero guard matches pinned x87 zero and unordered skip; local48-byte matrix and late current reload preserved. Fixture checks zero/negative-zero/quietNaN skip and signed finite/infinite pass-through, exact argument bits, full prepared matrix, callback order and full post-state.

Arithmetic inside external rotation/multiply helpers is stubbed; fixture covers wrapper contract under pinned default x87 environment, not alternate FPU modes.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-012/review.json).
