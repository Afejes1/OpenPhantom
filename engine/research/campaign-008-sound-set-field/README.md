# Set sound descriptor field

Set one of five floating fields or truncate float to the signed integer field.

**Complete 120-byte relocation-adjusted match.** Final independent native/Docker verification passes
44 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed natural full switch and all14 operands including __ftol; ordinary cast invokes pinned CRT truncate-and-restore behavior. Independent finite signed truncation boundaries and all selected/unselected bit-store paths checked.

Case5 excludes NaN/infinity/out-of-range inputs because conversion is undefined in C; float-store cases include quiet NaN, signedzero, infinities and subnormal. Complete120-byte span includes24-byte jump table.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
