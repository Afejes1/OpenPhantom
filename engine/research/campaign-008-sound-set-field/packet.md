# Prepared target: sound_set_field

Team-not-done bp/bapsound.c. Complete interval0x0041670F..0x00416787: 96 instruction-body bytes and 24 switch-table bytes; full120 mandatory, no alignment exclusion. Full body/instructions, boundary and caller/callee/global context reviewed. Every PE relocation, table label and direct call independently verified. Pinned VC5 C /Od /MT; immutable target.

void op_sound_set_field(int field,float value); extern float op_sound_field0,op_sound_field1,op_sound_field2,op_sound_field3,op_sound_field4; extern int op_sound_field5;
Natural switch cases0..5. Cases0..4 copy value bits exactly into selected float global; case5 performs a natural signed int cast, emitting compiler __ftol at49A44C. Default including negative/>5 changes nothing. No initialized gate, no clamp. Full setter caller004167C3 pushes value then field and pops8; many projectile callers corroborate. Runtime conversion body independently inspected: temporary truncation mode, integer conversion and restored x87 control. Do not manually call or reimplement __ftol: use ordinary C cast and pinned compiler runtime in the fixture. Code ends41676F followed by24-byte six-label table; full span ends416787. All14 operands verified including table labels and runtime call. Fixture independent expected bits for float copies, unchanged unselected globals, positive/negative fractions truncate towardzero, signedzero, representable signed boundaries. Exclude NaN/infinity/out-of-int-range from case5 (undefined C conversion); they may be tested as bit copies only in cases0..4.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Use readable multiline fixtures, actual guards before array indexing and independent numeric expectations. Same15min/10candidate compile/five nonimprovements cap; freeze/handoff promptly. Native compile only; authored fixtures run only in locked Docker. No target/shared-tool/Ghidra/Git changes, fabricated tables/frames, raw instructions, warnings or byte exclusions. Worker stop05:43:20 UTC; publication05:53:20 UTC. Parent independently reviews and verifies final pairs.
