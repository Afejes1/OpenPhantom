# zap_shutdown contract

Release the live sprite handle by address even when NULL, retain callback mutations and return one.

Complete span 23 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
