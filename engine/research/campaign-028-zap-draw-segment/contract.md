# zap_draw_segment contract

Operational depth domain is nonnegative, avoiding undefined negative C shifts. Clamp4 and higher to4 after material lookup, generate then submit count(1<<depth)+1 with captured material and caller width/color bits. Scratch fixture owns17 three-float points; producer capacity beyond this usage is not claimed.

Team manifest reports not_done and prior-campaign inventory found no attempt. Full extent and all operand bindings are required. No original game execution, original resource strings, byte exclusions or warning waivers.
