# Transform point by current transform

transform point by current transform.

**Complete 96-byte relocation-adjusted match.** Final independent native/Docker verification passes
28 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed full core ABI, live current reloads after helper, three local additions before final point copy. Independent explicit11/22/33 oracle and full matrix/point storage cover distinct point and valid old/current-new translation aliases with callback mutation.

Finite exact arithmetic and default x87 fixture domain; arbitrary rounding modes/NaN propagation and full rotation math not covered.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-012/review.json).
