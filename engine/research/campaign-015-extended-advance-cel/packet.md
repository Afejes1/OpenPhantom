# Prepared extended advance cel

cdecl void op_extended_advance_cel(void *ignored_context, OP_EXTENDED_FACE *face). Partial face has material pointer at0x20 and SIGNED int cel at0x24. Partial material has SIGNED int cel_count at0x64. Capture material in local before ++face->cel; if(face->cel >= material->cel_count) face->cel=0 using SIGNED comparison. First argument unused. Caller registration 00428913 inside FUN_004288D1 assigns this callback for animated materials, initializes random cel. No null guards in target. Fixture compile-time offsets, positive/negative/boundary cel and count, context ignored, whole face/material/neighbors unchanged except cel. Avoid INT_MAX increment undefined overflow oracle.

Full original body and interval verified through the next function entry; no alignment or table tail. Every listed operand independently validated against owned original PE and complete HIGHLOW relocation coverage before implementation. Team manifest: not_done, bp/extended.c. Use C89-era VC5 C, /Od /MT and existing runner. No masks, assembly, byte insertion, manufactured frames, warning waivers or target edits. Per function15minutes/10compiles, stop after five no-improvements or campaign worker deadline. Freeze near results rather than polish.
