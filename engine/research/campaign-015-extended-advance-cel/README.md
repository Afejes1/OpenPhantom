# Advance face material cel

advance and wrap a face material cel with signed comparisons.

**Complete 56-byte raw-code-match.** Final independent native/Docker verification passes
18 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed captured material, ignored context and signed comparison. Parent added defined signed extrema and both sides of negative-count comparison; replaced the fixture do-while-zero CHECK macro rejected by VC5 C4127 with a function, preserving strict warnings. Complete guarded face/material snapshots pass.

Full56-byte candidate has no address operands and is raw-code exact. Signed overflow and null dereference inputs are outside the fixture domain; no guards added. One earlier fixture compile failed before execution due to C4127 and remains recorded. Shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
