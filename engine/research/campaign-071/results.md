# Campaign 071

Four full-span exact path component copiers: extension 99 bytes, stem 133, directory 116 and drive-directory 196. Total 544 bytes and 11 verified call operands, with zero resolved differences. All final native/Docker pairs agree; the retained matrices pass 1,251,840 assertions.

Implementation used 13 bounded compiles: 1 extension, 7 stem, 2 directory and 3 drive-directory. Natural pointer expression grouping corrected one-byte extent differences; descriptive local naming reproduced VC5 local allocation without adding dead locals. Stem matched on its last bounded attempt. Histories are retained; no retry cap was reset.

The original clears destination before helpers. Tests use the actual path helper bodies and guarded arrays, exhaustively combining short strings, alignment, bounded post-NUL data and exact alias. Intermediate observers verify the drive prefix and bounded copy writes before final termination. Partial overlap is outside this contract. Separator remains explicitly unaccepted dependency test support. Shared checkpoint pending in [Acceptance 051](../acceptance-051/result.json).
