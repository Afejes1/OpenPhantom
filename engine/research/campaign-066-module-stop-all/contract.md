# module_stop_all

Dispatch event4 for nodes with status bit1 set; clear the live bit only after a zero callback result.

Full/body193 bytes at0046F0BF ending0046F180 with no tail; parent independently inspected the complete disassembly. Finder operand+24 points46F529; head operand+99 points6D64C4. Targeted missing/ineligible returns1, eligible returns the signed callback result. ID0 caches head, traverses live next after callbacks, and returns0. Callback arguments are cdecl event4,0,0. Signed one-bit status extraction and post-callback live raw-word clear preserve all unrelated bits and callback writes. The contributor names this lifecycle stop; no direct callers were recovered, so higher-level naming is provisional.

The fixture links the accepted finder and checks guarded lists, duplicates, signed/missing IDs, eight bit1/high-word patterns, five signed return classes, independent visit plans, current-link skip/truncate and global endpoint mutations. A callback can replace the status word before the function reloads it; expected bit1 clearing occurs only on result0. Full expected snapshots are captured before calls and explicit callback effects are applied separately. Fatal observer guards prevent unexpected visits from indexing arrays. Only bounded authored graphs execute in Docker; no original execution or byte exclusions.
