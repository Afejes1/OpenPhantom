# world_load_via_skip

The original VC5 RTM compiler produces a complete 32-byte
function match after 1 verified call bindings, with zero differing bytes.
The independently reviewed locked-Docker fixture passes 36 checks.

[Contract](contract.md), [source](candidate.c), [fixture](behavior.c),
[receipt](result.json) and [campaign review](../campaign-018/review.json).

Shared canonical types and final whole-executable placement remain separate integration work.
Research status is separate from immutable accepted history; no original game or
real backend was executed. No byte exclusions or new warning waivers.
