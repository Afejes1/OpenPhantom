# Prepared target: music_resume

Team not done bp/bapmusic.c; complete0x00410825..0x00410870 span75 bytes, body60, 15 trailing INT3 bytes retained. All instructions, PE relocations and relative call boundaries independently verified before source. Original immutable hash remains pinned.

void op_music_resume(void). Call live op_music_lock (stdcall void(void)); increment live signed op_music_lock_depth; op_backend_resume(cdecl void(void)); call live op_music_unlock(stdcall void(void)); decrement reloaded live depth; op_music_paused=0. No initialized/paused guards and no return. Fixture: negative/zero/positive bounded depths; varied paused flags; strict lock/backend/unlock order with explicit stage and depth/paused expectations. Callbacks mutate depth and paused; backend replaces unlock callback, final decrement must use callback-mutated depth and paused must become0 only at end. Preserve complete owned globals/function-pointer state. Test authored callback code only; no signed overflow domain claim. Real backend thunk no arguments; lock/unlock imported no-arg stdcall names corroborate.

Pinned VC5 C /Od /MT, no inline assembly/fakeframes/rawbytes/padding insertion/waivers/exclusions. Full target MUST remain unchanged even if natural object lacks original trailing padding: park and report body/layout separately, never count a shortened match as full exact. Worker owns candidate/api/behavior/README/worker-log only; parent handles fixedtargets/Git/sharedfiles/metadata. Eachcase15min/10compiles/five no-improvements; early park preferably after1-2 natural variants. Worker hardstop08:28UTC. All fixtures MUST print checks/failures and use full state and independent explicit oracles; memcpy for float bits, ordinary check function avoids VC5 C4127. Complete each fixture and notify per-case before moving next. Native compileonly, parent authoredfixture execution locked Docker.
