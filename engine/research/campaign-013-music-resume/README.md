# Resume interactive music

resume interactive music.

**Body exact (60 bytes), original full 75-byte layout pending.** Final independent native/Docker verification passes
45 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed real no-arg stdcall lock/unlock and cdecl resume thunk, live increment/decrement and final paused-clear timing. Three signed-depth fixture cases cover callback order, paused before/after each callback, live depth mutation and late unlock replacement; no source correction.

Complete 60-byte body matches after verified relocations, but original full75-byte span retains 15 trailing INT3 bytes absent from candidate; final placement/padding is unresolved and this is not a complete function match.
Depth fixtures avoid signed overflow; backend audio behavior is stubbed. Full75-byte target retains15 INT3 padding bytes absent from natural object.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-013/review.json).
