# Is sound muted

is sound muted.

**Body exact (39 bytes), original full 54-byte layout pending.** Final independent native/Docker verification passes
96 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed exact disabled==1 test and initialized==0 fallback, no callbacks, exact0/1 return. Explicit6x4 table includes negative/extreme nonzero states, repeat calls and unchanged globals. Source independently agrees with complete instructions.

Complete 39-byte body matches after verified relocations, but original full54-byte span retains 15 trailing INT3 bytes absent from candidate; final placement/padding is unresolved and this is not a complete function match.
Original full54-byte span retains15 trailing INT3 bytes; candidate39-byte body is evaluated separately from unresolved layout.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-013/review.json).
