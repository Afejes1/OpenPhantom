# Ready assignment: scan-line advance

Team bapline_step, not_done, source line 1321, unknown original file. Entry 0x004083D0; RET 0x004084A0; next function 0x004084A1. The complete body/span is 209 bytes with no trailing alignment. No calls, PE absolute operands, globals or referenced strings appear in the complete body. Ghidra incoming-XREF query returns none; this is not proof the game never invokes it.

One cdecl pointer to the 80-byte op_scan_line record from campaign-002. Return int 0 or 1, not a C++ bool. If error at +0x30 is nonnegative, copy diagonal_x/y (+0x34/+0x38) to step_x/y (+0x10/+0x14), add diagonal_x/y directly to x/y (+8/+12), and add diagonal_error (+0x3C) to error. Otherwise perform the same operations using straight_x/y (+0x40/+0x44) and straight_error (+0x48). Original branch order puts the nonnegative path first. Increment index (+4) after either branch. Return 1 iff the updated index is strictly greater than count (+0); return 0 on equality. The original does not stop writes merely because index was already beyond count, and has no null guard. Other record fields remain untouched. Prologue has no local storage. Readable field assignments in original branch order are a reasonable first hypothesis.

Fixture must independently check negative, zero and positive error; both return values including updated index == count and count+1; custom signed steps, untouched fields, and repeated steps. Avoid signed overflow and null invalid inputs. Check all writes/return plus untouched fields, not only final endpoint. If using trajectory cases, initialize expected records independently from the existing reset implementation. A struct snapshot and expected selected updates is appropriate; do not call the candidate as its own oracle.

Coordinator-prepared static evidence, 2026-09-13. Original WMAIN.EXE is static only. Use the pinned original VC5 C frontend /Od /MT. No exclusions, synthetic frame objects, byte insertion or warning waivers. Read engine/docs/campaign-rules.md. The existing campaign-001 runner stages the case inputs by basename, including the previously reviewed line.h. Do not edit that shared header or any parent-owned target/case/packet. Native runs compile only; authored fixture execution is permitted only via locked Docker. Reproduce with: python engine/research/campaign-001/verify.py --case CASE_FOLDER --runner native --reference C:/Dev/Ghidra/projects/TPM/WMAIN.EXE; final Docker adds --behavior and --expect-report NATIVE_REPORT. Parent independently verifies final inputs. Each function has a separate 15-minute/10-worker-compile cap, five consecutive no-improvement stop. Each worker compile, including verification repeats, must be logged with hypothesis, source hash and report path. Freeze candidate/fixture before final peer comparison. No original call body may be executed.
