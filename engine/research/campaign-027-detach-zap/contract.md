# detach_zap contract

Clear matching endpoints in every active zapline record; preserve inactive records and every unrelated byte. Actor is compared only, never dereferenced. Sole caller is accepted object teardown.

Complete span 116 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
