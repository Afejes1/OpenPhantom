# Poll sound channel status

Query selected sample status and release finished or update playing channels.

**Complete 166-byte relocation-adjusted match.** Final independent native/Docker verification passes
48 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed128-byte channel layout and type-selected stdcall query, saved status switch, live record and flags after callback, clear-before-release and update mode4. Fixture guards all indices and independently checks complete final flag values and unchanged neighbors.

Valid channel indices0..11 are a precondition; original has no range guard. Authored callbacks replace real Miles status queries and release/update implementations. Full166-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
