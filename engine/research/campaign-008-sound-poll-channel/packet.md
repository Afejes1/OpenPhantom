# Prepared target: sound_poll_channel

Team-not-done bp/bapsound.c. Fullinterval0x00415D8A..0x00415E30, 166 bytes, no alignment excluded. Fullassembly/body and boundaries/caller/calleecontext reviewed. Complete PE relocation inventory and all operands independently verified. Pinned VC5 C /Od /MT.

void op_sound_poll_channel(unsigned index); extern op_sound_channel op_sound_channels[12]; channel128 bytes: void *sample0,void *sample3d+4,int is3d+8,void *record+12,unsigned flags+16,opaque rest through128. typedef int (__stdcall *op_sound_status_fn)(void *sample); extern op_sound_status_fn op_sound_status2d,op_sound_status3d; void op_sound_release_slot(unsigned index); int op_sound_update_slot(unsigned index,int mode);
No bounds/init guard in original; onlyfixture indices0..11. If is3d!=0 query actualIAT8C1788 AIL_3D_sample_status@4(sample3d); else8C178C AIL_sample_status@4(sample); save intstatus. Switch status: case2 if CURRENTrecord !=NULL then CURRENTflags &=~0x20000 before release_slot(index); case4 update_slot(index,status) ignoringresult; othercases untouched. NaturalC switch evidence is extra dispatch local plus case branches. Poll caller periodic passesexact12slots; both release and updatecallee fullbodies reviewed. Querycallback may mutate selectedrecord/flags/type; branchsample chosenbeforecallback, laterrecord andflags readlive. Release seesclearedbit; itsmutations retained. Fixture 2D/nonzero3D inclnegative, statuses2/4/0/other, null/non-nullrecord, flagpreservation, query mutations gatebothdirections, exactcallbackargument/order with realbounds guards and ownedfakehandles; unchangedneighborrecords. No out-of-rangefixturecall.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Same15min/10candidate compile/five nonimprovements cap; native compileonly, authored fixture executiononlylocked Docker; no target/sharedtool/Git/Ghidra changes. Workerstop05:43:20,publication05:53:20UTC.
