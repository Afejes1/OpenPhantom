# Get sound descriptor field

Copy one of five floating fields or convert the signed integer field to float.

**Complete 128-byte relocation-adjusted match.** Final independent native/Docker verification passes
17 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed natural six-case switch, all float-copy paths and signed FILD conversion, default no-dereference behavior, full 24-byte jump table and 13 bindings. Independent boundary and ties-to-even expectations; fixture uses quiet NaN sentinels because its float-return helper passes through x87.

Default fixture FPU rounding only; no alternate control-word claim. Complete128-byte span includes24-byte generated switch table; no exclusions.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
