# Prepared target: sound_get_field

Team-not-done bp/bapsound.c. Complete interval0x0041668F..0x0041670F: 104 instruction-body bytes and 24 switch-table bytes; full128 mandatory, no alignment exclusion. Full body/instructions, boundary and caller/callee/global context reviewed. Every PE relocation, table label and direct call independently verified. Pinned VC5 C /Od /MT; immutable target.

void op_sound_get_field(int field,float *output); extern float op_sound_field0,op_sound_field1,op_sound_field2,op_sound_field3,op_sound_field4; extern int op_sound_field5;
Natural switch cases0..5. Cases0..4 copy corresponding float bits exactly (MOV words), case5 converts signed int field5 tofloat with x87 FILD/FSTP. Default including negatives and>5 leaves output untouched without dereferencing it. No initialized gate. All six case heads and full24-byte switch table reviewed; target has13 operands including table base and six internal local labels. Do not fabricate a table or alter target offsets to fit output. Code ends4166F7 and full span ends41670F. Getter XREF query returned zero; exact mapping is corroborated independently by setter and its callers. Fixture all fields, signedzero/quietNaN bit copies, int-to-float rounding including INT_MIN/INT_MAX with independent expected bits, default sentinel preservation, null output for default only, valid output alias to float global, untouched globals. Default FPU environment only.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Use readable multiline fixtures, actual guards before array indexing and independent numeric expectations. Same15min/10candidate compile/five nonimprovements cap; freeze/handoff promptly. Native compile only; authored fixtures run only in locked Docker. No target/shared-tool/Ghidra/Git changes, fabricated tables/frames, raw instructions, warnings or byte exclusions. Worker stop05:43:20 UTC; publication05:53:20 UTC. Parent independently reviews and verifies final pairs.
