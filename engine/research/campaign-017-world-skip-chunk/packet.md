# Prepared world_skip_chunk

cdecl int op_world_skip_chunk(OP_STREAM *stream, unsigned int bytes). Stream partial layout: void *handle at0, int mode at4. If mode==0 call cdecl int op_stream_seek(void *handle, long offset, int origin) with handle, (long)bytes,1 and ignore return. Otherwise local void *buffer=op_allocate(bytes), call op_stream_read(buffer,1,bytes,stream), op_release(buffer). Return1 unconditionally. Allocator cdecl void*(unsigned int); release cdecl void(void*); stream reader cdecl int(void *destination,unsigned int element_size,unsigned int count,OP_STREAM *stream) four arguments in exactly that order. Backend interprets size/count signed in compressed division; this wrapper forwards full32bit words, no arithmetic. Seek callee is CRT fseek with three stack args; caller stack and callee offsets verified. Callback failures are ignored, including allocator NULL passed to read/free. Fixtures only stub these external callbacks; no claim NULL or huge counts are safe through the real backend. Cover mode0, nonzero positive/negative modes, zero and large byte words, failed callbacks, order, live stream mutation by allocator, captured allocation pointer unchanged after read callback; full guarded stream oracle. One local pointer only. Six callers: chunk dispatcher, animation definition/map readers and0041E4BA.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
