# Campaign045: palette lookup tables

Two verified team-not_done targets total803 complete bytes and5 operands. Source prototypes distinguish the16384-byte mono table and65536-byte blend table, each with768-byte palette input. The independently prepared colormap serializer packet is reserved for a following cohort with separate service/failure fixtures. [Plan](plan.json).
