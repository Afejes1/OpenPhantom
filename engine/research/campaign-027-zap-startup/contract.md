# zap_startup contract

Return one immediately for a cached non-NULL sprite. Otherwise acquire by exact name pointer, store the callback result and return its non-NULL status. Authored name replaces private original resource text.

Complete span 50 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
