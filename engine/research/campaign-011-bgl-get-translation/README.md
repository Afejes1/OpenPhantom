# Copy current transform translation

copy current transform translation.

**Complete 49-byte relocation-adjusted match.** Final independent native/Docker verification passes
7 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed three ordered word copies from affine translation offsets24/28/2C. Parent fixture adds full source/state preservation, self translation and valid overlapping word output, with independent signed-zero/NaN payload oracles.

Complete49-byte match with3 verified operands. Bit-preserving word interface; no floating arithmetic. Three valid output words required; no null guard exists in original.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
