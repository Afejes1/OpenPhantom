# world_read_global_vertices

cdecl int target(OP_STREAM *stream, OP_WORLD *world, OP_HEADER *header); stack8,C,10 confirmed against dispatcher0041E7E4. Callback cdecl int op_stream_read(void *destination,int size,int count,OP_STREAM *stream), count1, verified actual00470786 stack8,C,10,14 and signed division. Header counts/size stored unsigned; use unsigned product and explicit int conversion for signed loop bound/callback size so modulo2^32 remains defined. VC5 conversion must be proved at high-bit boundaries. No original backend execution. Use valid owned in-range record storage; payload sizes are argument-forwarding tests only. Header count0x48, disk size0x4C, payload0x800; world count0x94, records pointer0x9C. First capture unsigned product; mismatch returns0 with no stores/callback. On equality capture world records pointer ONCE. Signed index starts0 and compares against LIVE (int)header count each iteration. Read(current local pointer,(int)LIVE disk size,1,stream). A zero read returns0 immediately, retaining previous world count except callback mutations; any nonzero (including negative) continues. Increment local pointer by0x20 after each successful read. At successful end store LIVE header count to world count and return1. World pointer mutations do not retarget captured local. Header count expansion/contraction affects loop; disk size mutation affects later calls; payload mismatch is NOT rechecked. Preserve local declaration ordering naturally; three locals (bytes,current,index), no fabricated stack. Fixtures mismatch return, zero and high-bit signed-bound counts, zero/wrapped/high-bit products, one/multiple records with correct stride, first and mid-loop read failures, negative success return, callback mutations of count/disk size/records/world count, captured storage and full guarded pre-call expected snapshots. High-bit positive-size callbacks may be tested with count1; do not walk huge arrays.

Team report: bapworld_loadGvrt; reported not_done. Dispatcher caller 0x41e86d.

Full interval, terminal return, next entry, empty base-relocation coverage and single real stream-call binding independently verified against static original PE/Ghidra evidence. No original bytes or decompiler output included.
