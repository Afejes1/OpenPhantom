# Campaign026: letterbox and halo lifecycle

Eight untouched team-not_done functions in the effects subsystem. Prioritize letterbox persistence, halo resources and the per-object cleanup callee used by accepted teardown. Parent implements; Sol reviews independently. Each function has a fifteen-minute/ten-implementation-compile cap, with five consecutive non-improving attempts stopping tuning. Reserve ten minutes of this forty-five-minute batch for review and publication.

Require complete original spans, all operand bindings, zero differences, native/Docker code and fixup equality, and focused authored fixtures. The overlay restore has a 51-byte body plus an eight-byte INT3 tail: retain the full 59-byte comparison and park it if natural compiler extent does not match. No synthetic padding, raw instructions, warning waivers or byte exclusions. No original game execution or original resource names in source.
