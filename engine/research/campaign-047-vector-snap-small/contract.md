# vector_snap_small

Cdecl void(vector,threshold), mutable12-byte vector and float by value. Sequentially assign X,Y,Z using nested ternary magnitude and strict below-threshold selection. External+0 used in sign comparisons; selected zero is positivezero. Full248-byte span and3 operands, no tail. Sole caller normal-from-three-points passes1e-6. Pinned VC5 x87 TEST AH,C0 treats unordered as below: a quietNaN component or threshold clears the component. The modern compiler uses ordered comparisons and retains it; the fixture explicitly distinguishes this supplemental behavior. Independent literal bit table covers signedzero, subnormal, below/equal/above positive/negative magnitudes, negative/zero/infinite/quietNaN thresholds and infinity/quietNaN components. No signalingNaN or alternate control-word claims.

Team not_done verified by numeric address. Fixtures use authored guarded storage and saved/restored maskedPC53 round-nearest control. Original executable is static evidence only. Independent native/Docker matching, passing focused fixtures and review precede any exact claim; shared acceptance and whole-executable identity remain separate. No exclusions or warning waivers.
