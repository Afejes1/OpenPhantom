# Prepared world_read_sprite_refs

cdecl int op_world_read_sprite_refs(OP_STREAM *stream,OP_WORLD *world,OP_HEADER *header). Header unsigned count0x2D0, payload_size0x800. World unsigned sprite_count0xCB4, void *sprites0xCB8. Local unsigned int bytes=count*0x3C wrapped32bit. Mismatch return0 unchanged/no callback. Success world sprites=op_allocate(bytes); then world sprite_count=LIVE header count after allocator; return op_stream_read(LIVE world sprites,bytes,1,stream). Allocator cdecl void*(unsigned int). Reader cdecl int(void*,unsigned int size,unsigned int count,OP_STREAM*) exactly four arguments, preserve return. No allocNULL guard; stub can observe NULL forwarded (does not establish valid real backend input). Fixture ordered allocate/read, zero/wrapped sizes, allocator changes header count/payload and world fields, read sees captured bytes vs live count, full pre-call guarded snapshots plus only explicit callback/target writes, callback negative return and mutation retention. Trusted sprite reference chunk reader and dispatcher0041EA09.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
