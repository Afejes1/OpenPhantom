# Next-work queue

Start from accepted source `39af9501bbaf0c05728dbead492509a5109a78b8`, immutable event38, and 105 accepted functions. Read the current branch/status before working; this file does not itself authorize another unattended window.

## Integrate existing exact source first

| Priority | Function | Original address | Full bytes | Frozen result |
| --- | --- | --- | ---: | --- |
| 1 | `zap_build` | `0x0043d6b1` | 211 | [receipt](../../research/campaign-030-zap-build/result.json) |
| 1 | `zap_subdivide` | `0x0043d784` | 326 | [receipt](../../research/campaign-031-zap-subdivide/result.json) |
| 1 | `zap_draw_opaque` | `0x0043d8ca` | 159 | [receipt](../../research/campaign-030-zap-draw-opaque/result.json) |
| 2 | `sprite_acquire` | `0x00429540` | 64 | [receipt](../../research/campaign-029-sprite-acquire/result.json) |
| 2 | `sprite_frame` | `0x0042959d` | 87 | [receipt](../../research/campaign-029-sprite-frame/result.json) |
| 2 | `sprite_release` | `0x004295f4` | 71 | [receipt](../../research/campaign-029-sprite-release/result.json) |
| 3 | `frame_prepare` | `0x0043f57a` | 70 | [receipt](../../research/campaign-033-frame-prepare/result.json) |

All seven functions already have exact complete spans and focused native/Docker evidence. Integration must preserve the frozen research files and create canonical shared source/types and connected fixtures before another acceptance event.

1. **Zap geometry (696 bytes).** Reconcile the current `float[51]` point storage with the new typed vector array. Use one alias-safe canonical declaration based on original static extents, not inconsistent view aliases. Replace the existing generation stand-in with the actual builder/subdivider in segment/default fixtures. Preserve endpoint-write-before-delta alias behavior, length scaling, random-call order, left-first recursive subdivision and draw callback mutations. The opaque draw path is build, copy, resolve, submit. Existing focused evidence is not evidence that this shared integration is complete.
2. **Sprite resources (222 bytes).** Resolve the same-address `sprite_frame` / `op_resolve_zap_material` collision with one canonical function and compatible types. Reconcile the release return type (actual integer result, ignored by callers) across declarations. Existing tiny sentinel handles are insufficient when the real resolver dereferences sprite/material/frame fields; provide owned typed backing storage. Update all affected shield, halo, ripple and zap fixtures to call real helpers, with only external backend callbacks substituted. Keep original format/resource strings private; use authored fixture strings.
3. **Frame preparation (70 bytes).** Reuse the canonical world/camera globals and actual accepted surface submission function. Preserve all three live world reloads and the camera transform pointer. The caller pushes three arguments to surface preparation despite stale one-argument callee metadata. Verify the complete caller/callee ABI before changing that metadata. Do not introduce a second fake canonical symbol at an already accepted address.

## Parked research: preserve existing caps

| Function | Original bytes | Candidate bytes | Status | Next evidence needed |
| --- | ---: | ---: | --- | --- |
| `ripple_tick` | 433 | 433 | Near: 4 differing bytes (0.9238%) | Explain x87 operand/load choice: original lifetime load plus reverse divide versus age load plus divide; finite fixtures alone do not establish nonfinite behavior. |
| `shield_set_offset` | 80 | 83 | Research | Resolve source return intent and code generation before further variants. |
| `overlay_restore_state` | 59 | 51 | Research | Establish compiler/linker alignment tail without fabricated padding. |
| `ripple_reset` | 138 | 136 | Research | Explain two trailing bytes under original layout. |
| `effects_restore` | 163 | 157 | Research | Explain full-span alignment tail. |
| `shield_relink` | 200 | 197 | Research | Explain full-span alignment tail. |
| `shield_hit` | 299 | 302 | Research | Resolve compiler allocation/output differences under the existing contract. |

Different extents have no supported mismatch percentage. A length ratio is not a byte comparison. Do not rename these assignments to reset exhausted attempt/time caps; resume only with a concrete new evidence question and explicit scheduling.

## Acceptance checklist for the next integration

- Confirm active Ghidra TPM:/WMAIN.EXE and immutable reference hash; use static analysis only.
- Retain pinned VC5/options, strict full spans, operand checks and zero-difference requirements.
- One implementation writer; independent read-only review of ABI, fixtures and input receipts.
- Use owned storage, pre-call expected snapshots and explicit expected callback mutations.
- Commit clean source before the full build; test authored fixtures in locked Docker, compare, then record.
- Run supplemental full CI once for the deliberate shared checkpoint, fixing real warnings without waivers.
- Update progress and team-work-order, publish to the fork branch, and leave exact research separate from accepted history and final executable identity.
- Original game data, compiler binaries, private build artifacts and raw decompiler output stay out of Git.
