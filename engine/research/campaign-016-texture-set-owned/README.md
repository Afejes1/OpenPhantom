# Set texture resource owned

forward ownership state to a nonnull texture resource.

**Complete 29-byte relocation-adjusted-match.** Final independent native/Docker verification passes
27 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed null suppression and two-stack-argument cdecl resource flag helper. Fixture forwards every signed flag value unchanged, preserves full resource storage, observes exactly one callback and retains callback mutations. Parent uses both valid old-flag return values0/1.

Full29-byte function exact after its single call binding. Resource database callback is authored fixture code; shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-016/review.json).
