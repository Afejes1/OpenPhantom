# Prepared texture set owned

cdecl void op_texture_set_owned(void *resource,int owned). Early return if resource==NULL, otherwise call op_set_resource_owned(resource,owned) and ignore result. Verified cdecl callee004722F4 reads stack8/c; finds resource record by data and toggles bit0 of 16-bit flags at0x1c; returns old bit as a full32-bit int0/1. No direct XREF found; inferred wrapper purpose rests on body/callee, consistent with team texture_setOwned. Use void/int32 contracts. Fixture: null resource suppresses call and preserves state even arbitrary owned; nonnull forwards exact pointer and0/1/negative/extreme owned values once; callback-mutated state retained, returned old flag ignored. No actual resource database accessed.

Full body and next entry independently verified in Ghidra; no tail. All operands and complete PE HIGHLOW coverage independently verified before experiments. Team manifest reports not_done. Existing C/VC5/strict matching campaign rules apply unchanged. Freeze near rather than polish;15minute/10workercompile cap and hard worker stop11:56 UTC2026-09-13. Parent final verification/publication by12:06 UTC. No original execution or public original content.
