# ripple_tick contract

- Address: `0x0043A44E`; full span433 bytes.
- ABI: `void __cdecl(void)`.
- Team manifest: `not_done`.

Scan256 records. Capture nonnegative creation age; skip active nonmoving records with nonzero last_update. Capture projected position/radius before removing the old token group. Consume live fields/lifetime after removal, clamp finite alpha, render with nine observed arguments and selector-101, then stamp live effect time and decide deactivation using live lifetime. Records skipped by the nonmoving gate retain activity even past expiry. Authored fixtures cover every selected slot, finite age/lifetime/gates, callback mutations, later activation/skipping and whole-pool preservation. NaN behavior is not claimed. Candidate remains near-only: four bytes differ at the x87 numerator/divisor load and divide direction, despite same433-byte extent and matching finite fixtures.

Exact requires zero complete-span differences after every validated address binding, matching native/Docker code and fixups, and a passing isolated authored fixture. Near means at most5% differing bytes at the same complete extent, never accepted as exact. Only authored source/contracts/digest evidence are public; original bytes, assets, resource strings and toolchain binaries remain private. No original execution, warning waivers or byte exclusions.
