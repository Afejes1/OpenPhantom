# halo_add contract

- Address: `0x00439857`; full span231, body231 bytes.
- ABI: `void __cdecl(actor*, uint node_key, int field8, int fieldc, char* resource_name, uint color, int mode)`.
- Team manifest: `not_done`; no prior candidate at this address.

NULL actor/name returns before all callbacks. Signed node ordinal must be positive. Scan32 records for the first owner==NULL, capture that record before acquisition, write returned sprite even on failure, then publish fields only on success. Increment the live global count and OR actor flag0x10 after callbacks. No inferred pool count invariant; count arithmetic excludes overflow. Authored fixture covers every slot, full pool, signed ordinal boundaries and callback mutations.

Only authored C, tests, semantic layout notes, addresses and digest evidence are public. Original executable, resource strings, compiler binaries and raw analysis remain private. External callbacks are authored boundaries until canonical integration. Exact classification requires zero differences across the full span after all validated address fixups, native/Docker code and relocation equality, and the isolated fixture. No padding insertion, byte exclusions or warning waivers.
