# Attach model colormap

attach a loaded colormap table to model materials missing one.

**Research candidate: non-relocation bytes differ; target 117 bytes, candidate 117 bytes.** Final independent native/Docker verification passes
12 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed loader-before-dereference, unsigned live model loop, null-only assignment and retained assigned counter. Independent review required pre-call expected snapshots; worker corrected those before handoff. Parent added full pointer-list preservation and owned existing-colormap sentinel. Mixed and repeated materials, callback-expanded model and failed loader pass.

Six bytes differ across full117 bytes (5.128205 percent), beyond the5 percent near threshold; retain research-candidate. Full extent and sole call binding verified, no known behavioral defect. Shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
