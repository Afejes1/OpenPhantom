# Prepared extended attach colormap

cdecl void op_extended_attach_colormap(OP_EXTENDED_MODEL *model,const char *name). Local loaded pointer = op_extended_load_colormap(name). If nonnull: local signed assigned=0; local UNSIGNED index=0; for(index=0;index<live model->material_count;++index), if(model->materials[index]->colormap==NULL), assign (unsigned char*)loaded+0x30 and increment assigned. The assigned local exists even though not returned; preserve natural local source. Partial model material pointer array at0x48, unsigned count0x4c. Partial material colormap pointer at0x60. Loader is cdecl void*(const char*), verified previous target. Caller00428D95 inside FUN_00428CF6 loads models. Fixture: failed load before any model dereference (NULL model permitted with failed load), zero count with null array, mixed existing/missing colormaps, repeated material pointers, loader mutating model count/array, complete model/material/storage snapshots; pointer arithmetic inside owned >=64-byte buffer.

Full original body and interval verified through the next function entry; no alignment or table tail. Every listed operand independently validated against owned original PE and complete HIGHLOW relocation coverage before implementation. Team manifest: not_done, bp/extended.c. Use C89-era VC5 C, /Od /MT and existing runner. No masks, assembly, byte insertion, manufactured frames, warning waivers or target edits. Per function15minutes/10compiles, stop after five no-improvements or campaign worker deadline. Freeze near results rather than polish.
