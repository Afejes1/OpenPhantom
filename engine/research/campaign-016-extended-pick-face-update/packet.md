# Prepared extended pick face update

cdecl void op_extended_pick_face_update(OP_EXTENDED_FACE *face). Partial face: material pointer0x20, unsigned cel0x24, float trail/intensity field0x30, 32-bit trail state0x44, update callback0x48. Partial material has UNSIGNED cel_count0x64. If face->trail_value >=1.0f: set callback op_capture_model_face_trail, zero trail state. Then independently if material!=NULL && unsigned cel_count>1: set callback op_extended_advance_cel, then face->cel=op_random15()%LIVE face->material->cel_count (unsigned DIV). This second callback overwrites trail callback but preserves prior trail reset. RNG callback may change material/count before modulo; reloads are required. Do not cache material across RNG. Original compare x87 FLD/FCOMP/FNSTSW testsC0, so quietNaN skips first branch; signed zeros and infinities follow natural >=. Constant1.0f verified004A82B4, not an invented extern.

op_random15 is cdecl unsigned int(void): actual0049A580 is CRT-style LCG updating004BA61C and returns(state>>16)&0x7fff; do not rename the runtime. Fixture may stub return within0..32767 to preserve actual contract. Stored trail callback actual ABI void(void *thing,face*,float *world_vertices), confirmed existing capture_model_face_trail body; advance callback actual visible args void(void *ignored,face*), verified prior015. This target only stores those function addresses, never calls them. A C89 unspecified-parameter function pointer typedef void (*OP_FACE_UPDATE)() is compatible with both typed pointer-argument callbacks and models the shared slot; avoid an incompatible-prototype assignment or new warning waiver. The real dispatcher passes extra cdecl args ignored by advance; do not alter prior015 candidate ABI.

Caller004289F1 inFUN00428932 prepares mesh material flags/intensity before this selector. Fixture: float below/equal/above1, quietNaN and infinities using memcpy bits (no undefined floating conversion); materialNULL/count0/1/>1 including unsigned highbit count; both-branch precedence, zeroed trail word retention, RNG call order (sees assigned callback), callback-time material/count mutation, complete pre-call face/material/storage expectations. Avoid modulo0 after mutation; no invocation of stored callbacks or original RNG. Snapshot expected state BEFORE target call, and apply only known desired writes. Use check(int) function, not do-while(0) CHECK macros rejected by strict VC5.

Full body and next entry independently verified in Ghidra; no tail. All operands and complete PE HIGHLOW coverage independently verified before experiments. Team manifest reports not_done. Existing C/VC5/strict matching campaign rules apply unchanged. Freeze near rather than polish;15minute/10workercompile cap and hard worker stop11:56 UTC2026-09-13. Parent final verification/publication by12:06 UTC. No original execution or public original content.
