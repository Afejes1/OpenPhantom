# Select model face update

select trail or animation callback and initialize the material cel.

**Complete 97-byte relocation-adjusted-match.** Final independent native/Docker verification passes
48 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed x87 threshold/unordered behavior, independent branch precedence, unsigned cel count and post-RNG material/count reload, typed callback targets and original random15 contract. Parent added callback-time trail-state and old-cel observations, count0, positive zero, NaN with animation, and original-material preservation in replacement case. Full pre-call expected states and guarded face pass.

Full97-byte function is relocation-adjusted exact with all4 operands and literal1.0f verified. Stored callbacks are not invoked; actual differing cdecl parameter contracts are recorded. No signed-overflow or divide-by-zero fixture inputs. Shared ABI integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-016/review.json).
