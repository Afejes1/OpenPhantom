# Remove level sound references

remove level sound references.

**Research candidate: non-relocation bytes differ; target 211 bytes, candidate 211 bytes.** Final independent native/Docker verification passes
57 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed full signed traversal, captured record cursor and live world/count reads, callback order, resource reload/clear timing and ignored dispatcher argument. Corrected fixture separates one-record and expanding-world cases and independently checks entire record bytes. Candidate retains twelve local-stack-offset differences after three recorded natural variants.

Complete 211-byte comparison with all 14 operands verified: 12 differing bytes, above the five-percent scheduling threshold. Research only; no byte exclusion. Callback worlds and resource pointers remain valid for the original observed access contract.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing; no native-listing fallback was used.
The listing lacks terminators; complete COFF inventory and native/Docker
code/fixup equality were retained and checked.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-010/review.json).
