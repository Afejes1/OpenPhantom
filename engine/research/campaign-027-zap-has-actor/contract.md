# zap_has_actor contract

Return one for any matching endpoint among four in a nonzero-active, nonzero-mode record; otherwise zero. No state mutation or pointer dereference. Enemy AI callers corroborate the query purpose.

Complete span 131 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
