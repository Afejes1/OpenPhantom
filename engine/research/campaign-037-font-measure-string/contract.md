# font_measure_string

NUL-terminated nonNULL input; float/ST0 return. Width local is intentionally uninitialized, total starts0. Every character calls measure, ignores result, adds current width, advances pointer. Defined authored cases are empty or first callback writes width; later failure retains prior width. No invented zero initialization or claim about initial failure. Signed-char byte loop preserved.

Team not_done; static TPM/WMAIN body, callers/callees, full extents and operand inventory independently reviewed. Pinned VC5/Od/MT; zero differences across complete spans/all operands; no exclusions. Native compilation only; authored fixtures execute isolated Docker. Fifteen-minute/ten-attempt cap, stop after five non-improving attempts. Original executable never executed.
