# Static contract

cdecl int(int slot,int enabled); full67 bytes. Same bounds/ownerchecks; stores exact32-bit argument at04, not normalized boolean; returns1 or0. Tableoperand30, strideB4. Fixture includes negative/fullwidth values and every slot.
