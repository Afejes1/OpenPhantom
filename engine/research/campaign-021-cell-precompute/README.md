# cell_precompute

complete span exact. Original VC5 /Od /MT; 71 complete bytes, 5 verified operands. The final fixture passed 55 checks with0 failures.

All five callbacks verify the same pointer, ordinal and complete state. The first clears count, proving later calls are not gated on count. NULL and count0/1/255 cases pass.

Shared acceptance remains pending. Whole-executable placement remains unverified. See [receipt](result.json), [contract](contract.md) and [attempt log](worker-log.json).
