# Prepared target: sound_tick

Team-not-done bp/bapsound.c. Full interval0x00415D13..0x00415D1D: 10 body/full bytes, no alignment. Coordinator reviewed full body/instructions, next boundary, callers and callee/global context. Complete PE relocation inventory and call destinations independently checked against owned original. Pinned VC5 C /Od /MT; immutable target.

void op_sound_tick(void); int op_sound_periodic(void);
A void cdecl wrapper makes exactly one no-argument periodic call; its return is ignored. Full periodic callee and frame-service caller0045F631 inspected; no wrapper arguments, guards, globals or mutation. Caller at0045F64D ignores result. Do not invent fastcall ECX from callee decompiler: wrapper has no use of it. Authored stub count, repeated calls, zero/negative/positive returns, and retained callback state. Actual sound periodic/backend never executed.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Use readable multiline fixtures. No target, shared tooling, metadata or Git edits. Same15min/10candidate-compile/five nonimprovement cap; freeze and report each result immediately. Native compile only; authored fixtures run only in locked Docker. No warnings, exclusions, raw instructions or original execution. Overall worker stop05:07:17 UTC; publication05:17:17 UTC. Parent independently reviews and verifies final pairs.
