# Campaign 077

Three complete matches: gameplay RNG102 bytes, seed75 and roll68. Total245 bytes and eight verified operands. RNG and seed matched first attempt; roll matched its second attempt using result/i local names whose VC5 slot ordering had already been observed. Four implementation compiles in total. All final native/Docker pairs agree and25,664 focused assertions pass. Shared checkpoint pending in [Acceptance055](../acceptance-055/result.json).

The RNG oracle uses independent64-bit multiplication and remainder. Seed input INT_MIN remains outside executed C negation; priorINT_MIN state is covered. Roll preserves unsigned remainder/accumulation and allows zero sides only when count is nonpositive.

Shared integration keeps canonical registered objects and bindings unchanged from the focused bodies. Explicitly named test-only source copies preserve controlled shuffle/roll callback oracles, while separate matrices execute the actual linked RNG-to-shuffle and RNG-to-roll chains. Probes carry no original address and do not add accepted functions. Whole-executable identity remains unfinished.
