# palette_resource_install

Read current pointer005badf4 and call colormap_set_current. Reload that global after the callback and pass the live value to existing helper0047b993; return1 regardless of the first callback return. The helper's already accepted OP_MATERIAL view exposes palette atoffset48, also present in OP_COLORMAP; an explicit pointer cast preserves the existing ABI and canonical type without claiming every field is interchangeable. External observers cover global replacement and null arguments without real dependency dereferences.

All storage and strings are authored. Complete guarded map/name/global snapshots and callback order are checked. Original program execution, copied game data, byte exclusions and warning waivers are prohibited. Individual functions remain separate from shared acceptance and whole-executable identity.
