# World controls integration review

The three source bodies are identical to final Campaign020 candidates after reversing only canonical header and type substitutions. All28 prior target and registry entries remain identical. All previous behavior.c content remains identical after removing the new fixture include and call; world_readers_behavior.c is byte-identical. The canonical world retains CD4 extent and all previous field offsets; nine control fields add explicit offset checks.

The shared fixture preserves focused finite/VC5-specific cases under their proper compiler domain. It adds one guarded world sequence of clock, ambient and fog, verifies full state after each real function, and has every fog backend observe the entire expected world including prior clock/ambient results. It also verifies complete fog event order, argument bytes, range and independent renderer flag reads. No external game backend runs.

The parent implemented the integration after the delegated worker's source-write approval was rejected for missing trusted user context; the parent scoped edit was approved using the user's direct fork-edit authorization. The worker performs read-only independent review. No warning or byte exclusion was added. Tooling checkpoint:100 engine tests and19 focused-runner tests passed. Clean committed original-toolchain build/test/compare/record and full CI remain pending.

Acceptance complete:260 controls checks,0 failures; all31 spans exact after verified relocations. Full CI succeeded. See result.json and event25.
