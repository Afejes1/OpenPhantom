# Static contract

Subsystem startup caller. Three ordered sprite acquisitions followed by individual global stores. Resource contents/names remain private; fixtures supply authored arrays.

Full 59-byte span at `0x004397F0`; body 59 bytes. Static body, caller references and all operands reviewed. Team not_done and no local duplicate. Original game is never executed.
