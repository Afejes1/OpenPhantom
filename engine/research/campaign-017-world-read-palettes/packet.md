# Prepared world_read_palettes

cdecl int op_world_read_palettes(OP_STREAM *stream,OP_WORLD *world,OP_HEADER *header). Header unsigned palette_count0x20, payload_size0x800. World unsigned palette_count0x64, palette_capacity0x68, void *palettes0x6C. Local unsigned bytes=count*0x300 wrapped32bit. Mismatch return0 with no writes/callback. Otherwise write world count=header count then capacity=header count; return op_stream_read(world palettes,bytes,1,stream). Reader cdecl int(void*,unsigned int size,unsigned int count,OP_STREAM*), caller/callee verified. Fixtures mismatch, zero/wrapped products, exact callback argument/order/pre-callback count and capacity, signed backend return preservation, callback mutation retention, full guarded pre-call object snapshots with explicit expected writes. Stub does not exercise real compressed stream at zero/large sizes. Trusted palettes chunk reader and dispatcher0041E835.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
