# Static contract

One load-path caller. Read28-byte staged block; return1 exactly on zero callback result, otherwise0. No live-state publication.

Full 33-byte span at `0x00439794`; body 33 bytes. Static body, caller references and all operands reviewed. Team not_done and no local duplicate. Original game is never executed.
