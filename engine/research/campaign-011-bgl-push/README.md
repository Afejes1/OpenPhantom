# Push current transform

push current transform.

**Complete 85-byte relocation-adjusted match.** Final independent native/Docker verification passes
31 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed signed upper guard, captured stack source, independent current increment and depth increment before copy. Parent fixture verifies full stack/alternate storage, owned destination, source independence and null cursor on guarded paths.

Complete85-byte match with8 verified operands. Explicit standard memcpy intrinsic. Active source/destination are valid nonoverlapping matrices; negative depth is an invalid original index domain and no new guard is introduced.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
