# world_read_dialog

cdecl int target(OP_STREAM *stream, OP_WORLD *world, OP_HEADER *header); stack8,C,10 confirmed against dispatcher0041E7E4. Callback cdecl int op_stream_read(void *destination,int size,int count,OP_STREAM *stream), count1, verified actual00470786 stack8,C,10,14 and signed division. Header counts/size stored unsigned; use unsigned product and explicit int conversion for signed loop bound/callback size so modulo2^32 remains defined. VC5 conversion must be proved at high-bit boundaries. No original backend execution. Use valid owned in-range record storage; payload sizes are argument-forwarding tests only. Dialog differs: NO payload validation. world dialog_count0xCD4 set0 BEFORE single read of world dialog_records0xCD8, (int)header payload0x2F4,1,stream; current-chunk field0x800 irrelevant. If read0 return0 without restoring count; callback writes remain. On nonzero read capture LIVE world dialog_records AFTER callback. Signed index starts0 and loops against (int)LIVE header dialog_count0x2F0, clearing exactly each16-byte record offset0xC runtime link, advancing local pointer16 in the for increment. At end copy LIVE header count to worldCD4 and return1. Natural locals current-pointer then index; do not invent unusedbytes local. Fixture backend can retarget world pointer and alter header count/payload, mutate count on failure; assert correct original read destination/size, resetcount visible insidecallback, redirected post-read link clears and untouched original storage. Negative/high-bit counts skip clearing but are copied to world; zero/negative read returns correctly distinguished. All record fields other than link, guards, and header/world unrelated bytes unchanged except explicit callback effects.

Team report: bapworld_loadDlg; reported not_done. Dispatcher caller 0x41e819.

Full interval, terminal return, next entry, empty base-relocation coverage and single real stream-call binding independently verified against static original PE/Ghidra evidence. No original bytes or decompiler output included.
