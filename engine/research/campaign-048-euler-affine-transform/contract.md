# euler_affine_transform

Build an observed48-byte affine transform from three live angle-component reads and a live final12-byte translation copy. Call degree sin/cos dependency00494238 three times with (angle,float*first,float*second); stack/callee ABI confirms cdecl and no return. Six float outputs and four explicitly rounded float products feed the observed XYZ basis equations. Full263-byte span ends at0047cbc7. Three rel32 call operands are offsets21/44/67. Calls occur before all matrix stores, while subsequent angle components and final translation remain callback-visible.

The authored fixture uses three literal dyadic trig-output sets and independently generated basis words. Four guarded matrices/vectors exercise disjoint inputs, angles aliasing output translation, translation self-alias and both inputs sharing output translation. Callbacks mutate later angle components and the translation, and verify previously returned local values are preserved. Every owned object and guard is compared. The trigonometric dependency is an authored observer, not an implementation claim. PC53 round-nearest is saved/restored. Arbitrary partial overlaps and other floating-point domains remain outside these fixtures.
