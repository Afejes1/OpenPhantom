# Static contract

Same original x87 gate as start, but calls only the renderer and has no cached-end store. One dispatcher caller.

Complete 33-byte span at `0x0043904D`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
