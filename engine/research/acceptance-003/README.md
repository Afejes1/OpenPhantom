# Acceptance003: shared world chunk readers

Promote the three exact Campaign017 readers only after canonical type and callback
reconciliation, a genuinely shared fixture and the unchanged clean registered
VC5 build/test/record checkpoint. Full extents94+108+90=292 bytes and all four
call bindings stay unchanged. No matcher or warning-rule changes.

The stream backend00470786 accesses destination,size,count,stream at stack8,C,10,14
and uses CDQ/IDIV by size. The canonical callback therefore uses signed int size
and count. Unsigned payload products remain defined modulo2^32 and explicit int
conversion forwards the exact32-bit word under the original VC5 implementation.
The compiled original argument bytes and focused high-bit cases must verify this;
no claim of arbitrary-compiler portability or valid huge real I/O is made.
