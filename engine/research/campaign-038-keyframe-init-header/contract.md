# keyframe_init_header

Clear132-byte header, copy31 bytes maximum from external default-name resource using original strncpy, then force byte31 zero. Caller allocation confirms132 bytes. Fixture content is authored; original shared default name is never copied into Git.

Team not_done. Static TPM/WMAIN full spans and operands confirmed. VC5/Od/MT; zero full-span differences and all operands required for exact status. Native compilation only; authored fixtures execute isolated Docker. Fifteen-minute/ten-attempt cap; stop after five attempts without improvement. No byte exclusions, raw byte source, warning waivers, or original-game execution.
