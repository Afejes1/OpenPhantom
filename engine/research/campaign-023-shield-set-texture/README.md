# shield set texture

Complete 148-byte original-VC5 match, with 3 verified operands and zero differing bytes. The independently reviewed isolated fixture passes 2740 checks. Native and Docker function bytes and relocation inventories agree.

[Source](candidate.c), [static contract](contract.md), [fixture](behavior.c), [receipt](result.json), [experiments](worker-log.json) and [review](../campaign-023/review.json).

This research result is separate from accepted integration and final executable placement. No original executable or real backend was run; no byte exclusions or warning waivers. The shield active word has the corrected integer type.
