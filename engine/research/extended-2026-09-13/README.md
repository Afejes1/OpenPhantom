# Authorized four-and-a-half-hour continuation

The user authorized execution for4.5 hours beginning2026-09-13 at11:54:31 UTC.
The hard stop is16:24:31 UTC (12:24:31 p.m. Eastern); new implementation stops at
16:14:31 UTC. This fixed window permits chaining otherwise bounded campaigns.
The existing thread heartbeat was updated for this window; no duplicate was added.
It must be paused at completion. The broad prior goal remains paused.

Start with acceptance-002: the compatible exact colormap load/free pair, one shared
header and genuine combined fixture, then the unchanged registered build/test/record
checkpoint. Follow ../continuation-2026-09-13/next-integration.md and
../../docs/campaign-rules.md. Continue module by module afterward, favoring prepared
team-not_done functions and one Sol medium implementation worker with independent
coordinator review. Keep each campaign within45 minutes including10 minutes for
review/publication, and each function/preparation within15 minutes; ten worker
compiles/five no-improvements remain the function limits. Never reset expired caps.

Record actual results, source/target hashes, all experiments and failures, private
receipt hashes, focused fixtures, acceptance promotions, commits and CI in status
and checkpoint reports. Exact full spans, all address operands and zero differences
remain mandatory. Near/body-only results are never promoted as exact. Only authored
fixtures execute, through locked Docker. Original game/compiled content is static
and private; no patching, original execution, upstream/develop merge or purchases.
Publish verified checkpoints to the existing personal-fork branch. Stop on concrete
missing prerequisites with preserved evidence; do not invent unattended progress.
