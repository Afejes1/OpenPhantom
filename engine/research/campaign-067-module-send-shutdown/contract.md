# module_send_shutdown

Conditionally dispatch event2 and clear bit0 only after a zero callback result, preserving the live post-callback status word.

Full/body198 bytes at0x0046EF38, no alignment tail. Parent independently inspected the complete disassembly; reviewer checked the next entry and manifest-not_done identity. Finder operand+24 targets46F529; head operand+99 targets6D64C4. Cdecl int(int id), three-word callback(event2,0,0), signed result. Targeted missing/ineligible returns1; ID0 caches head, walks live next, filters bit0-set nodes, and returns0. No direct callers were recovered; lifecycle names follow contributor terminology and observed flag/event mechanics.

Focused-only case: no promotion or second shared full regression in this one-hour window. The accepted finder is linked into guarded authored fixtures. Eight status patterns include the target bit and high-word values; lengths0..5, rotations, duplicate/signed/missing IDs, extreme callback returns, current-status replacement, current-link truncate/skip, endpoint mutations and full snapshots are retained from the reviewed lifecycle matrix. Expected post-callback flag writes are predicted only on success. Fatal observer bounds protect indexing. No arbitrary pointer dereference, original execution, byte exclusions or warning waivers.

The original has an extra post-callback result copy to stack-12 in its broadcast branch. Its source/compiler origin remains unresolved. Initial natural source deliberately has no invented dead local; if complete extents differ, report research with no mismatch percentage rather than shorten the target or fabricate a frame.
