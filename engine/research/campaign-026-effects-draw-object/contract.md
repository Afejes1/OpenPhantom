# Static contract

Two rendering callers. Optional halo callback precedes live actor flag and signed slot reads; third stack word unused. Forward transform unchanged, no slot upper bound at this bridge.

Full 69-byte span at `0x00438E78`; body 69 bytes. Static body, caller references and all operands reviewed. Team not_done and no local duplicate. Original game is never executed.
