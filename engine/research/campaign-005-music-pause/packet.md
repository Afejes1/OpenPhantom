# Prepared music wrapper: music_pause

Team-not-done bp/bapmusic.c. Full interval 0x004107E9..0x00410825: 60 body/full bytes, no alignment. Coordinator inspected full Ghidra body, every instruction, next entry, XREFs and target import thunk. Original identity, complete PE relocation inventory and direct call target independently verified. VC5 C /Od /MT. No original code executed.

Unconditionally call lock(), increment current lock_depth, pause_backend(), unlock(), decrement current lock_depth, then set paused = 1. No initialized guard and no already-paused guard. All callbacks observe old pause flag (subject to their own mutations), final write happens after unlock and counter decrement. Verify order, before/after counter visibility, callback mutations, repeated pause, initial paused0/1/negative, and state-independent call execution. Exclude signed counter overflow. Pause backend0x49A30E is a no-argument import thunk. Full caller/thunk inspected; dispatcher, initialize and menu caller evidence agree.

Interface void op_music_pause(void). Globals are int op_music_lock_depth and, as used, op_music_initialized/op_music_paused. Model the actual indirect IAT slots as extern no-argument function pointers: typedef void (__stdcall *op_music_lock_fn)(void); extern op_music_lock_fn op_music_lock, op_music_unlock. These declarations represent the observed import slots at 0x8C1774/0x8C172C; they do not assert the original source declaration spelling. Use extern void op_music_service(void) or op_music_pause_backend(void) for the direct call to the respective no-argument import thunk. No parameters are pushed; zero-argument ABI has no stack cleanup ambiguity. Fixture binds authored stubs to the slot variables and executes none of the imports.

Worker owns candidate.c, api.h, behavior.c, README.md, worker-log.json only. Existing rules/caps apply independently: 15min/10invocations/five successive nonimprovements, no target changes, fabricated frames, warnings, exclusions, raw instructions, Git or metadata edits. Freeze on exact result for independent final review/verification. Native compile only; fixture execution locked Docker only. Complete before overall worker deadline 04:12:17.
