# world_mangle_names

complete span exact. Original VC5 /Od /MT; 158 complete bytes, 0 verified operands. The final fixture passed 15 checks with0 failures.

Owned actor pointers for every positive count, full32-byte names, early/late NUL, dots and high-bit bytes. NULL/zero/negative/INTMIN counts and repeated complete-state retention. Local temporary transformation is supported by raw-byte proof, not claimed observable from behavior alone.

Shared acceptance remains pending. Whole-executable placement remains unverified. See [receipt](result.json), [contract](contract.md) and [attempt log](worker-log.json).
