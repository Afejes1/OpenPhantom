# Load extended colormap

load and track a colormap while signed tracking capacity remains.

**Complete 73-byte relocation-adjusted-match.** Final independent native/Docker verification passes
40 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed cdecl single-name loader contract, signed live count and successful return when capacity is full. Parent corrected a fixture sentinel bug: expected writes now use the explicit supplied slot, not callback control input. Added callback transitions into and out of capacity and owned sentinel addresses; full128-slot oracle passes.

Full73-byte function is relocation-adjusted exact with all6 operands verified. Invalid negative insertion indexes remain outside the fixture domain; null-load negative count is covered. Shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
