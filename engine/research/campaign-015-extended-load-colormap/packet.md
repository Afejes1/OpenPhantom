# Prepared extended load colormap

cdecl void *op_extended_load_colormap(const char *name). One local pointer captures op_rd_colormap_load(name). Callee 00478737 is cdecl void*(const char*), one stack argument and cleanup4; the old decompiler thiscall signature is incorrect. If pointer nonnull and SIGNED int op_extended_colormap_count <128, append pointer to void *op_extended_colormaps[128] at live count then increment count. Return loaded pointer even when full. Caller 00428DF8 is colormap attachment. Fixture: null result, counts0/127/128, load callback mutating count before append, complete array/global state, null result with negative count. Do not test nonnull negative index or count overflow. No guard may be added.

Full original body and interval verified through the next function entry; no alignment or table tail. Every listed operand independently validated against owned original PE and complete HIGHLOW relocation coverage before implementation. Team manifest: not_done, bp/extended.c. Use C89-era VC5 C, /Od /MT and existing runner. No masks, assembly, byte insertion, manufactured frames, warning waivers or target edits. Per function15minutes/10compiles, stop after five no-improvements or campaign worker deadline. Freeze near results rather than polish.
