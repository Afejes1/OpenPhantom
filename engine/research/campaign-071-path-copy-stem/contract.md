# path_copy_stem

Cdecl void(const char*source,char*destination). Full/body133bytes, no tail. Destination byte0 is cleared before helper calls; exact alias therefore empties the source and disjoint buffers retain the source. Partial overlap is outside the fixture contract. Copy-extension uses intrinsic strcat, including the observed destination scan (not strcpy). Stem copies basename through the byte before extension, or live strlen when no extension. Directory copies the first-directory through file offset, or strlen if missing filename. Drive-directory prepends the char drive and colon/backslash before appending directory. All bounded copies preserve existing op_copy_keyframe_name identity.

Fixtures link four reconstructed helper bodies plus the explicitly unaccepted separator test dependency. Guarded96-byte source/output arrays, interior pointers, four alignments, exhaustive short paths, longer authored examples, exact aliases and bounded post-NUL data cover the chain. Full expected buffers are prepared before invocation; upper/search/copy observers verify clear/prefix/copy-stage visibility and exact arguments. Expected copy bytes use immutable source snapshots with independent NUL padding. No arbitrary offsets, CRT negative-character calls, original execution, exclusions or warning waivers.
