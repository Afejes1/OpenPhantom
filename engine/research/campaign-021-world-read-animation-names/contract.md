# Static contract

cdecl int op_world_read_animation_names(OP_STREAM*,OP_WORLD*,OP_HEADER*). Header uint count+A8,payload_size+AC. World uint count+1E8,payload_size+1EC, name-table pointer+1F0, actors pointer+1F4. Capture original name-table byte cursor BEFORE stream read(cursor,(int)header->payload_size,1,stream). Return0 only if read returns0; any nonzero succeeds. After callback advance captured cursor by LIVE headercount*4; signed i loop against live signed-count interpretation. For each index load signed length from LIVE world name table entry, replace that entry with current cursor pointer, advance captured cursor by length. After loop publish live headercount and live payloadsize to world in order. If published count nonzero AND actor pointerNULL, allocate unsigned count*4 and assign result evenNULL. Return1 regardless allocation failure. No payload-validation or clearing. Typed union length/name entries of exactly4 bytes permit explicit length-to-pointer representation under VC5; use declared typed fields, not aliasing casts into char storage. Original source arithmetic is compiler-dependent for invalid data; fixtures keep all actual pointer arithmetic in owned bounded buffers and signed lengths valid. Fixtures: readfailure with no stores beyond callback effects; zero/one/three names, zero/varied lengths, existing actor pointer, NULL allocation and success, stream callback retargets name-table while captured cursor remains original, mutates count and size, allocator observes completed table/world publication and mutates actor pointer then returned pointer overwrites it. Snapshot both tables/buffers, fullworld/header/stream and guards; exact argument/order/counts. No original data.

Chunk dispatcher case0x10 call0041E9A0 passes stream/world/header; full body and allocator/stream callees checked.
