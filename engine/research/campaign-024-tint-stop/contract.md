# Static contract

Inactive path changes nothing. Active path clears active, mode, start and duration, preserving all unrelated state. Three callers.

Complete 56-byte span at `0x004393FA`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
