# Dispatch sound subsystem event

dispatch sound subsystem lifecycle events.

**Complete 256-byte relocation-adjusted match.** Final independent native/Docker verification passes
121 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed event mapping, callback ABI, default status, case 13 to 14 fallthrough, and both original switch tables. Fixture independently enumerates every event 0 through 26 and signed out-of-range values, observes callback-time quality, callback return propagation, and retained callback mutations.

Full 256-byte function span is relocation-adjusted exact, including 44-byte jump and 25-byte mapping tables. Standalone focused research evidence; shared ABI integration remains pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own valid listing; no native-listing fallback was used.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-014/review.json).
