# Campaign018: world cleanup and skip wrapper

One Sol medium worker processes six prepared team-not_done functions sequentially.
The coordinator independently reviews ABI, behavior and complete byte evidence.
See ../../docs/campaign-rules.md and campaign.json for exact caps and deadlines.
Each function gets at most15 minutes or10 compiler experiments; stop after five
non-improving attempts. Reserve the final10 minutes for review and publication.
No source bytes, assembly insertion, warning waivers or comparator changes.

Five cleanup functions and one skip wrapper cover639 complete original bytes and
11 call bindings. Inline array extents are explicitly partial views: recovered
count and entry offsets do not establish full original capacities. Fixtures use
owned in-range storage and callback mutations to check live reload/clear order.
Signed loop bounds, NULL forwarding and ignored callback returns must be retained.
The overall authorization ends2026-09-13T16:24:31Z, without extension.
