# ui3d_begin

Complete span 0x0045C1FC to0x0045C214, 24 bytes with no unresolved tail. All five selected functions are contributor not_done and had no prior candidate. Ghidra decompilation, disassembly and function boundaries establish cdecl signatures and full control flow.

Debug toggle maps zero to1 and every nonzero word to0. Mouse-focus setting retains all bits. Widget activation returns the signed word at offset4; only the8-byte prefix is modeled, with a larger owned fixture and guards. UI3D begin changes only zero active-state to1. End acts only on exactly1, invokes material-bucket flush402155, draw-queue flush488000 and backend finish46C12A in order, then sets active0 even if callbacks mutated it. These three external functions are authored observers in this focused fixture and are not claimed reconstructed here. The draw-queue routine visibly returns1; its callback status is ignored by this wrapper.

Fixtures check full owned-widget snapshots and all related globals, signed edge values, skipped paths and ordered callback-time mutation state. No game assets, original execution, ignored bytes or warning waivers. Related get-color-scheme462738 (body10/full24), UI-active4612BA (body10/full22) and load-state45C556 (body17/full26) remain unattempted because their tails require separate layout evidence.
