# shield_set_visibility_bypass

Exact full-span function match. 67 bytes; 0 differing bytes; 1 verified operands. VC5 /Od /MT. Final isolated fixture: 840 checks, zero failures.

The stable visibility-bypass ID also sets the no-save/transient flag; both observed effects are documented in the contract.

Research only; no registered promotion, byte exclusion or new warning waiver. Final linked placement remains unverified. See [result](result.json), [static contract](contract.md), [eligibility](eligibility.json) and [attempts](worker-log.json).
