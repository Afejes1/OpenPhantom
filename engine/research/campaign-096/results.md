# Campaign096

Four sound lifecycle/control functions match354 complete bytes and22 verified operands. The first compiler runs were byte exact; review of the neighboring attenuation setter then corrected an inferred buffer slot from format-pointer to signed volume before acceptance. The corrected second runs remain exact. Both generations of final receipts are retained; the final four pairs pass193,920 authored fixture assertions. Shared acceptance pending.

This correction shows why byte equality and semantic type review are separate gates: two32-bit parameter types can produce identical instructions. The selected sources use volume(OP_AUDIO_BUFFER*,int) and the signed global op_audio_primary_volume. Original bytes, matcher rules, deadlines and all prior caps were unchanged.
