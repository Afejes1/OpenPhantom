# palette_resource_release

Pass the initial current pointer to release_resource, then overwrite current with null and return1. The callback may replace the current global; that replacement is still cleared afterward. Full original interval is41 bytes:34-byte body plus seven CC alignment bytes through00414edf before00414ee0. The complete41 bytes remain required; no tail waiver or manually inserted bytes is permitted.

All storage and strings are authored. Complete guarded map/name/global snapshots and callback order are checked. Original program execution, copied game data, byte exclusions and warning waivers are prohibited. Individual functions remain separate from shared acceptance and whole-executable identity.
