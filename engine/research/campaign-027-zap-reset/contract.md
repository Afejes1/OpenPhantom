# zap_reset contract

Clear exactly 64 records of 120 bytes, then zero the aggregate count. Ordinary memset is compiler-intrinsic; no raw instructions or padding.

Complete span 31 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
