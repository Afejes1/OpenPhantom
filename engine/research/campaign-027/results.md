# Campaign027 results

Six untouched team-not_done functions match all461 complete bytes, each on the first implementation compile. All23,949 focused checks pass. Native and Docker code/fixup inventories agree, and all20 operand bindings have independent review.

| Function | Bytes | Checks |
|---|---:|---:|
| detach_zap | 116 | 4614 |
| halo_free_all | 110 | 648 |
| zap_has_actor | 131 | 18435 |
| zap_reset | 31 | 120 |
| zap_shutdown | 23 | 72 |
| zap_startup | 50 | 60 |

The zapline cohort covers cached resource startup, shutdown, full pool reset, active endpoint detach and an active/mode-gated query. Fixtures enumerate every endpoint position and preserve all unrelated pool bytes. NULL actor values are compared only and are not evidence for dereferencing a NULL actor. The halo cleanup verifies clear-before-release, post-callback count decrement, dynamic later slots, no revisits and final count zero. Its lifecycle provenance remains unresolved because no direct callers were found.

Per-case receipts record source snapshots, compiler image, report hashes and verified native listing fallback. Existing semantic Ghidra names/comments are preserved and the project saved. No original game execution, original resource strings, byte exclusions, forced padding or warning waivers.

No full regression suite ran for this research commit. Registered acceptance remains69 functions /32 events. These six exact functions will be combined with the next research batch for canonical integration. This is complete function evidence, not whole-executable identity.
