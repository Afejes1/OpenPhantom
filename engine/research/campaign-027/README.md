# Campaign027: zapline lifecycle and halo cleanup

Six untouched team-not_done functions, 461 full bytes. Prioritize the actual zapline detach callee in accepted object teardown, resource startup/shutdown, pool reset and endpoint query. Halo free-all is a related cleanup helper with no direct callers; preserve that provenance limit. The parent implements and Sol reviews independently.

Each case has a fifteen-minute/ten-implementation-compile cap, stopping after five consecutive non-improving attempts. Require complete original spans, all operands, native/Docker code and fixup equality, and focused owned-state fixtures. No original game execution, original resource names, synthetic padding, byte exclusions or warning waivers.
