# Ready assignment: scan-line free wrapper

Team bapline_free, not_done, source line 1322, unknown original file. Entry 0x004084A1; RET 0x004084B7; next function 0x004084B8. Complete body/span 23 bytes, no trailing alignment. No absolute operands. The sole direct call at 0x004084AE has operand offset 14 and target 0x00495452. This is the tracked deallocator: its body walks an allocation header at pointer-16, updates linked bookkeeping/counters and releases that header through 0x0049C330. Bind op_scan_release to the tracked wrapper, not CRT free. Ghidra incoming-XREF query returns none, with normal limited-search scope.

One pointer argument, cdecl, void return. If nonnull, call op_scan_release with the exact incoming pointer; null produces no call. No record reads/writes before calling the deallocator. There is no local storage or extra error handling. Use the previously reviewed op_scan_line type if helpful; op_scan_release accepts void*.

Fixture: null produces zero calls; each of two distinct supplied record addresses produces exactly one call with identity preserved. Authored stub may inspect/snapshot state, but must not free stack memory or execute the original allocator. Assert record unchanged by this wrapper using a no-write stub.

Coordinator-prepared static evidence, 2026-09-13. Original WMAIN.EXE is static only. Use the pinned original VC5 C frontend /Od /MT. No exclusions, synthetic frame objects, byte insertion or warning waivers. Read engine/docs/campaign-rules.md. The existing campaign-001 runner stages the case inputs by basename, including the previously reviewed line.h. Do not edit that shared header or any parent-owned target/case/packet. Native runs compile only; authored fixture execution is permitted only via locked Docker. Reproduce with: python engine/research/campaign-001/verify.py --case CASE_FOLDER --runner native --reference C:/Dev/Ghidra/projects/TPM/WMAIN.EXE; final Docker adds --behavior and --expect-report NATIVE_REPORT. Parent independently verifies final inputs. Each function has a separate 15-minute/10-worker-compile cap, five consecutive no-improvement stop. Each worker compile, including verification repeats, must be logged with hypothesis, source hash and report path. Freeze candidate/fixture before final peer comparison. No original call body may be executed.
