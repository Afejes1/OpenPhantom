# util_file_exists

Int cdecl single path parameter, confirmed by caller004974AC pushing path at0049760D. Current Ghidra thiscall inference is stale. Open via service+0x30 receives original path and fixed mode identity; captures handle. Zero returns0 without close. All nonzero signed handles close through reloaded global services+0x34, ignore close result, return1. Two guarded tables test callback retargeting with0,1,-1,INT_MIN,INT_MAX handles and varied ignored close returns. NULL path is observer-only forwarding; no real filesystem access. Recover open field from previously unknown service slot without changing sizeof0x44 or existing close/read/write offsets.

Independent static review confirmed all complete spans and all six cohort operands. No alignment tails, byte exclusions, warning waivers, original execution or whole-program claim. Test callbacks and data are authored; original strings and raw decompiler output are not included.
