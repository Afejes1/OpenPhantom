# Campaign 095

Three frozen research candidates, zero accepted functions. Resource close/eof/tell compile to173/113/111 bytes against complete original extents176/115/113. Different extents have no mismatch percentage. Final native/Docker pairs agree and all114,960 authored fixture checks pass. Shared acceptance is not attempted.

The selected ordinary C byte-stride expressions remain experiment2. EOF alternatives using integer address arithmetic and the C++ frontend did not improve its extent. An attempted one-based representation required an unsupported negative addend and failed preflight before compilation; no matcher rule changed. The archived candidate.cpp records that unselected frontend experiment. Existing parked task/resource/layout limits remain in force.

Compiler address generation (ADD into the accumulator versus the original LEA into another register) is the remaining lead. Further source work requires new evidence and a later allocation, not a reset of this campaign.
