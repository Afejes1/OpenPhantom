# shield set offset

Research-only extent mismatch:83 candidate bytes versus80 original bytes. The explicit pointer return adds a load; intended original return use remains unproved. The independently reviewed isolated fixture passes 396 checks. Native and Docker function bytes and relocation inventories agree.

[Source](candidate.c), [contract](contract.md), [fixture](behavior.c), [receipt](result.json), [experiments](worker-log.json), [review](../campaign-024/review.json).

Research is separate from canonical acceptance and final executable placement. No original game or real backend execution, byte exclusions or warning waivers.
