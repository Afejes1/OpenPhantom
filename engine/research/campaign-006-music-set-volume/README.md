# Music volume setter

The candidate preserves the original VC5 equality path, including its unordered
quiet-NaN exit, initializes a null buffer local, and updates the cached input only
after callbacks and live depth balancing. Its first native compile matches the
complete 115-byte target with all eleven bindings verified.

Worker native report: `engine/build/campaign-001-music_set_volume-a8307ee63d46/research-result.json`.
The fixture covers equality, signed zero, quiet NaN in either operand, the
initialized gate, infinities, exact buffer/input forwarding including null, and
callback mutations/timing without overflow. Coordinator final verification is complete. The original executable was not executed.
Final parent review and fresh native/Docker verification confirm the complete 115-byte relocation-adjusted match with 42 focused checks passing.
See [result.json](result.json) for source, toolchain, object, fixture and report hashes.
The verified native-listing fallback was used after code/fixup equality; no bytes
were excluded. Final executable layout and accepted-history promotion remain pending.
