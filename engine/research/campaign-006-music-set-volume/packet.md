# Prepared target: music_set_volume

Team-not-done bp/bapmusic.c. Full interval0x004106CC..0x0041073F, 115 body/full bytes; no alignment. Coordinator reviewed full body/instructions, next function, callers and callees. Original identity, complete PE relocation inventory and direct call targets independently verified. Pinned VC5 C /Od /MT; immutable target.

Observed API (cdecl except explicitly typed lock slots):
`void op_music_set_volume(float volume); extern float op_music_volume; extern int op_music_initialized, op_music_lock_depth; typedef void (__stdcall *op_music_lock_fn)(void); extern op_music_lock_fn op_music_lock, op_music_unlock; void *op_music_grab_buffer(void); void op_music_apply_volume(void *buffer, float volume);`

A normal local buffer pointer starts null (original store0 at [EBP-4]). Compare cached float with input before initialized gate. Equal values exit unchanged; positive/negative zero compare equal, so retain cached sign. Original x87 FCOMP then TEST AH,0x40 also exits on unordered (quiet NaN in either operand), under the pinned VC5/default masked FPU environment. Record this observed compiler behavior explicitly; do not use modern-C NaN assumptions or insert raw instructions. Next initialized==0 exits; any nonzero value enters. Call lock(), increment CURRENT depth, grab_buffer(), apply_volume(returned buffer, original input), unlock(), decrement CURRENT depth, then copy original input bits to cached volume. Do not read changed cache after callbacks to replace the input, clamp volume, reject null buffer, normalize callback outcomes or recheck initialized after entry. Grab thunk0x49A308 takes no args; apply0x496CA0 independently reads buffer[EBP+8] and float[EBP+C], returns void; pointer/float order proved against caller pushes. Actual imports are never executed.

Fixture ordinary equal/different values, signed-zero equality with no cache sign rewrite, both quiet-NaN operand positions (unordered skips), infinities where useful, initialized0 and non-1 values, call order and exact buffer/input bits, null returned buffer still forwarded, cached value visible before final store, callbacks mutating depth/cache/initialized with final input overriding cache only after unlock. Use memcpy for bit setup, valid authored addresses, no pointer dereference in apply stub, and safe integer counters. Exclude signaling NaNs, altered FPU control and actual DirectSound behavior.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. No target/case/shared tooling/Git/Ghidra writes. Per function15min/10candidate compiles/five successive nonimprovements; freeze when exact. Record all input hashes and attempts including failures. Native compile only, authored fixtures locked Docker only. No exclusions, warning waivers, raw bytes, artificial frames or original execution. Parent final pair/review is separate and must fit campaign deadline04:50:17; worker stop04:40:17.
