# Set sound master volume

Update normalizedgain and3Dchannel volumes then forward raw digital master volume.

**Complete 224-byte relocation-adjusted match.** Final independent native/Docker verification passes
56 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed integer normalization, x87 stored intermediates and inclusive clamp grouping, exacttype1 gate, postcallback gain/driver reloads and raw finalvalue forwarding. Expanded fixture independently checks gain bits for signed boundaries, finite clamp outputs,qNaN-to-zero, skippedtypes, unchangedrecords and livecallbackmutation.

Default pinned x87 environment only; explicit >=0 and <=127 grouping matches unordered behavior and all224bytes. Real Miles callbacks replaced by authored stubs; no input-range normalization added.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own valid listing; no native-listing fallback was used.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
