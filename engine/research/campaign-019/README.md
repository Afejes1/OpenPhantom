# Campaign019: world record readers

Six prepared team-not_done cases cover907 full bytes and6 stream-call bindings.
One Sol medium worker processes them sequentially under the existing campaign
rules:15 minutes/10 experiments per function, five no-improvement attempts,
45 minutes total including ten reserved for review and publication.

The local-vertex reader returns success on a payload mismatch; preserve this
observed behavior. Dialog uses a bulk read before capturing its live record
pointer; the other readers capture a pointer before their per-record loops.
All contracts specify unsigned wrapped products and signed live loop bounds.
Synthetic callbacks only; no original game/backend execution, comparator changes,
new warning waivers or byte exclusions. Whole-executable identity remains pending.
