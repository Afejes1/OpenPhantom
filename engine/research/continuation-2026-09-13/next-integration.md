# Next integration slices

The new results are exact function evidence, not automatic registry promotions.
Keep one Sol medium implementation worker and independent review. Prepare at most
six related functions at a time and preserve the existing per-function caps.

Start the next acceptance slice with extended_load_colormap and
extended_free_colormaps. Their current headers already agree on signed int
op_extended_colormap_count and void *op_extended_colormaps[128]. Unify those in one
header and use a combined fixture that calls the real reconstructed load and free
functions together. The backend loader/free callbacks remain explicit synthetic
fixtures. Exercise repeated loads to capacity, failure without insertion, ordered
free with callback mutations, retained slots and final count reset. Rebuild their
unchanged full target spans with VC5 after common-header integration, then pass the
existing registered shared build/test/record checkpoint before promoting either.

Treat the face family as a separate type-reconciliation task. The attachment view
places colormap at0x60; advance-cel reads signed cel_count at0x64, while the selector
compares that same word unsigned. Advance uses a signed cel field0x24 and selector
uses unsigned storage. A canonical layout must preserve the exact signed operation
at each use; simply changing every declaration to one signedness can change code
or behavior. Reconcile offsets with shared structures and recompile every affected
function. The attachment remains6/117 bytes different and is not eligible for exact
promotion. The selector stores two callbacks with different visible cdecl argument
counts; its C89 unspecified-parameter slot is documented and must not be silently
replaced with an incompatible prototype.

The sound lifecycle dispatcher connects nine previously reconstructed sound
callbacks. Integrate it only after the existing sound level/channel/placement
partial-type and callback declarations have been reconciled. The resource handler
is still9/288 bytes different and remains near-match research. Do not combine all
research main functions or point the accepted registry at untracked research paths.

Keep exact, near and research status separate from team-reported completion. The
original matcher, full extents, zero-byte criterion and accepted-history recorder
remain unchanged. Full regression belongs to these coherent integration checkpoints;
focused fixtures and feature CI remain sufficient for ordinary research pushes.
