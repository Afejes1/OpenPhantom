# One-hour continuation after acceptance

The user authorized another hour after the acceptance checkpoint. The fixed window
is September13,2026 11:06-12:06UTC; new implementation stops11:56. Work happens in
this active thread; the old overnight heartbeat remains paused. Do not extend the
window or start another campaign after its deadline without a new instruction.

One Sol/medium worker processes prepared cases sequentially. Each campaign keeps
its45-minute maximum and final10-minute review reserve. Each function keeps its
15-minute/10-worker-compile/five-no-improvement cap. The one-hour authorization
permits another bounded campaign only if verification/publication fits before12:06.
Complete original extents and all operands remain mandatory;5% is only a parking
threshold. Native compiles only, authored fixtures execute in locked Docker.
No original game execution, new waiver, byte exclusion, upstream/develop merge or
accepted-history promotion occurs merely because a case matches.

Read [status.json](status.json), [campaign rules](../../docs/campaign-rules.md),
and [acceptance results](../acceptance-001/results.md) before resuming. Preserve
all parked case budgets. The coordinator owns shared files, Git and Ghidra metadata.
