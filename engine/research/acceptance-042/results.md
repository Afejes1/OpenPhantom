# Acceptance042 results

Thirteen platform and math functions bring the accepted baseline from225 to238. They match405 complete original bytes after29 verified address operands, with zero differences and no exclusions or original-VC5 warning waivers. Clean source 371444e3748d03f970725f5670bc2c90f104e215 builds as 20260914-052337-484176a3. All238 comparisons and shared authored tests pass. [Event65](../../evidence/runs/000065-778ccb8a19969a5d.json) records the source snapshot; [same-source supplemental CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34827841775) succeeds.

The shared fixture retains56,149 focused checks and adds16,214 connected checks:2,400 heap-chain,13,734 timer-chain and80 vector-angle checks. Heap sequences call actual allocation, identity, reallocation and release wrappers through authored observers. Timer sequences carry callback-mutated flags and scales from actual ticks into actual seconds. The linked vector-angle implementation now calls the actual scalar arccosine implementation. Its historical mutation/ABI probe remains a fixture-only compile of the unchanged source under local names; it is not an accepted byte artifact. Canonical function objects are compared separately.

The first supplemental run exposed modern cosine argument rounding in two near-zero cases while the original fixture passed. A separate modern expected table corrects that test; original VC5 tables and bounds remain unchanged. The original integration prose counted31 operands by including the unpromoted sine research case; the actual promoted total is29. Both superseded receipts and explanations are retained. One initial targeted fixture compile found an unsigned test counter compared with a signed counter; its type was corrected without a warning waiver.

Independent source/oracle review and all focused source/report hashes completed. The reviewer's optional Python numeric recomputation remained blocked by automatic approval review despite the prior authorization; no workaround was used. An extra independent JSON-prefix helper was inconclusive and its corrective read hit capacity; parent's successful exact-prefix comparison is the prefix evidence. No real COM, filesystem, heap service or original game runs. Existing supplemental modern CRT suppression remains unchanged. All13 additions are team-not_done. Whole-executable identity remains pending.
