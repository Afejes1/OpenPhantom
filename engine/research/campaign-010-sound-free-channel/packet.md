# Prepared target: sound_free_channel

Team not done, bp/bapsound.c. Complete 0x00417567..0x00417659, 242 bytes, 5 bindings. Existing trusted Ghidra name stop_and_release_sound_slot remains. void op_sound_free_channel(unsigned int slot).

Channel128 layout: void *sample at0, *sample3d at4; int type at8; record *record at0xC; unsigned flags at0x10; opaque[0x2C] from0x14; unsigned state40 at0x40; opaque[0x2C] from0x44; unsigned state70 at0x70; opaque[4] at0x74; int *caller_handle at0x78; float *position_owner at0x7C. Record60: opaque[0x38], void *resource. extern channel op_sound_channels[12]. State40/state70 remain unnamed four-byte reset fields; no semantic inference beyond observed zero stores.

Callbacks: void (__stdcall *op_sound_end2d)(void *sample); void (__stdcall *op_sound_end3d)(void *sample); cdecl int op_sound_mark_resource(void *resource,int flag); int op_sound_release_resource(void *resource). Actual Miles IAT @4 and full resource callees confirmed; stop-all, initialization, voice and status callers all supply a single slot.

If unsigned slot>=12 return untouched. Capture channel=&channels[slot]. If flags&0x20000, use type==0 for end2d(current sample), otherwise end3d(current sample3d). Then if CURRENT caller_handle nonnull write -1 through it and clear field. If CURRENT record nonnull, optionally mark its CURRENT resource with1 when CURRENT flags&0x4000; release resource from RELOADED channel record; then clear resource through RELOADED channel record again, then set channel record null. Finally position_owner=null; flags=0x80000 (assignment, not OR); state40=0; state70=0. Sample handles, type and other fields are preserved. No initialized gate, no return status, no extra guards after callbacks.

Fixture: slot0/11 and12/~0, inactive and active2D/3D (including noncanonical nonzero types), absent/present caller_handle and record, mark-bit paths, null resource allowed with valid record. End callback changes handle pointer/record/flags before subsequent branches; mark changes record/resource before release; release changes record pointer before final clear. Keep all dereferenced pointers owned/valid. Assert exact callback order, full expected channel bytes and unchanged neighbors; verify caller handle invalidation, final flags and all four resets independently. Guard every test pointer/index. Preserve original crash contract if callbacks null a required record; do not exercise invalid pointer paths.

The coordinator verified every instruction against the owned reference PE and every absolute operand against its relocation inventory. All target operands and complete extents are immutable. Use pinned VC5 C /Od /MT and the existing campaign-001 verifier. All callbacks below are cdecl unless explicitly stdcall. Do not change ABI, targets, flags, tools or accepted history. No raw bytes, artificial frames, warning waivers or byte exclusions.

Worker owns candidate.c, api.h, behavior.c, README.md and worker-log.json only. Work sequentially; each case stops at 15 minutes, ten candidate compiles or five consecutive nonimprovements. Record actual clock observations, each hypothesis/hash/report, failures, and explicit timestamp bases. Native compile only; coordinator executes authored fixtures in locked Docker after review. Guard fixture array/pointer accesses and use independent expected states, not copies of the candidate algorithm. Worker stop 06:49:21 UTC; publication deadline 06:59:21 UTC.
