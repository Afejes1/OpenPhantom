# Stop and release sound slot

stop and release sound slot.

**Complete 242-byte relocation-adjusted match.** Final independent native/Docker verification passes
37 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed unsigned slot bound, fixed backend branch, live caller-handle invalidation and record/resource reloads after end/mark/release callbacks. Corrected event oracle distinguishes mutually exclusive end callbacks. Parent added typed full-channel/full-record expected states and the unmarked null-resource release path; unrelated fields and neighbor channels are preserved.

Real Miles and resource backends are authored stubs. Callback changes keep all dereferenced record/handle pointers valid. Complete 242-byte span and all five address operands match without exclusions; field40/field70 purpose remains unspecified beyond observed zero stores.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing; no native-listing fallback was used.
The listing lacks terminators; complete COFF inventory and native/Docker
code/fixup equality were retained and checked.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-010/review.json).
