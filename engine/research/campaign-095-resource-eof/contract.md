# Resource file resource_eof

Complete original span 00473544..004735B7, 115 bytes, 5 PE-verified address operands, no tail. Cdecl int(int handle). The captured slot uses static base006D5D74 + handle*102C before validation. Slot fields: backend handle0, position4, EOF18, real-file flag1C, size102C. Existing host services00868640 are refined at assertion18 and debugprintf54; separate backend pointer006F8350 supplies close34/EOF44/tell48. Diagnostic arrays remain external symbolic data; fixture text is authored and no original string-data identity is claimed.

Fixture uses34 owned slots and safe handles0/1/2/15/31/32/33; only0/33 are invalid and the returning assertion observer keeps execution within owned memory. Negative/extreme handles and arbitrary callback effects are excluded. Two byte patterns, inactive/internal/real states, signed return/position/EOF boundaries, three log gates, and assertion/backend/debug mutations are preplanned. Complete slot-array/table snapshots are captured before candidate calls; callbacks apply only explicitly expected changes. Live host/backend pointers, log handle and slot fields are observed independently. Close clears the captured slot after close callback mutations; neighboring changes survive. EOF/tell retain their backend callback changes and returned word. API callbacks are cdecl and printf varargs is checked as(int stream,const char *format,int handle). No real files or original code execute.

The selected function has its own attempt history and15-minute cap; the capped resource_backend_handle candidate is unchanged. No byte exclusions, warning waivers or padding. Shared acceptance remains pending.

Research freeze: the selected second candidate retains the observed signed <=0 validation. Emission uses ADD EAX rather than original LEA ECX, with rotated later registers and a different complete extent; no mismatch percentage is assigned. EOF additionally tested C++ with C linkage without improvement; its one-based-base experiment stopped at unsupported negative-addend preflight before compilation. Archived candidate.cpp is not the selected candidate. The comparison implementation and every prior parked cap remain unchanged.
