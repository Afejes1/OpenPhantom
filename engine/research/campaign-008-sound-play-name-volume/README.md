# Play named sound with volume

Set temporary volume and category2, play with flag0x800, then restore default volume.

**Complete 92-byte relocation-adjusted match.** Final independent native/Docker verification passes
38 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed setter-play-setter order, mode timing, flags0x800, saved signed return and bit-preserving volume argument; fixture first setter clears init to prove no re-gate. All five address operands and three source arguments independently verified.

Quiet NaNs only through float-return fixture helpers; original wrapper makes no numeric conversion. Backend behavior represented by authored callbacks. Full92-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
