# ripple_reset contract

Clear projected overlays before deactivating256 ripple records. Visit8 cached materials; destroy nonNULL then clear each slot after callback, including NULL slots. Complete138-byte span includes2-byte alignment tail; park if compiler emits136, without padding or exclusion.

Team manifest reports not_done and prior-campaign inventory found no attempt. Full extent and all operand bindings are required. No original game execution, original resource strings, byte exclusions or warning waivers.
