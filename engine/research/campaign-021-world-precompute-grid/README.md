# world_precompute_grid

complete span exact. Original VC5 /Od /MT; 158 complete bytes, 1 verified operands. The final fixture passed 99 checks with0 failures.

Exact row-major pointer oracles, sparse/NULL/zero-count cells, unsigned128/255 counts, zero/negative/INTMIN dimensions, bounded live table/width change, height contraction and future-count clear. Every callback and final state uses whole guarded comparisons.

Shared acceptance remains pending. Whole-executable placement remains unverified. See [receipt](result.json), [contract](contract.md) and [attempt log](worker-log.json).
