# colormap_load_entry

Read into an844-byte colormap through global services at00868640. Open(path,readmode) occurs before basename/copy. Open0 skips name/read/close, invokes gray fallback and returns1. Otherwise copy31 basename bytes and force name[31]=0. Read64-byte header and store four words atmap32..44 before marker validation. Ignore version and read return values. Invalid marker closes then falls back. Valid marker reads768 palette bytes, allocates0x4100, stores raw handle820 and rounds table816 upward to256-byte alignment, then reads0x4000 bytes. Reload live flags bit0 after that read; if set, allocate0x10100 and publish raw828/aligned824, then read0x10000 bytes. Close and return1. Either allocation failure closes then calls gray and returns1, without invented cleanup or resetting untouched fields.

Fixtures cover open failure, bad marker, both allocation failures, success with/without alpha, true pointer residues0/1/255, empty/short/31-byte/long bounded names, header version ignored, live post-light flags, allocator mutations overwritten by returned handles and close/fallback ordering. Every requested read destination is fully initialized with authored data even when the reported count is short; actual truncation behavior is outside this contract. Open mutation of the owned path is visible to subsequent basename lookup. No path/name overlap or allocation-address overflow is claimed. Full original581-byte span and16 address operands.

Team not_done is verified by numeric address. All buffers, paths, marker text and modes are authored. External mode/marker symbols bind to the observed original locations without reproducing original strings. The existing op_copy_keyframe_name name reuses the already accepted CRT entry identity at0049AC20; its fixture here observes this caller separately.

Two alternating file-service tables prove global-pointer reload after open and every read/write/close. Nonzero ordinary/high-bit handles are valid; zero open is the failure condition. Callback return values include0,1,all-bits-set and negative close/fallback values; they do not affect the observed result except open. Complete guarded844-byte map snapshots and owned data pools cover specified writes and callback mutations. Native compilation only; authored behavior runs in the locked Docker container. Original executable execution, byte exclusions and warning waivers are prohibited.
