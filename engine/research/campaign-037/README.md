# Campaign037: font lifecycle and measurements

Six untouched functions,893 complete bytes/45 operands. Existing system-font getter semantic type needs signed-slot correction at integration; prior byte identity remains valid. Resource names are external symbols backed by authored fixtures. [Plan](plan.json) preserves the initial preparation time.
