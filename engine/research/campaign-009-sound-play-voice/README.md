# Play voice sound

Release prior voicehandle and configure positional or nonpositional playback.

**Complete 180-byte relocation-adjusted match.** Final independent native/Docker verification passes
59 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed actual3-argument sourceABI from dialoguecaller, signedhandle release gate, positionalfield constants/order, inputlock branches, mode timing and final five-argument descriptorcall. Expanded fixture observes priorhandle and mode beforecallback mutations; source has noinitgate or postplayrestoration.

Observed caller discards wrapper result; authored void wrapper matches all180bytes. Existing Ghidra phantomthis inference is metadata only. Actualnextfunction417368 is separate and alreadyreconstructed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
