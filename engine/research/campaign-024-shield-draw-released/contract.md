# Static contract

Positive active count gates a fixed32-slot traversal of live active/stopping fields. Renderer calls precede live pass-counter increments; both pass globals clear after the scan. Nonpositive count is a no-op. One effects dispatcher caller.

Complete 134-byte span at `0x0043B67B`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
