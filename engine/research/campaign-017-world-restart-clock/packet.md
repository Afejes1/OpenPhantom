# Prepared world_restart_clock

cdecl void op_world_restart_clock(OP_WORLD *world). If null return. World unsigned timestamp0x50, mirror0x58, elapsed0x5C. Assign timestamp=op_clock_ticks(); mirror=LIVE timestamp; elapsed=0; op_world_clock_hook(world). Clock cdecl unsigned int(void), actual475B1D calls timer then subtracts globalword006F83C8 with32bit wrap; no args. Hook cdecl void(OP_WORLD*) at41EABC verified one stack arg and loop over script-name records. Its broader purpose is uncertain: do not invent stronger semantics. Fixtures null suppression, zero/high-bit/max ticks, clock-before-hook ordering, pre-hook visible newtimestamp/mirror/elapsed, clock callback mutation before stores, hook mutation retained, entire guarded world pre-call oracle. Caller load_level_world0043F89A. UnknownFUN_0041eb5a purpose clock restart supported by stores/calls. No extra local or guard.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
