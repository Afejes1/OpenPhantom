# Prepared target: sound_get_master_volume

Team-not-done bp/bapsound.c. Full interval0x00417459..0x00417477: 30 body/full bytes, no alignment. Coordinator reviewed full body/instructions, next boundary, callers and callee/global context. Complete PE relocation inventory and call destinations independently checked against owned original. Pinned VC5 C /Od /MT; immutable target.

int op_sound_get_master_volume(void); extern int op_sound_initialized; extern void *op_sound_driver; typedef int (__stdcall *op_sound_query_volume_fn)(void *driver); extern op_sound_query_volume_fn op_sound_query_volume;
Initialized==0 returns0 without callbacks. Any nonzero calls the actual IAT slot8C1758 AIL_digital_master_volume@4 with current driver, including null, and preserves int return (no clamp/normalization). Caller menu00441FA4 uses numeric result; initializer binds driver. Stub exact driver, count, returns negative/zero/positive and boundary integers; callback global mutations retained. No real Miles calls.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Use readable multiline fixtures. No target, shared tooling, metadata or Git edits. Same15min/10candidate-compile/five nonimprovement cap; freeze and report each result immediately. Native compile only; authored fixtures run only in locked Docker. No warnings, exclusions, raw instructions or original execution. Overall worker stop05:07:17 UTC; publication05:17:17 UTC. Parent independently reviews and verifies final pairs.
