# Get sound master volume

Return zero when uninitialized, otherwise preserve the Miles master-volume query result.

**Complete 30-byte relocation-adjusted match.** Final independent native/Docker verification passes
19 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed initialized zero gate, any-nonzero query, exact stdcall driver argument including null, unchanged int return and retained callback global mutations. Boundary integers are returned without arithmetic.

Actual Miles query and driver validity are represented by authored callbacks; no device behavior is executed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
