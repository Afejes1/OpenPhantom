# Sample random unit vector components

sample random unit vector components.

**Body exact (31 bytes), original full 36-byte layout pending.** Final independent native/Docker verification passes
6 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed full three-pointer downstream ABI against complete real callee and exact wrapper pointer arithmetic. Authored fixture uses owned float array, verifies exact X/Y/Z pointer identities, single callback and complete five-word output/neighbor bits.

Complete 31-byte body matches after verified relocations, but original full36-byte span retains 5 trailing INT3 bytes absent from candidate; final placement/padding is unresolved and this is not a complete function match.
Downstream RNG/trigonometry is stubbed; original full36-byte span includes5 trailing INT3 bytes absent from candidate31-byte body, so complete-function layout remains pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-013/review.json).
