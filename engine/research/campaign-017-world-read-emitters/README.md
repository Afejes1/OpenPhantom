# world read emitters

**Complete 94-byte relocation-adjusted match**, with every call operand verified and zero differing bytes. Final independent native/Docker verification passes35 focused checks.

Reviewed header count/disk size wrapped multiplication and mismatch exit, pre-read world count store and exact four callback arguments. Parent added aggregate stream/header/world guards and explicit expected sizes including a wrapped nonzero product.

External callbacks are authored fixtures. Shared types, actual backend integration and final linked executable placement remain pending. The explicit verified native-listing fallback was used after complete native/Docker code and fixup equality; the empty Docker listing is retained privately.

See [result](result.json), [all worker experiments](worker-log.json), [static contract](packet.md), and [parent review](../campaign-017/review.json).
