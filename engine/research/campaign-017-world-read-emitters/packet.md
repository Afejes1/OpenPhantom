# Prepared world_read_emitters

cdecl int op_world_read_emitters(OP_STREAM *stream,OP_WORLD *world,OP_HEADER *header). Header unsigned count0x2E8, disk_size0x2EC, payload_size0x800. World unsigned emitter_count0xCCC and void *emitters0xCD0. Local unsigned int bytes=count*disk_size with32bit wrapping. If bytes!=payload return0 with no callbacks/writes. Else world emitter_count=header count; return op_stream_read(world emitters,bytes,1,stream). Read actual cdecl int(void*,unsigned int size,unsigned int count,OP_STREAM*) caller/callee verified, preserves return word. Typed partial layouts, assert offsets. Fixture mismatch, zero and wrapped products, success/failure backend return, count visible before callback, callback mutations and complete pre-call world/header snapshots with only expected writes. Read stub performs no actual copy for huge or zero sizes; this is forwarding proof only. Trusted read_b3d_emitter_placements_chunk and dispatcher caller0041EA22.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
