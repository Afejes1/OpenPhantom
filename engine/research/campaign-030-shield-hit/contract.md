# shield_hit contract

- Address: `0x0043B8FF`; complete span299 bytes.
- ABI: `OP_HIT * __cdecl(int, OP_VEC3 *, float)`.
- Team manifest: `not_done`.

Check signed slot0..31 and active shield; scan unsigned hit count for first inactive hit. Copy direction, normalize, obtain reference frame, build identity/rotation and transform, then initialize progress/lifetime/mode. Invalid/full returns NULL. This candidate explicitly returns the hit pointer on success, adding3 bytes relative to the original terminal register behavior. Original source-level return intent remains unresolved; callers inspected discard the return. Retain defined C and park as research, with no warning waiver or byte exclusion.

Public inputs are authored C and fixtures, semantic contracts, addresses and digest evidence. Original bytes, resources, compiler binaries and raw analysis remain private. Exact requires zero full-span differences after every validated address fixup, native/Docker code and relocation equality, and passing isolated authored fixtures. No padding, warning waivers or byte exclusions. Shared integration and linked placement remain separate gates.
