# font_query

Null selection returns0; otherwise forwards font+8 to00479ba0 and preserves all return bits. Full span includes13 trailing alignment bytes; body-only identity cannot count exact.

Team manifest: not_done, font3d module. Ghidra TPM/WMAIN static body, callers, callee contracts and complete extent reviewed independently. Pinned VC5 /Od /MT; full span including alignment and every external operand required, zero differences. No original executable execution. Authored fixtures use all16 records, complete pool snapshots and a trailing guard. Fifteen minutes/ten attempts per function, stop after five non-improving attempts. Floats copied without arithmetic include raw bit patterns; z-range arithmetic uses exact finite dyadic cases.
