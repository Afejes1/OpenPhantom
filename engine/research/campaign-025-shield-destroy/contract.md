# Static contract

Two callers. Signed bounds and active word gate eight free-helper calls in order1C/40/20/24/28/2C/30/34; each pointer clears afterward, with count18/count3C clearing after the first two. Release sprite by field address, clear attached/active, decrement live subsystem count, return1. Preserve unmentioned fields and callback effects.

Full 333-byte span at `0x0043B09C`, no trailing gap. Team not_done and no local duplicate; full static instructions, call/data operands and caller references reviewed. Original game is never executed.
