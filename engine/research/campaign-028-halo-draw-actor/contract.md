# halo_draw_actor contract

NULL actor and flags lacking0x10 return. Scan32 halo records independent of aggregate count; call each current matching owner with its index. The initial flag gate is not rechecked after callbacks.

Team manifest reports not_done and prior-campaign inventory found no attempt. Full extent and all operand bindings are required. No original game execution, original resource strings, byte exclusions or warning waivers.
