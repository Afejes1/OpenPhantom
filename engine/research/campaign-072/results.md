# Campaign 072

Three complete function matches: directory offset 171 bytes, copy Nth directory 100, and copy filename 99. Total 370 bytes and six verified call operands, with zero resolved differences. All final native/Docker pairs agree and the retained matrices pass 6,111,472 assertions.

Five implementation compiles were used. The copy functions matched on their first attempt. Directory offset first encountered C4127 for a constant while condition, then C4706 for an implicit decrement condition; equivalent for (;;) and explicit --index != 0 forms passed on attempt three with an exact match. Both original diagnostics remain in private receipts identified by worker-log.json. No warnings were waived.

The fixtures enumerate directory components independently, including a final filename which must fail the following-separator requirement. They cover invalid indexes, repeated separators, leading drive-like pairs, exact alias for copies, guards, alignments and bounded post-NUL bytes. The real helper chain is used, with separator still unaccepted test support. Shared checkpoint pending in [Acceptance 052](../acceptance-052/result.json).
