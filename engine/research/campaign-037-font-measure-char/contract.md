# font_measure_char

Null selected font returns0; scale arguments captured before scale callback; char-size uses later live font pointer. Backend consumes only low character byte, not unspecified upper argument bits. Failure leaves caller outputs untouched; success optionally copies both, height last when aliased. Original char backend currently always returns1; authored failure branch evidence is bounded wrapper behavior.

Team not_done; static TPM/WMAIN body, callers/callees, full extents and operand inventory independently reviewed. Pinned VC5/Od/MT; zero differences across complete spans/all operands; no exclusions. Native compilation only; authored fixtures execute isolated Docker. Fifteen-minute/ten-attempt cap, stop after five non-improving attempts. Original executable never executed.
