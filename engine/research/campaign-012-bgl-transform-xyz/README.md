# Transform components by current transform

transform components by current transform.

**Research candidate: non-relocation bytes differ; target 97 bytes, candidate 97 bytes.** Final independent native/Docker verification passes
37 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed complete three-pointer ABI, required X/Y and optional Z, positive-zero fallback and original X/Y/Z store order. Independent six-row full-storage oracle covers null Z, all alias, X/Y alias, X/Z alias and Y/Z alias, preserved neighbor and callback mutations. Natural conditional candidate remains research with19 differences over full97 bytes; shorter second variant rejected.

External transform math is stubbed; reviewed semantic candidate is not an exact byte match and remains parked without resetting attempts.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-012/review.json).
