# Play sound by name

play sound by name.

**Research candidate: non-relocation bytes differ; target 275 bytes, candidate 275 bytes.** Final independent native/Docker verification passes
58 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed all64 descriptor bytes, bounded name copy, field override and exact postcallback reset constants, original flags controlling reset, category default and retained return. Independent fixture uses explicit signed-zero/quiet-NaN bit patterns, full descriptor oracle, template/name mutation and category-at-callback assertions; all five globals are checked in both reset branches.

Complete 275-byte comparison with all17 operands verified retains13 differing bytes (4.727272727272727 percent); near-match scheduling requires this passing reviewed fixture and is not exact acceptance. Original active-path overlong names remain invalid and are not exercised. Standard strcpy intrinsic is explicit; original template data is not committed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing; no native-listing fallback was used.
The listing lacks terminators; complete COFF inventory and native/Docker
code/fixup equality were retained and checked.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-010/review.json).

Scheduling category: reviewed near match (at most 5% differing bytes).
This is not an exact match or accepted-history promotion.
