# Prepared target: sound_play_by_name

Team not done, bp/bapsound.c. Complete 0x004171A1..0x004172B4, 275 bytes and17 bindings. int op_sound_play_by_name(int id,const char *name,int *handle,float *position,unsigned int flags). Full named/voice wrapper callers push five arguments and remove20 bytes; downstream descriptor routine takes descriptor/handle/position and caller removes12.

Descriptor64 layout: char name[24] at0; unsigned flags at0x18; float field0 at0x1C, field1 at0x20, field2 at0x24, field3 at0x28, field4 at0x2C; int field5 at0x30; opaque[8] from0x34; int id at0x3C. extern descriptor op_sound_default_descriptor; extern float op_sound_field0..field4; extern int op_sound_field5, op_sound_category, op_sound_initialized. cdecl int op_sound_start_descriptor(descriptor *value,int *handle,float *position). Existing get/set-field reconstruction confirms field0..4 floats and field5 signed int. Standard float copies under VC5 preserve the observed word moves; do not insert arithmetic conversions.

If initialized==0 return-1 without reading other args. Otherwise local descriptor=default_descriptor (all64bytes). strcpy(local.name,name), then local.flags=flags and local.id=id. Observed inline copy may use #pragma intrinsic(strcpy); no manual string or instruction injection. If flags&0x800, copy CURRENT globals fields0..5 into local fields. If CURRENT category<0 setcategory5. Call start_descriptor(&local,handle,position) and retain its integer return. If ORIGINAL flags&0x800, reset globals AFTER callback: field0=2.0f, field1=0.0f, field2=14.0f, field3=3.0f, field4=14.0f, field5=90. Return saved callback value. Without flag800, preserve callback mutations to these globals. Category not restored and init not rechecked. Local descriptor/template contents are not exported from game; fixture template is independently authored synthetic data.

Fixture: init0 with null inputs guarded, init noncanonical nonzero, empty/ordinary/23-character valid names, both800 branches with unrelated bits, categorynegative/0/positive, arbitrary id and callback returns includingnegative. Assert full64-byte local value at callback, template/name independence (including template mutation after copy), untouched name tail, field float bits (signed zero and quiet NaN payload included for MOV copies), field5 signed values, pointer forwarding and ordering. Assert actual global reset bit constants and retained return after callback modifies globals/init/category/local flags. Original flags determine reset, not mutated local flags. Avoid overflowing24-byte name field or retaining/dereferencing stack pointer after call. All fixtures owned, guarded and independent.

The coordinator verified every instruction against the owned reference PE and every absolute operand against its relocation inventory. All target operands and complete extents are immutable. Use pinned VC5 C /Od /MT and the existing campaign-001 verifier. All callbacks below are cdecl unless explicitly stdcall. Do not change ABI, targets, flags, tools or accepted history. No raw bytes, artificial frames, warning waivers or byte exclusions.

Worker owns candidate.c, api.h, behavior.c, README.md and worker-log.json only. Work sequentially; each case stops at 15 minutes, ten candidate compiles or five consecutive nonimprovements. Record actual clock observations, each hypothesis/hash/report, failures, and explicit timestamp bases. Native compile only; coordinator executes authored fixtures in locked Docker after review. Guard fixture array/pointer accesses and use independent expected states, not copies of the candidate algorithm. Worker stop 06:49:21 UTC; publication deadline 06:59:21 UTC.
