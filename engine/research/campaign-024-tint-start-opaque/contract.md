# Static contract

Six-argument cdecl wrapper forwards mode, duration bits, hold and three low-byte colors, adding alpha255. Seven callers. Only low-byte color values are meaningful in their four-byte stack slots.

Complete 42-byte span at `0x004393D0`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
