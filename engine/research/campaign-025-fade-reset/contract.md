# Static contract

Zero direct callers; retain generated analysis name and unresolved lifecycle provenance. Calls reconstructed tint stop then clears letterbox target/previous/step, preserving visible/mask and unrelated state. The fixture links the actual Campaign024 tint-stop C body.

Full 40-byte span at `0x00439720`, no trailing gap. Team not_done and no local duplicate; full static instructions, call/data operands and caller references reviewed. Original game is never executed.
