# console_box_clear

Full original0x0048C785 through0x0048C7C8, 67bytes, no tail; parent disassembly and XREF review verifies adjacent boundaries. Allfour latest-manifest unfinished and no earlier target/cap. Preparation began19:55UTC. Natural VC5 strlen/memset intrinsics match observed REP sequences; no fabricated tables or code bytes.

Put helpers compare only low16attribute bits and call actual accepted setAttr only when needed. Character API reads exactlyone stackargument byte; string API measures the live string after attribute callback mutation. Callbacks observe stage, lowbits, live handle, owned input bytes, zeroreserved and exact4-byte output storage, then perform known mutations. API results/outputcounts are ignored as observed.

Box layout refines the known0x90-byte allocation: width14,buffer78,inner_size80,total_size84,column88,row8c. Clear fills the captured bytecount then resets column/row; fixture covers0..48bytes, two objects, four alignments/patterns and external/object-representation aliases beforebufferfield. Row clear covers widths2..33, rows-3..3 with owned centered buffers; no negative length or out-of-bounds pointer domain is claimed. Full object/buffer snapshots preserve all other bytes.

No original execution, shortenedspan, byteexclusion or warningwaiver.
