# Sound record lookup wrapper

The candidate forwards the live registry and name, and returns the lookup pointer unchanged.
It matches all 24 bytes with both bindings verified. Fixture covers null/non-null identities and callback mutation.
Worker report: `engine/build/campaign-001-sound_find_record-47f1ab58ae96/research-result.json`.

Final parent review and fresh native/Docker verification confirm the complete 24-byte relocation-adjusted match with 15 focused checks passing.
See [result.json](result.json) for source, toolchain, object, fixture and report hashes.
The verified native-listing fallback was used after code/fixup equality; no bytes
were excluded. Final executable layout and accepted-history promotion remain pending.
