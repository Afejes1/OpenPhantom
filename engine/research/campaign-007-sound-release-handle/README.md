# Release sound channel handle

Release the selected 2D or 3D handle and clear that field after the callback.

**Complete 92-byte relocation-adjusted match.** Final independent native/Docker verification passes
25 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed indexed 128-byte records, type-selected stdcall IAT callbacks, base-plus-four/eight addends, selected handle clear after callback and retained unselected/type changes. Fixture pointers and neighbor snapshots stay within owned12-slot storage.

Only valid slot indices are covered; real Miles handle ownership and release behavior remain external. Callback stubs check wrapper ordering without invoking device code.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
