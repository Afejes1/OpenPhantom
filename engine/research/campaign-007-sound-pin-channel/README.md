# Pin sound channel position

Set pin flag and copy XYZ words into an active channel.

**Research candidate: 13 differing bytes in the 70-byte complete span (18.571429%).** Final independent native/Docker verification passes
9 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed active guard, 128-byte channel stride and field offsets, preserved flags, aggregate coordinate bit copies and self alias. Original and candidate have the same complete70-byte extent and verified base binding; thirteen bytes differ in copy register setup. Defined fixture uses only valid indices and owned source objects.

Research candidate: thirteen of70 bytes differ (18.5714285714 percent), above the near-match threshold. Partially overlapping distinct typed vector objects, invalid indices and concurrent mutation are outside the fixture.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
