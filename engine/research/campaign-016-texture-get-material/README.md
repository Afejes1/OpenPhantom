# Get sprite material

return the material field of a nonnull sprite resource.

**Complete 33-byte raw-code-match.** Final independent native/Docker verification passes
7 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed original argument null guard, captured local sprite and exact material field0x84 with trusted existing callers and type. Fixture compares the full guarded sprite and checks null, owned independent pointer and interior alias values.

Full33-byte function is raw-code exact with no address operands. Trusted existing Ghidra name get_sprite_material retained; shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-016/review.json).
