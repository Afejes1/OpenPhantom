# Static contract

cdecl int(int slot,unsigned char r,g,b,a); full94 bytes. Signed0..31 bounds and active owner required. Stores only low argument bytes at38..3B, returns1; invalid/inactive returns0. Table006C9A30 operand30, strideB4. Six callers corroborate five pushedargs. Shared fixture exercises every slot, invalidbounds, inactive, eachchannel extrema and complete other-record preservation.
