# Prepared extended load material

cdecl int op_extended_load_material(const char *name,void **out). *out=op_load_mat_resource(name,NULL,NULL); if null, *out=op_load_mat_resource(op_default_material_name,NULL,NULL); if still null return2; fallback success return1; primary success return0. Loader0047B4ED has THREE cdecl stack args (const char*,void*,void*), verified pushes/cleanup12 and callee stack reads8/c/10; ignore stale decompiler implicit-this signature. External const char op_default_material_name[] is common literal dflt.mat verified at004AC490; no game asset embedded. No direct XREF found, so purpose rests on full body and callee contract. Fixture primary/fallback success and total failure, exact ordered argument triples, output pointer observable between callbacks, callback mutation overwritten by returned pointer, whole output-neighbor guards; no real loading.

Full original body and interval verified through the next function entry; no alignment or table tail. Every listed operand independently validated against owned original PE and complete HIGHLOW relocation coverage before implementation. Team manifest: not_done, bp/extended.c. Use C89-era VC5 C, /Od /MT and existing runner. No masks, assembly, byte insertion, manufactured frames, warning waivers or target edits. Per function15minutes/10compiles, stop after five no-improvements or campaign worker deadline. Freeze near results rather than polish.
