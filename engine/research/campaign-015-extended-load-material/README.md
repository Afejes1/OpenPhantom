# Load material with default

load a material by name and fall back to the default material.

**Complete 80-byte relocation-adjusted-match.** Final independent native/Docker verification passes
32 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed both output assignments and all three returns against verified three-stack-argument loader ABI. Fixture tests ordered argument triples, initial/null output visibility between callbacks, callback writes overwritten by returns, and guarded final output. Parent uses the verified fallback name and owned sentinels. VC5 rejected memset of the const-char-pointer array; explicit typed initialization fixes the fixture without warning waivers.

Full80-byte function is relocation-adjusted exact with all3 bindings verified. One earlier fixture compile failed before execution on VC5 pointer qualifiers and is preserved. Resource loader is an authored callback; shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
