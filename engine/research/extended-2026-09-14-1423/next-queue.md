# Next preparation queue

Continue the bounded, related-function strategy and preserve all previous caps. The final batch in this window requires corrected shared acceptance before new implementations.

Two adjacent unregistered functions need layout preparation rather than immediate source experiments:

| Address | Contributor name | Body through RET | Complete interval | Concrete blocker |
| --- | --- | ---: | ---: | --- |
| 0046F639 | module_getName | 61 | 71 | Ten trailing bytes; Ghidra still displays a false thiscall receiver despite three stack arguments. |
| 0046EBA4 | list_freeAll | 59 | 60 | One trailing byte; establish original alignment placement before accepting any body candidate. |

Static inspection confirmed getName calls the accepted module lookup with one ID, copies count bytes from node+20 to the destination with the existing bounded-copy alias, returns zero on success and one on missing, and leaves missing output untouched. Its sole observed caller at00451BD2 passes count8 and cleans12 stack bytes. Signature setter attempts failed; the decompiler display remains uncorrected and must not define a candidate ABI. No implementation attempts were started for these addresses.

List-free-all resets the cursor, repeatedly obtains the next payload and removes it, stopping on null. Inspect mutation order against the accepted list helpers and preserve the full interval.

The already attempted path separator remains research at42compiled versus56target bytes; no percentage or accepted body-equality claim is made. Module send-shutdown retains its earlier capped history. Do not restart either history under a new campaign number.

For throughput, prepare another small cohort from the remaining utility/resource functions after resolving full extents and callback ABIs. Keep one implementation lane and independent read-only review, batch the original-toolchain shared checkpoints, and retain the strict zero-difference gate.
