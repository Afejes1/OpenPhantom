# Static contract

One dispatcher caller. Positive remaining gates subtraction. Terminal/nonpositive/unordered result copies target only to cache and optionally restores world; no direct renderer-start call on that branch. Positive continuation computes the observed grouped expression, captures binary32 result, then get/set-clear/start/get/set-enable. Continuation reloads stored remaining. Original x87 unordered behavior is part of the contract.

Full 162-byte span at `0x00438F81`, no trailing gap. Team not_done and no local duplicate; full static instructions, call/data operands and caller references reviewed. Original game is never executed.
