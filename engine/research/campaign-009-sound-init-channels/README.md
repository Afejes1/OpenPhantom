# Initialize sound channels

Release, clear and initialize all twelve128-byte sound channels.

**Complete 145-byte relocation-adjusted match.** Final independent native/Docker verification passes
315 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed full128-byte clearing between handle release and allocation, driver reload, null allocation forwarded, callback write timing and final flag/slot stores. Fixture independently compares entire zero and finalrecords for all12 slots with bounds guards. Explicit zero initializer fixes VC5 fixture C4132 without a waiver; source intrinsic matches observed inlineclear.

Explicit #pragma intrinsic(memset) is a source compiler directive; CLI flags/toolchain unchanged. Earlier parent fixture compile failed C4132 and is retained in review. Authored callbacks only. Full145-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
