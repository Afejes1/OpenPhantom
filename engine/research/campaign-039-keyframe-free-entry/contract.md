# keyframe_free_entry

Nonnull132-byte entry. Null node-array no-op. Capture array for44-byte traversal; unsigned loop rereads live count+38. Release nonnull payload+28 and clear live field after callback. Final release rereads live entry.nodes+3c then clears it after callback. Bounded authored count mutations, aliases and replacements observe callbacks without actual deallocation.

Team not_done. Full function spans and all operands statically reviewed. Strict pinned VC5/Od/MT matches require zero full-span differences, no exclusions. Ten-attempt/fifteen-minute cap per function, stop after five non-improving attempts. Native compilation only; authored fixtures execute isolated Docker. Original executable never run.
