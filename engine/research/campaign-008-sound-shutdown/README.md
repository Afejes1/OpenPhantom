# Shutdown sound system

Resume and stop channels, release resources and handles, destroy registry, then shut down Miles.

**Complete 131-byte relocation-adjusted match.** Final independent native/Docker verification passes
115 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed all cleanup calls and12-slot loop, live registry gate, clear-after-destroy and initialized-clear-last order. Independent bounded event oracle and callback mutation cases include initialized cleared during first callback without re-gating; final quick callback registry mutation survives.

Callee backends are authored stubs; exact function-boundary behavior and bytes, not real Miles lifecycle integration.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
