# Prepared target: sound_shutdown

Team-not-done bp/bapsound.c. Complete interval0x00415AB5..0x00415B38: 131 instruction-body bytes and 0 switch-table bytes; full131 mandatory, no alignment exclusion. Full body/instructions, boundary and caller/callee/global context reviewed. Every PE relocation, table label and direct call independently verified. Pinned VC5 C /Od /MT; immutable target.

int op_sound_shutdown(void); extern int op_sound_initialized; extern void *op_sound_registry; void op_sound_resume_all(void); void op_sound_stop_all(void); void op_sound_free_extra_refs(void); void op_sound_release_handle(int index); void op_sound_clear_backend(void); void op_sound_destroy_registry(void *registry); typedef void (__stdcall *op_sound_shutdown_fn)(void); extern op_sound_shutdown_fn op_sound_quick_shutdown;
Initialized==0 returns0 with no callbacks. Otherwise resume, stop-all, free-extra-refs; loop signed indices0..11 releasing each handle; clear-backend; if CURRENT registry !=null call destroy-registry with current pointer then write registry=null; call actual IAT8C1790 AIL_quick_shutdown@0; initialized=0 LAST; return0. No re-gating, ignored callback outcomes and live global reloads. Prior release/resume/stop source plus full cleanup and registry-destroy callee reviewed; dispatcher004158F0 event2 caller. No stack arguments are consumed; old propagated fastcall inference is not a source argument. Fixture initialzero/noop and arbitrary nonzero init, exact callback order and12 slots, null/non-null registry, callbacks changing registry before its gate and destroy changing it before finalclear, quick shutdown sees init before finalclear and can mutate globals; final initialized clear overrides it while quick-shutdown registry mutation is retained. Use bounded callback captures and owned fake handles only.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Use readable multiline fixtures, actual guards before array indexing and independent numeric expectations. Same15min/10candidate compile/five nonimprovements cap; freeze/handoff promptly. Native compile only; authored fixtures run only in locked Docker. No target/shared-tool/Ghidra/Git changes, fabricated tables/frames, raw instructions, warnings or byte exclusions. Worker stop05:43:20 UTC; publication05:53:20 UTC. Parent independently reviews and verifies final pairs.
