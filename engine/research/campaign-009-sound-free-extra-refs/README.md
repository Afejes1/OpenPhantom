# Free extra sound references

Release each extra sound resource and remove/free linked reference nodes.

**Complete 171-byte relocation-adjusted match.** Final independent native/Docker verification passes
75 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed live resource/registry/counter reloads, clear-before-removal and nextcapture-beforefree with finalglobalheadclear. Corrected fixture covers empty/one/three nodes and callback-creatednext; oldnode is poisoned after ownedpointer guard to detect postfree reads. Source retains observed cursor/alias cleanup and localnull store.

Owned acyclic lists and nonoverflowing counters only. Authored resource/hash/free callbacks replace real allocation and resource systems. All171bytes and11operands match.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
