# Static contract

Snapshot the low fog flag from a nonnull active world and store exact minus-one remaining-time bits. One dispatcher caller.

Complete 35-byte span at `0x00438F14`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
