# Acceptance 043 results

Fourteen length and audio functions bring the accepted baseline from 238 to 252. They match 1062 complete original bytes after 24 verified address operands, with zero differences and no exclusions or new warning waivers. Clean source 282dd1acaf65e781bf245e6422eb7f11fcb779ef builds as 20260914-060456-b793d40f. All 252 comparisons and shared authored tests pass. [Event 66](../../evidence/runs/000066-ef96a162fba0f9fd.json) records the source snapshot; [same-source supplemental CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34831369244) succeeds.

The shared fixture retains 402,348 focused checks and adds 22956 connected checks: 12676 estimator and 10280 audio checks. Estimator calls verify independent ordering relationships plus complete inputs and FPU control preservation. Audio sequences call the actual initialization, configuration, listener, stop, release and uninitialization wrappers without reseeding owned state between steps. Stop changes live vtables and clears the gate; later releases must skip until explicitly reenabled, then use the swapped tables.

Independent review approved all14 include-only source substitutions, retained cases, ABI and connected state transitions. Parent verified exact preservation of all238 previous target and registry entries; an optional reviewer prefix helper used an incorrect JSON shape and was not relied upon. The initial supplemental build rejected local shadowing in fixture helpers. Unique fixture local names corrected all C4459 warnings; canonical source and expected results were unchanged. The original fixture passed before this correction and the failed CI remains in the superseded receipt. The separate position/velocity research functions remain unaccepted (45/51 bytes, no valid percentage). All14 additions are team-not_done. Only authored fixtures execute, with no real COM/audio service or original game. Existing modern CRT suppression is unchanged. Whole-executable identity remains pending.
