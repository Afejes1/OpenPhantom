# task_alloc_state

Full original interval0x00475A02..0x00475A65, 99bytes and no tail. Independent inventory and parent disassembly confirm ABI and all operands; previously preparation-only, no prior attempt cap.

Task run accepts a record pointer, returns zero fornull record/event, otherwise snapshots current and publishes its input, writes optional data, calls the live event callback, clears live countdown forbit1, masks only that bit from the full32-bit return, clears the live optional output and restores the saved current. Fixture checks null/present gates, two saved contexts, literal32-bit return tables, callbackretargeting, pre/post output aliases to ownedrecord.data, complete64-record/pending/sixglobal snapshots and output/token ownership. The event slot is now typed unsigned(void), size/offset unchanged. Null/foreign current aftercallback, arbitrary output destinations and recursive callbacks are outside this focused domain.

Allocation rounds uint size upward through increments to a multiple4, calls external op_allocate495290, zeros roundedbytes and stores caller output and data into the live postallocator current. The allocator is not yet an accepted reconstructed body, so an authored scoped observer supplies guarded96-byte storage and mutates current/pending/count. Tests cover0..65 and topthreeuintvalues wrappingto0, both currenttargets and three outputlocations including record.data. Pool+8 is alwaysvalid, everybyte outside requestedroundedspan preserved. No allocation-failure or huge-allocation claim.

No original execution, ignored operands, bytepadding/exclusions or warningwaivers. Shared allocator routing must be selected/restored without weakening existing fixtures.
