# Prepared world_free_scripts

cdecl void op_world_free_scripts(OP_WORLD *world). World SIGNED int script_count0x1F8 and OP_SCRIPT **scripts0x200. Loop index0 while index<LIVE world script_count, capture world scripts[index] in local OP_SCRIPT*, call cdecl void op_free_script_data(OP_SCRIPT*), increment. Original pointer local atEBP-4 and index atEBP-8. No array/count reset, no null guards. Callee00429DD7 onecdeclarg, checks script data pointer at4 then op_release; real script pointer must be nonnull. Fixtures use owned nonnull script records with ignored bytes and pointer arrays, counts0/negative/multiple, order, callbacks contract/expand count or replace array, capture current element before callback and retain callback effects; full snapshots. Caller destroy_b3d_world0041CD0A. Existing FUN_0041e373 purpose supported by cleanup call graph and callee; no original symbol rename required by worker.

Full interval through the next function entry, no tail or table; all relative operands and empty HIGHLOW coverage independently checked against owned PE. Team bp/bapworld.c not_done. No original bytes/decompiler output in public files. Read ../../docs/campaign-rules.md. Per function15minutes/10compiles/five no-improvement. Existing runner only; no target/matcher changes, masks, inline assembly, fabricated frames or warning waivers. Freeze exact/near candidates promptly.
