# Static contract

Two callers. Six full incoming stack words: integer duration, float target, three integer colors, integer restore. Reject nonpositive duration or negative/unordered target. Publish restore first. Restore0 runs six renderer calls, then enables fog in late-reloaded active world. All accepted paths publish target, signed-duration-to-binary32, and remaining copy. RGB inputs are explicitly masked full words, contrary to stale byte-parameter Ghidra metadata.

Full 193-byte span at `0x0043906E`, no trailing gap. Team not_done and no local duplicate; full static instructions, call/data operands and caller references reviewed. Original game is never executed.
