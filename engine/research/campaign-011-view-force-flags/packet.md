# Prepared target: view_force_flags

Team not done bp/bapview.c. Full0x00419400..0x00419420 is32bytes, body19 plus13 NOP alignment bytes. Full PE operands verified. Pinned VC5 C /O2 /MT; optimized instructions corroborate module configuration. Full target immutable; no trimming/padding code/fabricated functions.

void op_view_force_flags(unsigned int flags); extern unsigned int op_view_forced_flags. OR supplied flags into global. Sole caller0042FC90 passes4; renderer004199B0 combines global with surface packed flags. No gates/callbacks/return. Independent explicit initial/mask/expected table includes zero/all/highbit/alternating/idempotence.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Native compile only; parent reviews and executes authored fixture in locked Docker. No shared files/metadata/Git/flags/ABI changes; no raw bytes/inline assembly/artificial frames/waivers/exclusions. Record started_utc/finished_utc/attempts/compiler_invocations with all hashes/hypotheses/report paths/timing. Max15min/10compiles/five nonimprovements. Freeze early if compiler body matches but tail differs; parent classifies without full-match claim. Worker hard stop07:31:19UTC, publication07:41:19.
