# Service sound periodic

Refresh listener and placements when enabled, then poll12 channels unless paused.

**Complete 109-byte relocation-adjusted match.** Final independent native/Docker verification passes
144 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed exact-equality gates, mode write, listener then placements, live paused reload and twelve polls with no loop re-gating. Fixture independently covers noncanonical nonzero flags and callback changes before and during polling.

Authored callbacks verify the service contract; listener, placement scheduler and Miles behavior are not integrated by this focused case. Full109-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
