# Campaign069

Two exact functions: shutdown121 and generic dispatcher230bytes, seven verified operands and zero differences. Shutdown took four bounded implementation attempts; generic matched on its first. Their final native/Docker pairs agree and retained authored fixtures pass1,411,200assertions. Earlier shutdown121-byte/nine-difference research receipts remain in the history and are superseded by the frozen fourth attempt. The effect-free switches reproduce directly observed compiler selector stores; original textual source remains unknown. No padding, exclusions or warning waivers.

Both canonical bodies are include-only copies. Acceptance049 will exercise the actual generic dispatcher from the existing save-chain fixture, preserving its prior path/load checks through an owned callback. Shared checkpoint pending.
