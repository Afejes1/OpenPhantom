# Sound placement deactivation

The natural initialized/null guards and active-field store match all 34 target bytes with the global binding verified. The fixture checks the 60-byte stride, field offset, gates, and byte preservation. Worker report: `engine/build/campaign-001-sound_deactivate_place-8e4ee2b8e1fe/research-result.json`.

Final parent review and fresh native/Docker verification confirm the complete 34-byte relocation-adjusted match with 10 focused checks passing.
See [result.json](result.json) for source, toolchain, object, fixture and report hashes.
The verified native-listing fallback was used after code/fixup equality; no bytes
were excluded. Final executable layout and accepted-history promotion remain pending.
