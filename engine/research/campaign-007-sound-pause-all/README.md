# Pause sound channels

Set pause marker on flagged channels before issuing 2D or 3D stop callbacks.

**Complete 120-byte relocation-adjusted match.** Final independent native/Docker verification passes
29 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed12-slot iteration, active-mask gate, pause marker before callback, zero versus nonzero 3D choice, exact handles and later-slot callback mutations. Fixed test-only guard placement and added repeated-pause coverage before execution; candidate unchanged.

Only authored callbacks execute. Concurrency and actual Miles stop behavior remain outside the fixture.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
