# Prepared target: sound_module_init

Team not done, bp/bapsound.c; complete 0x004159F0..0x00415AB5, 197 bytes and 18 bindings. int op_sound_module_init(void).

Globals: int op_sound_initialized; void *op_sound_driver, *op_sound_registry; extern const char op_sound_volume_key[], op_sound_disabled_key[] (address-bound names; fixture contents independently authored).
Callbacks: int (__stdcall *op_sound_quick_startup)(int,int,int,int,int); int (__stdcall *op_sound_preference)(int,int); void (__stdcall *op_sound_quick_handles)(void **driver,void **midi,void **dls). Original Miles IAT symbols have @20/@8/@12 stack cleanup respectively. cdecl void *op_sound_create_registry(int capacity); void op_sound_init3d(void), op_sound_init_channels(void), op_sound_set_master_volume(int value), op_sound_disable(void), op_sound_enable(void); int op_sound_read_option(const char *key,int fallback,int *out). Full registry/option callees and module caller confirm ABI. Option function returns full integer and writes the supplied output, not a float.

If initialized==1 return1 with no callbacks. Other values enter initialization. Call quick_startup(1,0,22050,16,2), preference(1,12), quick_handles(&driver,0,0). Ignore their return values. registry=create_registry(512); null registry returns1 without cleanup and without final initialized store. On success call init3d then init_channels, read_option(volume_key,127,&local_volume), set_master_volume(local_volume), read_option(disabled_key,0,&local_disabled). If disabled!=0 disable, else enable. Then initialized=1 and return0. No gate is rechecked after callbacks and no source input clamp is added.

Fixture: initialized 1 guard, initial 0/2/-1, allocator null and success, startup/preference failure returns ignored, output pointer identity, registry assignment overrides callback global mutation, option output controls master and enable/disable even if option return indicates failure, initialized stays at callback-mutated values until final store. Test callback ordering and live state after each stage. Avoid reading uninitialized output before option writes. For null registry case preserve partial side effects. Independently assert callback constants and separate local output addresses. All real backends replaced by authored stubs.

The coordinator verified every instruction against the owned reference PE and every absolute operand against its relocation inventory. All target operands and complete extents are immutable. Use pinned VC5 C /Od /MT and the existing campaign-001 verifier. All callbacks below are cdecl unless explicitly stdcall. Do not change ABI, targets, flags, tools or accepted history. No raw bytes, artificial frames, warning waivers or byte exclusions.

Worker owns candidate.c, api.h, behavior.c, README.md and worker-log.json only. Work sequentially; each case stops at 15 minutes, ten candidate compiles or five consecutive nonimprovements. Record actual clock observations, each hypothesis/hash/report, failures, and explicit timestamp bases. Native compile only; coordinator executes authored fixtures in locked Docker after review. Guard fixture array/pointer accesses and use independent expected states, not copies of the candidate algorithm. Worker stop 06:49:21 UTC; publication deadline 06:59:21 UTC.
