# ripple_material contract

Appearance domain0..7; no bound check invented. Cached material returns immediately. Acquire color then alpha, resolve alpha material then color under VC5 argument order, compose, publish cache, release local color then alpha. Return captured composed material even if release callbacks change cache. NULL backend results are authored stub-domain tests only.

Team manifest reports not_done and prior-campaign inventory found no attempt. Full extent and all operand bindings are required. No original game execution, original resource strings, byte exclusions or warning waivers.
