# Copy current transform

copy current transform.

**Complete 25-byte relocation-adjusted match.** Final independent native/Docker verification passes
9 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed complete copy from current representation. Parent fixture checks explicit bit pattern, two current sources, full source/output/neighbor preservation, pointer/depth and copy independence; removes memcpy self-overlap test.

Complete25-byte match with1 verified operand. Explicit standard memcpy intrinsic; valid disjoint matrix storage required by C copy contract. No original game data committed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
