# font_destroy

Invalid signed index no-op. Capture valid populated row; clear current only on equality. Release sprite, reload row font, clear its borrowed material at+10, free live font, clear row font, release backdrop. Valid index requires live font after first release. Callbacks can retarget current or captured row font/backdrop; later operations reload them. No defensive null-font behavior invented.

Team not_done; static TPM/WMAIN body, callers/callees, full extents and operand inventory independently reviewed. Pinned VC5/Od/MT; zero differences across complete spans/all operands; no exclusions. Native compilation only; authored fixtures execute isolated Docker. Fifteen-minute/ten-attempt cap, stop after five non-improving attempts. Original executable never executed.
