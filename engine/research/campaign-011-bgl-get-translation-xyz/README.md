# Copy current transform translation components

copy current transform translation components.

**Complete 65-byte relocation-adjusted match.** Final independent native/Docker verification passes
37 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed independently nullable ordered outputs and live matrix-word reads. Parent fixture uses explicit eight-row immutable oracle, full matrix/neighbor/state checks, null current with allnull, aliased outputs and live alias-chain expectations.

Complete65-byte match with3 verified operands. Full bit patterns preserved. Aliases use correctly typed owned unsigned words; allnull skips current access, active outputs require valid storage.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
