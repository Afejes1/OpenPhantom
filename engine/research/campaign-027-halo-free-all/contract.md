# halo_free_all contract

Walk exactly 32 halos, clear each non-NULL owner before release and decrement the live count after each callback. Finally set count to zero even for an empty pool. No direct callers: lifecycle provenance remains unresolved.

Complete span 110 bytes; every relocation operand is bound. Team manifest reports not_done; prior-campaign inventory found no attempt. No original execution, original resource names, byte exclusions, padding or warning waivers.
