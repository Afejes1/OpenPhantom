# Prepared target: sound_register_level

Team not done, bp/bapsound.c; complete 0x00415B38..0x00415C40, 264 bytes and 18 bindings. int op_sound_register_level(void).

Record/world layout is identical to remove-level: record60 flags0/name[52]+4/resource+0x38; world opaque[0xCB4], signed count+0xCB4, record pointer+0xCB8. Globals: int op_sound_initialized, op_sound_load_failures, op_sound_category, op_sound_ref_count, op_sound_level_registered; world *op_sound_world; void *op_sound_registry.
Callbacks: void op_sound_init_channels(void); void op_sound_bind_listener(float *eye,float *angles,float *pivot); int op_sound_insert_name(void *registry,const char *name,record *value); int op_sound_load_ref(record *value); int op_sound_remove_name(void *registry,const char *name). Complete callees and module caller reviewed. Insert's three arguments are registry/name/value; returns ignored. Load's integer success value controls removal.

If initialized==0 return1 untouched. Else load_failures=0; init_channels(); bind_listener(0,0,0); category=6. Then check CURRENT world. If nonnull capture its record pointer and loop signed index from0 while index<CURRENT world's signed count. Insert(current registry,record name,record); increment CURRENT ref_count; reload record flags and if bit0x2000 clear call load_ref(record). If load returns0, remove_name(CURRENT registry,name) then decrement CURRENT ref_count. Advance captured record pointer by60 regardless. Set level_registered=1 even if no world/no records; return0. No re-gating initialized and no post-callback null check inside loop.

Fixture: init guard, null world, signed zero/negative counts, several records with flags2000 and preload success/failure/noncanonical nonzero return; insert failure ignored; all callback ordering/count values, flags mutated by insert before preload gate, registry/count changes by callbacks, setup callback changes world before capture, loop world/count reload while record cursor remains captured, no category reset after later callback and registered set last. ABI offsets and unrelated record fields unchanged; bound all owned arrays and avoid counter overflow/null-world mutations inside traversal.

The coordinator verified every instruction against the owned reference PE and every absolute operand against its relocation inventory. All target operands and complete extents are immutable. Use pinned VC5 C /Od /MT and the existing campaign-001 verifier. All callbacks below are cdecl unless explicitly stdcall. Do not change ABI, targets, flags, tools or accepted history. No raw bytes, artificial frames, warning waivers or byte exclusions.

Worker owns candidate.c, api.h, behavior.c, README.md and worker-log.json only. Work sequentially; each case stops at 15 minutes, ten candidate compiles or five consecutive nonimprovements. Record actual clock observations, each hypothesis/hash/report, failures, and explicit timestamp bases. Native compile only; coordinator executes authored fixtures in locked Docker after review. Guard fixture array/pointer accesses and use independent expected states, not copies of the candidate algorithm. Worker stop 06:49:21 UTC; publication deadline 06:59:21 UTC.
