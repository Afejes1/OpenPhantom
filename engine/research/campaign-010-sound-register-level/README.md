# Register level sound references

register level sound references.

**Research candidate: non-relocation bytes differ; target 264 bytes, candidate 264 bytes.** Final independent native/Docker verification passes
39 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Parent corrected the inverted preload gate; final source loads only when bit 0x2000 is clear. Independent fixture uses owned-record indices and explicit callback order for skipped, failed and successful preload; tests count expansion via a different world while the original record cursor remains captured, signed empty counts and complete record bytes. Final source retains twelve local-stack-offset differences.

Measured complete 264-byte comparison with all 18 operands verified: 12 differing bytes (4.545454545454546 percent). Passing reviewed fixture permits near-match scheduling only; exact acceptance and linked layout remain pending. Invalid callback-induced null worlds are outside the fixture domain.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing; no native-listing fallback was used.
The listing lacks terminators; complete COFF inventory and native/Docker
code/fixup equality were retained and checked.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-010/review.json).

Scheduling category: reviewed near match (at most 5% differing bytes).
This is not an exact match or accepted-history promotion.
