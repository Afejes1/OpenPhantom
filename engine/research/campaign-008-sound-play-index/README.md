# Start b3d indexed sound

Bounds-check descriptor index and start indexed positional sound with default category8.

**Complete 94-byte relocation-adjusted match.** Final independent native/Docker verification passes
28 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed unsigned bound,64-byte descriptor stride, signed mode default and exact three-argument callback. Parent added explicit mode-at-callback expectations for negative,zero and positive cases, including INT_MIN, plus a fixture index guard before forming the expected pointer.

Non-null valid world is a precondition; out-of-range indices are tested only through the original unsigned rejection path. Callback return is intentionally discarded. Full94-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-008/review.json).
