# Reset first sound placement

Repeat first-placement reset according to signed world placement count without advancing the pointer.

**Complete 91-byte relocation-adjusted match.** Final independent native/Docker verification passes
10 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed null world, signed count loop, fixed first-record pointer, active-from-initial and positive-zero deadline store. Corrected fixture-only NaN-sensitive float equality to explicit zero-bit comparison and independent expected fields before execution. Candidate unchanged.

The original loop repeatedly touches only the first record; this behavior is preserved. Positive counts require a valid first record, and concurrency or very large counts are outside the fixture.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the immutable target and behavior contract](packet.md), and
[the independent campaign review](../campaign-007/review.json).
