# Build least component reference axis

build least component reference axis.

**Research candidate: non-relocation bytes differ; target 245 bytes, candidate 245 bytes.** Final independent native/Docker verification passes
211 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed repeated absolute expressions, minimum2/defaultindex0, finite strict ties and pinned x87 unordered comparison behavior. Original immediate decodes90.0f and full core callee ABIs use same local axis/matrix. Seven explicit finite/NaN tables check selection, callbackorder, complete local matrix, borrowed output, captured axis, alias and full owned state. Natural memset intrinsic is ordinary zero initialization, not inserted instructions.

This is an observed reference-axis construction, not a mathematical perpendicular guarantee. Full245-byte comparison differs61bytes (24.898%); retain research. QuietNaN observations use pinned default x87 only; real core math is stubbed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

Docker emitted its own listing without terminators. Complete COFF inventory
and native/Docker code/fixup agreement were retained; no fallback was used.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-013/review.json).
