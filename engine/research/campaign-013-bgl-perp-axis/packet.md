# Prepared target: bgl_perp_axis

Full0x0041FAA9..0x0041FB9E, body245, full245, 4 verified operands. Team not done bp/bgl.c. Pinned VC5 /Od /MT C.

void op_bgl_perp_axis(OP_VEC3 *output,const OP_VEC3 *input). Select smallest magnitude among3 input floats using minimum2.0f and default selected0; strict less comparison means earliest finite tie. Absolute expression is value<0.0f ? -value : value; original reevaluates that expression to update minimum, do not simplify it to cached first abs. Start local one-hot vector[3] all +0, selected element1.0. Call op_build_rotation(&localMatrix,localAxis,90.0f), then op_rotate_basis(output,localAxis,&localMatrix). IMPORTANT corrected original immediate0x42B40000 is90.0f, NOT180.0f; original input is NOT forwarded to these helpers, same local one-hot seed is. CorecalleeABIs fully verified in012: cdecl void op_build_rotation(OP_MATRIX*,const OP_VEC3*,float); void op_rotate_basis(OP_VEC3*out,const OP_VEC3*input,const OP_MATRIX*matrix). Real48-byte matrixfloatlayout; inputvec3can be represented float[3] with legal indexing (prefer typedef struct with float v[3] for this case). Natural local declaration order matrix then axis then min then i then selected is evidence-guided but no fakeframe. Fixture explicit finite table x/y/z minima, negatives/zeros/ties/all>=2 default0; check one-hot all3bits and exact90angle, same local pointer acrosscallbacks, full48-byte builder output consumed, output/input fullneighbor preservation and allowed alias output==input, callbacks mutate original input after selection. Use synthetic matrix bytes and explicit outputbits, not actual helper math. Pinned x87 unordered comparisons setCF and can update selected forquietNaN; if tested use independently derived expectations and document not portable C math semantics. Caller004207F7 normalizes direction then invokes this and crossproduct; sphere hit caller likewise normalized direction. Avoid asserting mathematically perpendicular for arbitrary input.

Worker candidate/api/behavior/README/log only; parent fixedtarget/sharedfiles/Git/metadata. Max15min/10compiles/five nonimprovements, preferably1-2variants then park. Hardworkerstop08:28UTC. Nativecompileonly; completefixturewithordinarycheckfunction/printedcounts thennotifyparent. No insertedpadding/rawbytes/inlineasm/fakeframes/waivers/exclusions. Fulltargetstaysimmutableevenifbodyonlymatches.
