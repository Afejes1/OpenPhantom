# colormap_set_current

Store current only if unequal; independently call hardware setter if live secondary pointerNULL, ignoring its result, return1. Callback observes prior current store and original argument; callback changes persist. NULL is a valid wrapper argument when authored backend does not dereference it.

Team not_done. Full original spans and every operand verified statically. Pinned VC5/Od/MT, no exclusions. Fifteen-minute/ten-attempt function cap, stop after five non-improving attempts. Native compile only; authored fixtures execute isolated Docker. Original executable never run.
