# Static contract

cdecl int(void), complete12 bytes. Calls0042178C to clear emitter state, ignores result, returns0. Call operand+4. Team reports done; overlap evidence only.

Team manifest: emitter_endLevel is done (source line 199).
