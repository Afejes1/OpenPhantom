# acos_degrees_copy3

Original cdecl float input and scalar ST0 float return, or void paired-output sincos. Exact observed radian scale0.01745329238474369f and degree scale57.296326654336454 are preserved. The latter is not mathematical180/pi. Scalar inverse inputs stay in[-1,1]. Scalar trig uses live x87 products; sincos first stores one binary32 radian local and writes sine before cosine. Exact output alias leaves cosine last. Tan calls the ordinary wrap function once with original input, stores its result and product without popping, and invokes original CItan on live ST0. The wrap fixture is an authored observer, not a reconstructed wrap implementation.

Literal numeric tables are authored independently using host mathematical functions and the observed constants before candidate execution. Stored binary32 outputs permit at most4 ULP; expected signed zeros are exact. Finite bounded vectors exclude poles, nonfinite inputs, domain errors and internal extended-precision identity claims. Fixtures save/restore masked PC53 round-nearest, preserve all owned guards, test every sincos NULL combination and exact alias, and test tan callback state mutation. Byte acceptance still requires zero differing bytes over the complete original span; numerical tolerance is only a bounded behavior oracle. Three acos wrappers have identical semantics at three distinct original addresses, not three different algorithms. No original execution or byte exclusions. Shared acceptance remains separate.
