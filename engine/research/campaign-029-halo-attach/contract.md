# halo_attach contract

- Address: `0x0043993E`; full span278, body278 bytes.
- ABI: `void __cdecl(actor*)`.
- Team manifest: `not_done`; no prior candidate at this address.

Requires valid actor definition and terminated model name. Capture model-name pointer at actor+20 then definition+8. Search a terminated external name/color table with case-insensitive comparison, taking the first live color or default0x00FFFFFF. Issue six ordered halo registrations for nodes9..14 with field8=0,fieldc=2, modes0/0/0/0/1/1 and three external resource-name symbols. Fixtures use authored names only; table/name capacities in api.h are fixture storage, not recovered production capacities.

Only authored C, tests, semantic layout notes, addresses and digest evidence are public. Original executable, resource strings, compiler binaries and raw analysis remain private. External callbacks are authored boundaries until canonical integration. Exact classification requires zero differences across the full span after all validated address fixups, native/Docker code and relocation equality, and the isolated fixture. No padding insertion, byte exclusions or warning waivers.
