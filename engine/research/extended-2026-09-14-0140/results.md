# Ten-hour continuation results

The authorized window is 01:40:41 to 11:40:41 UTC on September 14. Final verification reconstructs 114 new exact functions across 8,494 complete bytes and 302 verified address operands. Of those, 111 were marked not_done by the supplied team manifest and three were marked done. Another 17 candidates remain research; zero were accepted under a mismatch allowance.

| Measure | Start | Final |
| --- | ---: | ---: |
| Accepted functions | 150 | 267 |
| Chained verification events | 50 | 69 |
| New exact functions this window | 0 | 114 |
| Carried-in functions accepted | 0 | 3 |
| New research candidates | 0 | 17 |

The 117 promotions include three material functions already exact before this window, covering 239 additional bytes. Twenty-five focused cohorts fed nineteen batch acceptances. Canonical implementation now contains 266 C files and one C++ file, plus 42 headers. The final [per-function index](functions.json) identifies each source, team status, compilation evidence, complete size, operand count, mismatch and acceptance state. [Metrics](metrics.json) and the [independent audit](audit.json) retain the accounting.

All 267 current function comparisons and shared authored fixtures pass with the pinned original VC5 toolchain. The final source is `3c27ef174bfd4c89ef93a2c9782dbfa051f2c503`, build `20260914-071516-c96c42c5`; [Acceptance 046](../acceptance-046/results.md) records same-source supplemental CI and all 461 source inputs matching their committed Git blobs. There are 7,609,206 focused assertions across the new cases, including research fixtures. Those assertions cover documented synthetic domains; they are not a whole-game coverage percentage.

The final audit found local CRLF endings in two older fixture files while Git stored LF. Historical events 66 through 68 preserve those local hashes; normalization alone reproduces the committed bytes. This explained publication progress-check failures. The files were normalized, a modern unreachable-return warning was fixed, and the entire final build was repeated. Earlier failed compiler, preflight and CI attempts remain recorded. No canonical function, target span or byte-comparison rule was relaxed.

The matching results are complete function spans after verified address relocation. Original linked placement and byte-for-byte identity of the complete 1999-era executable remain unfinished. Original game code is never executed; runtime fixtures and external callbacks are authored. Source, Docker recipes, toolchain fingerprints, comparison tooling and receipts are committed. Private compiler files, the original executable, game content and local build artifacts remain outside Git.

Next work should continue with one implementation writer and one independent reviewer, combine compatible exact candidates into larger acceptance batches where useful, and use new type/local-layout evidence before reopening debug_reset, debug_clear_page4 or video_surface_type. Handle alignment tails in a shared linker/layout investigation. Defer repeated naming searches on the larger colormap, Euler and snap mismatches until new evidence changes the hypothesis. Keep the original campaign caps and full-span requirements.

The [next-work inventory](next-work.json) also identifies three previously unattempted team-not_done dependencies: stream reading at 0x00470786 (eight accepted callers), save-header writing at 0x00451BC2 (two), and packed clear colour at 0x00487B50 (two). Static triage is complete; ABI/fixture packets and compiler profiles still need review before compilation. The colour helper has five trailing NOP bytes and belongs with layout work. No compiler attempt was started for these follow-ups.

Shared weekly account usage moved from 62% to 79% used during the window (21% remained at 11:26 UTC). This is account-wide usage, not an exact task charge. No usage reset or purchase was made.
