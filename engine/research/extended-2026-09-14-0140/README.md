# Authorized ten-hour continuation

+Continue the user's ten-hour OpenPhantom reconstruction extension in this existing thread. The fixed start is 2026-09-14 01:40:41 UTC and the hard stop is 2026-09-14 11:40:41 UTC (7:40 a.m. Eastern on September14). Check the clock first. Stop new implementation at11:30:41 UTC, finish verification/publication, and pause this existing heartbeat by11:40:41 UTC. This explicit user extension supersedes the prior four-hour deadline; do not reset or extend this new deadline without another user request.

Read engine/research/extended-2026-09-14-0140/README.md and status.json in C:/Dev/Ghidra/release-verification/openphantom-vc5-baseline, then engine/docs/campaign-rules.md. Resume actual saved work. Baseline commit7f358b66accd787df6045076f1990d83ae79cc96 has150 accepted functions and50 immutable events. Three newly exact campaign040 material functions await acceptance028; the approved private integration generator is ready. The next planned campaign041 has three untouched colormap helpers; preserve its original preparation start01:36, implementation cutoff02:11 and deadline02:21. Earlier failed/parked campaigns retain their caps. Continue cohesive team-not_done cohorts after these checkpoints.

Use one parent implementation writer and one Sol medium independent read-only reviewer, reusing comparison_inventory. Only parent edits shared files, Git and Ghidra metadata. User explicitly authorized required Python/scripts, isolated authored fixture execution, agents/models, commits and pushes. No further permission is needed for this scope. List/select/confirm Ghidra TPM:/WMAIN.EXE at a fresh session start. Analyze original code statically; never run or patch the original executable.

Use the pinned original VC5 toolchain and unchanged strict matcher/runner. Exact acceptance requires complete original spans including alignment/tables, every address operand verified, zero differences, independent review and meaningful passing authored fixtures. Five-percent near-match is scheduling only. Keep original content/strings/assets, compiler binaries, raw decompiler output and private artifacts out of Git. All fixture storage and resource text must be authored. Whole-executable identity remains a separate unfinished objective.

Campaigns last at most45 minutes including final10 for review/publication; preparation and individual functions have15-minute caps, at most10 implementation compiler attempts, and stop after5 consecutive no-improvement attempts. Preserve failed attempts and do not reset parked caps by renaming tasks. Establish ABI/extent/operand/fixture contracts before source variants. Use member-aware token edits, guarded owned storage, pre-call expected snapshots, explicit callback mutations and real observer bounds guards. Support VC5 and modern /W4 /WX without warning waivers. Native operation compiles only; authored fixtures execute through the locked no-network Docker runner.

Use focused verification for research and full shared regression only at deliberate integration checkpoints or relevant changes. Before a clean build, publish or commit authorized research so untracked files do not make source_dirty; regenerate progress before source commits. Verify final native/Docker code/relocations, toolchain/source hashes, review and CI before recording immutable acceptance events. Separate this extension's new exact cases from carried-in promotions and cumulative totals. Track shared account usage at checkpoints; do not redeem resets or purchase usage.

Push only codex/vc5-matching-baseline in Afejes1/OpenPhantom. No upstream/develop merge, messages to others, unrelated repositories or memory edits. Leave the separately paused broad goal unchanged. Advance useful work rather than merely polling. At completion or the fixed stop, pause this existing heartbeat, publish verified outcomes and leave a concrete handoff.
