# Acceptance checkpoint 001 results

**The batch tests passed. Seven BGL functions are now promoted through the existing
accepted-build chain, increasing the registered total from14 to21.** The other59
overnight exact functions remain batch-tested research sources pending shared ABI
and fixture integration. No near or body-only result was promoted.

All66 overnight exact cases were freshly compiled natively and in locked Docker:
5,315 full-span bytes matched after every address binding was verified, native and
Docker code/relocations agreed, and all2,343 focused assertions passed. The new
batch orchestrator preserves132 fresh receipts and stops on incomplete comparison,
stale fingerprints, changed extents, lost assertions or failed behavior.
[The case-batch report](case-batch-result.json) records their hashes and locations.
Original bytes, toolchain files and private build artifacts remain outside Git.

The existing fourteen-function registry first received a fresh complete VC5 build,
Docker behavior pass and immutable record20. Then seven Campaign011 BGL state
functions were moved into fingerprinted engine/src paths with one canonical48-byte
raw-word matrix type. Their bodies, signatures, flags, extents and operand maps
were preserved; only their common-header include changed. The expanded21-function
VC5 build passed all old and new byte comparisons and its single linked fixture,
including45 new shared-state checks. The normal recorder created
[record21](../../evidence/runs/000021-863f58b48cff0d91.json); no alternate acceptance
rule or untracked research source was fed into that history.

The seven promotions are identity-template copy, matrix push/pop, matrix load/get,
and both translation getters:320 original bytes. The shared fixture exercises
actual calls together, full copies and neighboring storage, signed depth guards,
independent current/stack provenance, nullable outputs and live alias order. Its
nonuniform identity-template value is authored test data, not copied game data.
See [integration evidence](../../docs/bgl-state-acceptance.md).

The100 synthetic tooling tests,19 focused runner tests and6 batch-receipt boundary
tests passed locally. [Full integration CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34753426271)
passed synthetic tests, tracking/annotations and the modern x86 supplemental
fixture on source commit60283298178fd303fa18d3ce2b5386134cb53060. The earlier
[baseline full CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34752732103)
also passed. Modern smoke is supplemental; only VC5 comparisons establish original
code-generation evidence. No full-game or whole-executable identity is claimed.

## Why59 exact cases still await promotion

The research runner deliberately isolates each fixture. The registered build has
one shared fixture and fingerprints src/tests, not arbitrary research directories.
Pointing the main target directly at research sources or linking duplicate case
mains would not be valid acceptance. Inspection found differing signed/unsigned
sound index declarations, incompatible partial sound channel/descriptor/placement/
world types, and multiple views of the same matrix storage.104 referenced symbols
in the66-case set also lack definitions in the existing14 plus those66 candidates;
one is CRT-provided and the remainder are shared globals, callbacks and helpers
requiring canonical integration or explicit fixture definitions. Independent stubs
are not evidence that those contracts have been unified.

The BGL state slice establishes the correct promotion route without forcing those
unresolved contracts together. Next integrate coherent music and sound slices with
offset-checked shared types and genuine combined fixtures. The original fourteen
specifications remain unchanged. The upstream HEAD was still8d53d5595e28b0699d344e9e95fd774833bd9283;
team-reported completion stays separate until their new source/build evidence is
available. No develop merge or upstream submission occurred.

## Reproduce and continue

The66-case fresh report belongs to commit58a1ec4, before registry expansion. Check
out that frozen checkpoint to rerun [rebuild.py](rebuild.py) with your owned
reference and original toolchain. Current registry expansion changes target.json
and intentionally makes those old whole-input snapshots historical; the batch
runner fails instead of silently accepting altered inputs. The expanded registered
build is reproduced with the existing engine/verify.py build/test/record commands
at source commit60283298178fd303fa18d3ce2b5386134cb53060.

The acceptance work started10:44:28UTC. Exact completion time, source/receipt hashes,
counts and scope are in [result.json](result.json). The shared weekly usage meter
read37% near completion; this is rounded account-wide usage, not isolated cost.
The next user-authorized one-hour reconstruction window begins after this accepted
checkpoint. It preserves one Sol worker, independent review, per-function caps and
zero-byte exact criteria.
