# Acceptance004: shared vertex readers

Integrate the two exact Campaign019 vertex readers into the existing canonical
world/header views. Preserve the prior 26 registered specifications, the frozen
research receipts and the original local-vertex mismatch return of one.

Acceptance requires a clean committed source revision, the original VC5 RTM
registered build, the combined authored fixture in locked Docker, complete
zero-difference comparison of all registered functions, an immutable evidence
event and the explicit full CI checkpoint. Near-match materials and light readers
and the dialog research candidate remain outside this acceptance.

The shared fixture exercises sequential real reader calls, original field offsets
and record strides, signed sizes and loop bounds, callback changes and partial
failures. Independent review must finish before compilation and publication.
This checkpoint makes no whole-executable identity or real-backend execution claim.
