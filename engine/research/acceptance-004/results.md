# Acceptance004 results

Global and local vertex readers now belong to accepted history: two functions,
301 complete original bytes and two verified stream-call bindings. The clean
source revision is340adfe0f0436734fd47c250a3b5b7993489ab07. Every one of the
28 registered functions passed the fresh original VC5/Docker comparison, with
zero differing bytes. Immutable evidence record24 preserves the complete build,
source, toolchain, function and command provenance.

The combined world-reader fixture passed488 checks with0 failures, including the
previous189 checks and299 added integration checks. Existing BGL45 and colormap321
checks also passed. The100 tooling tests and19 focused runner tests passed once;
the explicit full GitHub regression passed. There were no failed build attempts.

Canonical type/header/field substitutions preserve both proven function bodies.
All26 earlier target and registry entries remain unchanged, and the previous eight
world-reader test bodies retain identical non-whitespace text. New tests cover
both actual readers in sequence on shared types, mismatch-return asymmetry,
empty/high-bit/wrapped inputs, signed size words, live callback changes, partial
failures, captured pointers and guarded original/alternate storage. Parent review
closed the first-reader snapshot and callback-argument gaps before compilation.

No bytes were excluded, no warning waiver was added and no original executable
or real backend was run. Final linked placement and whole-executable identity
remain separate work. The three near readers and dialog research case remain
unaccepted. Continue with the prepared world lifecycle/control packet.

[Full CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34761658187) and
[result receipt](result.json).
