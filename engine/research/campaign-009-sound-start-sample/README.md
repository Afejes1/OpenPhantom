# Start sound slot

Start bounded2D or3D sample and mark playing after backend callbacks.

**Complete 138-byte relocation-adjusted match.** Final independent native/Docker verification passes
26 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed unsigned bound, fixed2D/3D branch, loopcountzero beforestart, handle reload afterloopcallback and playingflag OR afterstart. Fixture tests noncanonical type, callbacktype/handle/flag mutations and unchanged neighbors with guardedindices.

Authored Miles callbacks; no live audio. Source start_sound_slot name retained. Full138-byte span.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
