# Clear world vertex visit range

clear world vertex visit range.

**Research candidate: non-relocation bytes differ; target 80 bytes, candidate 80 bytes.** Final independent native/Docker verification passes
23 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed unsigned16 endpoints,32-bit remaining count, null/reverse guards and highest inclusive index. Parent strengthens full-world and full-array preservation oracles on all guarded paths. Best natural variant restored after recorded worse second variant.

Complete80-byte comparison includes70-byte original body plus10 NOP alignment bytes. Thirty residual bytes differ; research only. Valid owned65536-vertex fixture exercises fullrange and65535 termination; no span trimming.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
