# Prepared target: view_clear_visits

Team not done bp/bapview.c. Full0x00419440..0x00419490 is80bytes, body70 plus10 NOP alignment bytes. Full PE operands verified. Pinned VC5 C /O2 /MT; optimized instructions corroborate module configuration. Full target immutable; no trimming/padding code/fabricated functions.

void op_view_clear_visits(OP_VIEW_WORLD *world,unsigned short first,unsigned short last). World opaque0..9B then OP_VIEW_VERTEX *vertices at9C. Vertex32bytes, visited byte0D and remaining opaque. Null world or null vertices returns; unsigned low16 first>last returns; otherwise clear visited inclusively first..last, stride32. Original computes first pointer before range test without dereferencing when reversed. Full range ending65535 must terminate without ushort wrap; natural pointer plus32-bit remaining count. No count bounds guard/globals/calls. Sole caller00407819 supplies ushort min/max after light surface batches. Fixture nulls/reversed/equal/zero/interior/highindex0x8000/last65535; use owned static65536 vertices, full independent expected array/world bytes; no invalid active pointers or wrapped ushort loops.

Worker owns candidate.c/api.h/behavior.c/README.md/worker-log.json only. Native compile only; parent reviews and executes authored fixture in locked Docker. No shared files/metadata/Git/flags/ABI changes; no raw bytes/inline assembly/artificial frames/waivers/exclusions. Record started_utc/finished_utc/attempts/compiler_invocations with all hashes/hypotheses/report paths/timing. Max15min/10compiles/five nonimprovements. Freeze early if compiler body matches but tail differs; parent classifies without full-match claim. Worker hard stop07:31:19UTC, publication07:41:19.
