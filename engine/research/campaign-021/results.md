# Campaign021 results

Five exact functions cover745 complete original bytes; two match raw bytes and three match after eight verified address operands are resolved. One248-byte material-run candidate remains nine stack-displacement bytes different (3.629%) and is unaccepted. All six focused fixtures passed354 checks.

| Function | Full bytes | Result | Fixture checks |
|---|---:|---|---:|
| world_read_animation_names | 253 | exact-function-match | 143 |
| world_mangle_names | 158 | exact-function-match | 15 |
| world_precompute_grid | 158 | exact-function-match | 99 |
| cell_precompute | 71 | exact-function-match | 55 |
| cell_lod_mask | 105 | exact-function-match | 22 |
| cell_material_runs | 248 | near-match | 20 |

The coordinator authored the source after delegated writes could not carry trusted user authorization; Sol independently reviewed every source contract and fixture.27 implementation compiles,12 final native/Docker candidate compiles and6 fixture compiles were used. There were no compile or fixture failures. Matching used the same original VC5 toolchain and unchanged runner/matcher. No whole-codebase regression was run for this focused packet.

The identifier experiment was capped. A subsequent, distinct natural-scope hypothesis made the LOD record pointer local to its loop and achieved105 raw matching bytes. The same scope hypothesis did not improve material runs; its best earlier candidate was restored and frozen. This suggests checking source scope before spending compiler trials on names.

Coverage includes animation pointer relocation and failure ordering, read-callback retargeting, temporary-only actor-name processing, live grid changes, five-call preprocessing, unsigned count/sentinel handling and the254-record run boundary. Public receipts contain authored source hashes, toolchain identity, reports and all attempts; original game bytes and assets stay private. Ghidra names/comments were saved. Two explicit signature updates were rejected by the bridge parser and the inferred signatures were retained.

Accepted history stays31 functions/25 immutable records. Next: a bounded shared name-reader/clock checkpoint. Full executable placement remains unverified.
