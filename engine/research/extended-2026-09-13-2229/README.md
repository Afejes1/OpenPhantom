# Authorized four-hour continuation

Continue the user's newly authorized four-hour OpenPhantom reconstruction window in this existing thread. The fixed start is 2026-09-13 22:29:21 UTC and the hard stop is 2026-09-14 02:29:21 UTC (10:29 p.m. Eastern on September13). Check the clock first and never extend the deadline. Stop new implementation at02:19:21 UTC; finish verification/publication and pause this existing heartbeat by02:29:21 UTC.

Read engine/research/extended-2026-09-13-2229/README.md and status.json in C:/Dev/Ghidra/release-verification/openphantom-vc5-baseline, then engine/docs/campaign-rules.md. Resume saved actual progress. All prior windows are completed history. Start at ec0b244bf7496813ce1f5fe0698a3b856714bdbd with105 accepted functions and38 immutable events. First integrate the already exact zap_build, zap_subdivide and zap_draw_opaque as acceptance017; then sprite_acquire/frame/release, then frame_prepare. Resolve canonical symbols/types and replace relevant fixture stand-ins with actual connected functions. Continue cohesive new team-not_done batches if time remains.

Use one parent implementation writer and one Sol medium independent read-only reviewer, reusing comparison_inventory. Only parent edits shared files, Git and Ghidra metadata. User explicitly authorized required Python/scripts, isolated authored fixture execution, agents/models, commits and pushes. No new permission is needed for this scoped work. Delegated work remains read-only because prior delegated writes did not inherit authorization reliably.

List/select/confirm Ghidra TPM:/WMAIN.EXE at session start. Analyze original code statically; never run or patch the original executable. Use the pinned original VC5 toolchain and unchanged strict matcher/runner. Exact acceptance requires full original spans including alignment/tables, all address operands verified, zero differences, independent review and meaningful passing authored fixtures. Five-percent near-match is scheduling only. Keep original content/strings/assets, compiler binaries, raw decompiler output and private artifacts out of Git.

Preserve existing caps and failed attempts. Campaigns last at most45 minutes including final10 for review/publication; preparation and individual functions have15-minute caps, at most10 implementation compiler attempts, and stop after5 consecutive no-improvement attempts. Do not reset parked caps by renaming tasks. Establish ABI/extent/operand/fixture contracts before source variants. Use token-aware edits, owned storage, pre-call expected snapshots and explicit callback mutations. Anticipate VC5 and modern /W4 /WX compatibility without warning waivers. Native operation compiles only; authored fixtures execute through the locked no-network Docker runner.

Use focused verification for research; full shared regression only at deliberate integration checkpoints or relevant tooling changes. Verify final native/Docker code and relocations, toolchain/source hashes and staged Git content. Preserve immutable accepted events. Record this window's new exact cases separately from promotions, near matches and whole-executable identity. Push only to codex/vc5-matching-baseline in Afejes1/OpenPhantom. No upstream/develop merge, messages to others, purchases, reset redemption, unrelated repositories or memory edits; leave the separately paused broad goal unchanged.

Advance useful work each continuation rather than merely polling. At completion or the fixed hard stop, pause this existing heartbeat, publish verified outcomes and leave a concrete handoff.

Prior handoff: [seven exact integration candidates](../extended-2026-09-13-1634/next-queue.md).
