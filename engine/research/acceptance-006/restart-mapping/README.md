# Restart mapping probe

Compile-only diagnostic:58 complete bytes match after both verified calls are resolved. No fixture was executed here. The empty behavior.c is an explicitly unused runner input; shared behavior lives in engine/tests/world_names_behavior.c. The original-toolchain clean shared acceptance record supersedes this pre-registration diagnostic.
