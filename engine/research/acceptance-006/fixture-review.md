# Shared names fixture review

Independent Sol review found no material defect. Prior31 target/registry entries remain an identical prefix; old fixture bodies are unchanged after removing the explicitly reviewed active-route branches. Canonical header/world offsets804/CD4 and previous fields are retained.

The focused ANAM and temporary-name fixtures were adapted to shared types with typed owned actor tables. The new chain verifies read results before recapturing expected state for restart. Timer callbacks compare the entire earlier world/header/table/actor state, overwrite clock fields and one unrelated byte, then final comparisons prove the original store order retains only intended callback effects. Ticks0,17,INTMIN andINTMAX plus a NULL restart are covered. The reconstructed helper is linked directly.

The fixture deliberately does not claim to observe temporary local normalization: exact code bytes establish it. No real game backend, original executable, byte exclusion or new warning waiver is involved. Clean build and immutable acceptance remain pending.
