# path_separator_offset

Latest contributor-not_done, no prior research target. Static parent/reviewer inspected complete instructions and next entries. Full56bytes/body42. Cdecl; drive returns char(AL), all offsets return int. Sources preserve exact slash/colon/dot rules and strlen intrinsic. Recovered drive ABI is corroborated by both callers consuming AL; full EAXzero is not claimed. Authored toupper observer validates sign-extended input and low-byte return without passing synthetic negative values to CRT.

Guarded96-byte character storage uses interior path pointers, so the basename predecessor remains inside its owned array. Exhaustive short strings over a,backslash,colon,dot,forwardslash cover separators/empty/trailing/prefix/dot combinations at four alignments. Full storage remains unchanged. Extension trailing-dot behavior intentionally resumes after the first NUL; a bounded post-NUL A.Z region tests this with expected offset length+3. Expected offsets are determined by forward delimiter scans and prefix rules, not copied from actual results. First-directory fixtures link separator body; extension fixtures link basename body. Separator keeps its full56-byte target including14alignment bytes and must stay research unless fully represented.
