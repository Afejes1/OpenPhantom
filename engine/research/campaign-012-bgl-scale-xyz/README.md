# Scale current transform components

scale current transform components.

**Complete 66-byte relocation-adjusted match.** Final independent native/Docker verification passes
15 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed ordinary standard memcpy intrinsic and correct diagonal indices0/4/8 in real48-byte matrix. Complete synthetic identity oracle, bit-preserving arguments, borrowed current, full callback matrix changes and identity preservation checked.

External multiplication is stubbed; original identity content is not committed.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-012/review.json).
