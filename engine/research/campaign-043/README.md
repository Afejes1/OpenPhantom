# Campaign043: canvas lifecycle and face allocation

Five untreated targets,398 full-span bytes. Canvas inner cleanup is deliberately retained as full9/body5 research due a4-byte tail. No padding. Other canvas functions and the face constructor preserve explicit cdecl signatures and captured allocation semantics. [Plan](plan.json) fixes the timebox.
