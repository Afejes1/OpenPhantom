# Free extended colormaps

free each currently tracked colormap and reset the live count.

**Complete 68-byte relocation-adjusted-match.** Final independent native/Docker verification passes
16 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed signed loop and live count/slot reloading against static calls and cleanup. Parent strengthened complete128-slot observations for callback contraction and expansion and replaced invented sentinel pointer values with owned addresses. Exact argument order, null forwarding, no slot clearing and final count reset pass.

Full68-byte function is relocation-adjusted exact with4 verified operands. Fixture frees are authored callbacks only; shared integration pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
