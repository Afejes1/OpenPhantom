# Module installation

Cdecl int(callback-word, name). Full299-byte body at0046ED64 through RET0046EE8E; no tail. Allocate36 or return0. ID initiallyself; clear bits1,3,2 separately, callback word, bounded name16, append to current tail, publish tail before filling an empty head. Callback(1,0,0) return0/2 sets livebit0;1 clears it; other results preserve. Return live post-callback ID, including on callback failure. No unlink/free. Authored owned allocator and callback validate insertion visibility, allocator endpoint changes, four endpoint combinations, padding/truncation, high allocator/status words, signed callback returns and post-callback ID/global changes. Full preplanned snapshots and guards. Switch result lowering follows actual scalar capture/comparison structure, with no fabricated dead locals.
