# Acceptance007 results

Six Campaign022 exact functions are now integrated and accepted: 319 complete bytes and seven verified operands. All40 registered functions pass the clean original VC5 build and complete-span byte comparison. The new shared fixture passed4112 checks with zero failures; previous fixtures also passed. Final source `5c3263060dc3991cc75d70cb0dfda47a8a095cc8` and immutable event28 identify all73 input hashes.

The100 engine tooling tests and19 focused-runner tests passed. [Full integration CI](https://github.com/Afejes1/OpenPhantom/actions/runs/34767687762) passed, including the modern supplemental fixture, which adds three secure-control API checks (4115 expected checks). The four shield functions operate on one canonical32-slot table; the fixture invokes all four in sequence and checks complete table state after each call. Model and font getters retain owned pointer and guarded-state coverage.

The first clean VC5 run passed and remains event27. Its modern CI failed on deprecated `_controlfp` use and a shadowing counter. The correction retains `_controlfp` for VC5, uses `_controlfp_s` for modern builds, and gives the counter a unique name. No warning waiver or production code change was introduced. A second clean build passed every check and is event28; the six promotions are counted once.

The initial Python discovery pattern found no tests; actual full discovery then passed100 and19 tests. The source preservation receipt confirms all prior34 target/registry entries and six production function bodies are preserved. Full original-executable identity and final function placement remain unverified.
