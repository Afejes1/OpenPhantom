# Reset extended flashes

forward the extended flash reset to the flash-table clearer.

**Complete 10-byte relocation-adjusted-match.** Final independent native/Docker verification passes
4 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed the complete10-byte forwarding wrapper and caller/callee role. Focused fixture checks exactly one call per invocation and retains callback state across repeated calls.

Full10-byte function is relocation-adjusted exact with its single call operand verified. Callee is an authored fixture stub; shared integration remains pending.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-015/review.json).
