# Campaign 014 results

**One exact function and one reviewed near match.** All229 focused assertions pass
in authored fixtures using the locked Docker runner and the original VC5 toolchain.

| Function | Full bytes | Result | Focused checks |
| --- | ---: | --- | ---: |
| [Sound lifecycle dispatcher](../campaign-014-sound-module-proc/README.md) |256| Exact after all24 address bindings; includes both switch tables |121|
| [Sound resource handler](../campaign-014-sound-resource-handler/README.md) |288|9 differing bytes (3.125%); all10 address bindings verified |108|

The dispatcher matched on its first worker compile. The resource handler improved
from59 to9 differences in three natural source experiments; the remaining local
frame displacements are parked, with no known behavioral defect. Near status is
research scheduling information, never acceptance or proof of correctness.

Independent review verified the actual callback contracts against stack setup and
callee accesses, including the file API's two-argument open call and the dispatcher's
single event argument. The resource fixture was strengthened for close-time name
and type mutations, null forwarding, unsigned size boundaries and retained callback
globals. No parent source rewrite was needed. Both final native/Docker pairs agree
on complete function code and relocations. Docker produced valid listings directly;
no listing fallback, byte exclusion, warning waiver or matcher change was used.

Four worker candidate compiles, four final candidate compiles and two fixture
builds/executions produced these results. Every source snapshot and receipt is
checked against staged Git blobs before publication. See [metrics](metrics.json),
[review](review.json) and the per-case experiment logs for reproducible provenance.

Ghidra function names and evidence comments were updated and saved. Exact callable
prototypes remain documented in the packets; the MCP signature parser rejected the
explicit-cdecl signature requests, so no successful database signature change is
claimed. The registration caller was re-decompiled after the metadata changes.

Accepted history remains21functions/21records. This checkpoint adds focused
research evidence, with shared ABI integration still pending. Full regression was
completed at the preceding [acceptance checkpoint](../acceptance-001/results.md),
not repeated for these function cases. The original game was never executed or
patched. All candidate sources here are C; this is not whole-executable identity.
