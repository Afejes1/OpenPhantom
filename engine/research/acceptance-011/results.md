# Acceptance011 results

Seven exact Campaign026 functions are promoted, raising registered acceptance from62 to69 functions and immutable evidence from31 to32 events. They cover455 complete bytes and42 verified operands. Overlay restore remains research at51/59 bytes.

The locked original VC5 build passes all69 comparisons and prior behavior suites. The new shared halo/overlay fixture passes11,035 checks; the shield lifecycle suite now passes17,353 checks with real halo cleanup linked into teardown. All100 tooling tests,19 focused-runner tests and the full supplemental CI pass.

Source commit: `3458cc1f00d8271ffeb8e4b66e2ffe25fb039844`. Build: `20260913-144530-8c52d312`. Event: `000032-38e01a80c42fdcdb`. All129 source snapshot inputs match the accepted commit and current files. Per-artifact hashes and CI links are in result.json.

The first fixture compile diagnosed a namespaced preprocessor directive; that fixture-only generation error was corrected, and its failed build is retained. A separate locked compile diagnostic verified the corrected fixture before the successful full rebuild. No function implementation changed to resolve the diagnostic.

Independent review verified shared layouts, scoped resource callbacks, full-block persistence, callback-visible state and the actual teardown chain. These are complete function matches after resolving documented operands; final executable placement is not yet proved. No original game execution, byte exclusions or warning waivers.
