# Prepared target: sound_add_extra_ref

Team not done, bp/bapsound.c; complete 0x0041705B..0x00417143, 232 bytes and 11 bindings. node *op_sound_add_extra_ref(const char *name).

Node64: unsigned flags0, char name[52] at4, void *resource at0x38, node *next at0x3C. Globals int op_sound_initialized, op_sound_ref_count, op_sound_extra_count; void *op_sound_registry; node *op_sound_extra_head. cdecl callbacks void *op_sound_allocate_node(int size), int op_sound_insert_name(void *registry,const char *name,node *value). Allocation is the engine allocator, not CRT malloc. Full caller forwards descriptor/name pointer and stores returned node; full allocator and insert callees confirm contracts.

If initialized==0 return null untouched. Otherwise allocate64, store local node. No allocation-failure guard exists: original immediately dereferences return. Set flags=0, resource=0, next=0; then ordinary strcpy(node->name,name). The observed inline string copy can be requested with #pragma intrinsic(strcpy), as recorded for campaign009; preserve standard C source semantics. Insert(current registry,node name,node), ignore return; increment CURRENT ref_count then CURRENT extra_count. If CURRENT head null, head=node and return node. Else traverse from CURRENT head with local cursor to last next==null; append node and return node. No recheck initialized or reset after callbacks. No duplicate check inside function.

Fixture: initialized0 accepts null name without access/allocation; valid nonnull allocations only for active path, explicitly document allocation failure as invalid/crashing original path, not a successful null return. Empty/ordinary/51-character names, untouched name tail bytes, zeroed fields before insert, null/one/multiple-node lists, returned node and append/head identity, insertion failure ignored, registry changed by allocation, counters/head/node fields mutated by insert. Assert live counts and head decision after callbacks; name copy independent from caller storage. Use owned bounded list nodes with cycle-free mutation and guard callback pointer checks. Never execute original allocator/game.

The coordinator verified every instruction against the owned reference PE and every absolute operand against its relocation inventory. All target operands and complete extents are immutable. Use pinned VC5 C /Od /MT and the existing campaign-001 verifier. All callbacks below are cdecl unless explicitly stdcall. Do not change ABI, targets, flags, tools or accepted history. No raw bytes, artificial frames, warning waivers or byte exclusions.

Worker owns candidate.c, api.h, behavior.c, README.md and worker-log.json only. Work sequentially; each case stops at 15 minutes, ten candidate compiles or five consecutive nonimprovements. Record actual clock observations, each hypothesis/hash/report, failures, and explicit timestamp bases. Native compile only; coordinator executes authored fixtures in locked Docker after review. Guard fixture array/pointer accesses and use independent expected states, not copies of the candidate algorithm. Worker stop 06:49:21 UTC; publication deadline 06:59:21 UTC.
