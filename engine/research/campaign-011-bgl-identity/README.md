# Load identity transform

load identity transform.

**Complete 27-byte relocation-adjusted match.** Final independent native/Docker verification passes
5 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed complete48-byte copy from address-bound external matrix and live destination pointer. Independent synthetic twelve-word fixture differs from identity values and checks full destination plus neighbors and pointer/depth.

Complete27-byte match with2 verified operands. External original identity data is not committed; authored fixture uses synthetic words. Valid destination storage required.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-011/review.json).
