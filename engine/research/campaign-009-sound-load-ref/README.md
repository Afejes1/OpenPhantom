# Ensure sound record sample loaded

Acquire missing BSND resource through a copied name and update failure counter.

**Complete 167-byte relocation-adjusted match.** Final independent native/Docker verification passes
52 focused assertions. Every address operand is verified; no
bytes, alignment or warnings are excluded. Accepted-history promotion and final
linked executable identity remain pending.

Reviewed null/alreadyloaded guards, intrinsic stacknamecopy, BSND acquisition ABI, returnedpointer store and live signedcounter transition. Parent added ABI assertions and proof that record name mutation cannot alter the independent copied name.

Explicit #pragma intrinsic(strcpy) reproduces observed inlinecopy. Fixtures use owned NUL-terminated names up to51chars and avoid signedcounter overflow; full167-byte span. No real resource acquisition.
Function-level evidence; final linked executable layout and accepted-history batch checkpoint remain pending.

The explicit verified native-listing fallback was used after complete code/fixup
agreement. The empty Docker listing and provenance remain recorded.
See [result](result.json), [all worker experiments](worker-log.json),
[the target and behavior contract](packet.md), and
[the independent campaign review](../campaign-009/review.json).
