# Campaign068 focused results

Three exact functions: open137, close86, install299 bytes. Fifteen verified operands, zero differences, no exclusions or warning waivers. Four implementation compiles (one for each candidate, plus preserving the existing copy-helper symbol in install). All three final native/Docker pairs agree and authored fixtures pass1,953,600 assertions. One failed installation-fixture compile exposed an implicit ternary conversion; an explicit character cast corrected it without changing the candidate or warning policy. Independent Sol review approved static contracts, snapshots and mutation ordering. The reviewed start193-byte carry-in joins these three at acceptance048. Shared acceptance remains pending.
