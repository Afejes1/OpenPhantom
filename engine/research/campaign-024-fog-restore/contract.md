# Static contract

Clear low fog flag, OR the saved word, then call world fog application with the final active-world load. Two callers. Real saved values are zero/one; raw noncanonical values demonstrate literal OR without claiming normal input.

Complete 74-byte span at `0x00438F37`, no trailing gap. Team not_done and no local duplicate. Ghidra full instructions, caller/callee references and all address operands reviewed. Fixtures use authored data only. Final linked placement remains a separate gate.
