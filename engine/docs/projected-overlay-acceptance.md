# Projected-overlay lifecycle acceptance

Five Campaign032 functions share a partial68-byte projected-overlay record view, with255 records, surface/address word at8, lifetime at52, group token at60 and unsigned sequence at64. Unresolved fields remain opaque. Full original spans include naturally generated alignment; all12 operands are verified.

The shared fixture retains all five focused contracts. The sort observer now receives the real reconstructed comparator. An additional connected fixture seeds255 uniquely ordered records, sorts through the original VC5 runtime qsort, verifies exact finite expiry boundaries, expires rows, removes an owner group including inactive rows, sorts the survivors and cleared rows, and verifies clear-all preserves dirty until the subsequent sort. Expected results are built from the seeded sequence/key order before calls, with every record compared.

No original executable is run. Accepted history requires a clean original-toolchain build, the full shared fixture, strict comparison and immutable record. Supplemental CI is separate from VC5 code-generation proof. See research/acceptance-014/result.json for checkpoint status.
