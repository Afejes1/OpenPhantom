# Frame preparation acceptance

Canonical `op_frame_prepare` at 0x0043f57a uses the existing world and camera globals, and calls the actual accepted `op_submit_gathered_surfaces` at 0x00406830. Its complete 70-byte span and all nine operands must match. No isolated camera or final-submit aliases are introduced. Static caller instructions confirm three independent world reloads, camera byte offset8, and three surface-preparation stack arguments. Callee analysis agrees on void return behavior; its current one-argument surface view does not erase the two additional caller arguments.

The authored connected fixture has guarded canonical world/camera storage and 240 cases across world rotations, NULL/non-NULL initial world, callback mutation, final counts0/1/7/UINT_MAX, and three signed backend results. Four ordered external preparation backends check live world/camera arguments and full expected state. Surface and graphics preparation change gathered count from zero to nonzero or nonzero to zero. Real final submission either omits drawing and preserves the last result, or passes the live gathered world/camera/count and stores the returned int despite backend mutation. Expected storage is captured before each call and updated only for specified callback effects. Existing gathered-submission and frame-state fixtures remain active.

NULL worlds here exercise pointer forwarding through authored backends; real world preparation dereferences the world, so this is not a claim that the complete original path accepts NULL. Cameras are always valid owned objects; no arbitrary pointer arithmetic or full renderer behavior is claimed. The four preparation callees and final draw backend remain external fixture boundaries. Original bytes/content stay private. No byte exclusions or warning waivers are permitted, and whole-executable identity remains pending.
