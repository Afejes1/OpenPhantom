# Material state acceptance

Three campaign040 exacts share the canonical material view. Its existing frame pointer remains at176 with its accepted type; cache invalidation casts that same pointer to the separately defined988-byte row view. Palette anchor48 and unsigned cache count172 split the existing opaque prefix without moving any field or changing the180-byte partial extent. All prior sprite/material/ripple/font fixtures remain part of this acceptance checkpoint.

Destructor tests now install callbacks through the actual canonical material hook setter. The shared release backend routes scoped material cases to owned observers. Palette callbacks observe the embedded address and their mutations survive except the documented post-call globals. Cache cases cover all bounded0..3 inner-count patterns for up to3 disjoint rows, preserving whole tables and materials. External contents/palette backends remain authored.

Color packing is not promoted: its170-byte raw-matching body still lacks the14-byte full-span tail. No changed target, synthetic padding or byte exclusions are used. Whole-executable identity remains pending.
