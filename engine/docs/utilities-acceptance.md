# Utility acceptance

Seven exact functions add657 complete bytes and six verified address operands: three raw-exact rectangle routines and four utility wrappers. The prior209 registry and target entries remain unchanged. Canonical function bodies change only their header include. Shared service recovery replaces one unknown word with the open callback at0x30; sizeof0x44 and close/read/write offsets remain asserted and unchanged.

All seven independently reviewed focused fixtures remain under separate local namespaces. Allocator routing uses the existing single authored wrapper. The file-existence fixture shares the existing service global and a16-byte authored mode-token array, initializes its own token, and checks the full shared token extent. Existing configuration fixtures retain their original behavior.

Connected tests call the actual formatting wrapper, duplication function, and lowercase function in sequence. Independent full-buffer expectations prove final format termination, allocator-time input mutation, copy-after-callback, allocation failure, and later lowercase writes. Owned input and copy buffers include guards; sizes and lengths remain bounded. External formatting/allocation/character conversion remain authored observers. No real CRT formatting, heap allocation, filesystem operation or original executable runs. Rectangle arithmetic retains original signed comparisons on explicitly wrapped32-bit endpoints, including unusual negative/zero extents; only exact self-alias and disjoint storage are claimed. Whole-executable identity remains pending.
