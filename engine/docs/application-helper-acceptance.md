# Application and generation helper acceptance

Seven exact functions from Campaign031/033 enter shared integration: application run, graphics close, diagnostic close, simulation halted setter, halo flicker, projection token and overlay save size. Complete spans total205 bytes and22 operands. Frozen focused evidence remains under the corresponding research cases.

Projection token now supplies the real ripple-spawn call. The connected fixture uses six unsigned counter seeds, including both sign boundaries and wrap to zero, at every free/full slot and enable/movement combination. Successful creation increments once; rejected creation preserves the counter. Whole pool, inputs and unrelated enable/clock state remain checked. The original counter source signedness remains unresolved; the unsigned implementation defines the observed32-bit wrap. MSVC's word conversion into the existing signed token field is checked.

Overlay size now returns28 in the real effects save chain. The earlier synthetic size callback's impossible state mutation is removed from the shared oracle; frozen standalone boundary evidence remains. Header and byte-write callbacks still check complete live state and real callback mutations. Header now observes the actual shield/overlay sizes before its own mutation.

The other five helpers retain their reviewed focused callback and boundary fixtures in the shared build. Original game, graphics APIs and file-close callbacks are not executed. Final clean VC5 comparison, shared behavior, immutable record and supplemental CI remain required before acceptance.
