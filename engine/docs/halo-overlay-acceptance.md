# Halo and overlay acceptance

Acceptance011 integrates seven exact Campaign026 functions (455 complete bytes). The 51/59-byte overlay restore remains research; its alignment tail is not excluded or synthesized. OP_ATTACHED_ACTOR now exposes flags at zero while preserving the slot at0x100 and total0x104 size. Halo entries have stride28 with owner at0 and sprite at20; all used layouts are asserted.

The shared fixture retains all 9,869 focused checks for the seven admitted candidates. Resource startup and shutdown share the same handles. Letterbox set, snapshot/write, staged read and reset run against the same globals. A staged read does not publish live letterbox words, even when its callback reports success. Callbacks cover full28-byte identity, the prefix12, ordered live resource stores, NULL/alias results and callback mutations. The standalone overlay global has no physically adjacent fixture guards; all its bytes and unrelated state are checked.

Object teardown now calls the actual reconstructed halo detach function. Its release callback changes the actor's shield slot before the actual stop helper reads it; the zap callback checks the resulting shield/actor state. The full prior shield cases remain, with additional owner-before-release and live count assertions. External resource, rendering and serializer boundaries remain authored fixtures. NULL actor inputs are outside the contract. Original game code is never executed.

Admission requires clean committed source, all registered VC5 comparisons, shared isolated fixtures, tooling tests and full supplemental CI. Shared header changes require full comparisons because VC5 code generation is sensitive to identifiers and declarations. No warning waivers or byte exclusions. Whole executable placement and resources remain pending.

The initial shared fixture compile exposed a namespacing-generator mistake in preprocessor directives. Only the authored fixture macro directives were corrected; the failed build receipt is retained. No candidate code or compiler warning policy changed.
