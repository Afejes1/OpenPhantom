# Debug state wrapper acceptance

Three exact functions add 203 complete bytes and 19 verified operands. All prior 264 targets and registry entries remain unchanged; canonical bodies change only the include. Clear-page4, reset and disable remain frozen research and are not linked or promoted.

Three retained focused fixtures call the canonical toggle-visible, next-page and toggle-size functions through scoped authored reset/resize observers. Connected sequences call all three canonical wrappers on the same owned state, continue through a second visibility toggle, and preserve callback mutation between operations without reseeding. Expectations are computed before each call and verified at callback boundaries and after return. Signed gate conditions, modulo increments, post-reset page clamping and the live post-resize selector store are covered.

Eight fixture-owned rows and guarded text buffers establish bounded test storage only; production extent is incomplete. Reset is an authored mutation observer, not the researched reset implementation. No release/allocator routing changes or fixture-only canonical source copies are needed for this three-function checkpoint. Original labels, game code and OS functions never execute. No byte exclusions or warning waivers are added. Whole-executable identity remains pending.
