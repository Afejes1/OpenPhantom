# Console text and skills IO acceptance

Six functions add368completebytes and12address operands, including two literal rawmatches. [Acceptance060](../research/acceptance-060/results.md) records successful shared checkpoint event83. All271,552focused checks are retained. Canonical setAttr and the complete accepted save helper chains remain active; only the external APIs use selected/restored observer routes.

Console box layout is refined without changing size or offsets; the previous destruction fixture still mutates the same byte7F, now namedunknown7c[3]. Text tests verify post-callback live string length and low16attribute comparison; buffer tests preserve full surrounding object bytes. Skills IO tests cover stream reloads, header/file/error side effects, exact216-byte state transport and observed return polarity. Shared tests restore their IO route, stream pointers and error context.

Effective difficulty is separately frozen research with13/156differences despite22,400passing checks. No byteexclusions, warningwaivers, manufacturedlocals or shortenedspans. Whole-executable identity remains unfinished.
