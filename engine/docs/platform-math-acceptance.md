# Platform and math acceptance

Thirteen exact functions add405 complete bytes and29 operands. All225 prior target and registry entries remain unchanged. Canonical source bodies change only their include. Six platform fixtures and seven math fixtures retain their reviewed cases under separate local namespaces.

Platform heap callbacks route to the currently selected focused fixture. Connected tests call actual alloc, lock, realloc, lock and free wrappers with owned/null results and unsigned size words while checking exact stage and complete backing preservation. Connected timers call actual ticks then seconds through one callback set; the second branch sees the flag left by the first callback, while independent expected state tracks live scales. No real heap or OS service runs.

The scalar arccosine implementation now occupies its canonical address binding. The previous vector-angle mutation and ABI probe is retained by a fixture-only textual compile of the unchanged vector-angle source under test-local names. It is not an accepted byte artifact. The separately linked canonical vector-angle object calls the actual scalar-angle implementation in new guarded finite in-domain sequences, including exact+1 bypass cases. Those connected stored-float results allow an absolute2e-5 tolerance to accommodate caller precision differences between original x87 and supplemental modern builds; zero bypass is exact. Focused scalar math retains its4-ULP nonzero/exact-signed-zero bounds. Byte comparison always requires zero differences.

The observed degree scale57.296326654336454 remains exact, and three arccosine wrappers are distinct original addresses with duplicate semantics. Sine is excluded from this batch because its complete extent remains unresolved; the existing halo callback tests remain intact. Optional independent Python table recomputation was blocked by automatic approval review; manual source/oracle review and receipt hashes completed. Whole-executable identity remains pending.

Supplemental modern cosine uses a separate literal table because float multiplication rounds its radian argument before double promotion. Original VC5 expectations remain unchanged and strict; both tables retain4-ULP nonzero and exact signed-zero checks. The first supplemental run exposed two near-zero cosine differences while the original fixture passed; this fixture distinction fixes the test without altering candidate bodies or byte requirements. The initial integration metadata incorrectly included the two unpromoted sine operands;29 is the actual promoted total.
