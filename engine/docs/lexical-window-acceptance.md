# Lexical and window-state acceptance

Nine exact functions add864 complete bytes and18 verified operands. Three parser/sort functions retain their complete source bodies and legitimate local names; six window-state helpers retain fixed32-bit handle storage and a neutral four-word rectangle. The prior216 target/registry entries and existing source semantics remain unchanged.

All nine reviewed focused fixtures are retained under separate local namespaces. Token copying routes through the existing single authored strncpy wrapper only while the tokenizer fixture is active. Other string callbacks remain unique authored observers. The original signed/unsigned clamp and pointer-return behavior are unchanged; zero-size lexical output is tested only with sufficient owned backing. Sort combines an independent final-order oracle with callback snapshots and a literal event schedule.

Connected window tests call all six actual setters/getters over216 initial states and six target patterns. They prove independent handle preservation, source-to-global rectangle transfer, mutation through the returned live pointer, and exact self-alias copy while retaining input guards and all documented state. They do not claim that independent globals are adjacent or that snapshots observe intermediate stores.

No original executable or OS operation runs. Original Win32 handle meaning comes from caller evidence; word transport does not validate arbitrary handles. Rectangle coordinate signedness, partial overlap and NULL sources remain outside the contract. Whole-executable identity remains pending.
