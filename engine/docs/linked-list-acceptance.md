# Linked-list acceptance

Nine canonical list primitives share one eight-byte node type with prev at0 and next at4. Each complete original span must be a raw code match; the422 bytes contain no address operands. Frozen campaign035 research remains unchanged. The first-node helper stays outside the accepted inventory because its six alignment bytes remain unresolved.

The focused fixtures remain active with literal input/output graph tables and full owned payload/guard snapshots. Connected tests initialize all nodes using the actual initializer, insert after and before, append, unlink, split a suffix, and unlink again. After each step, the real count/advance/last primitives check the literal expected topology. Negative and excess steps are checked without cyclic or long walks. Expected links are predicted before calls; no candidate result is copied into its oracle.

Mutators require nonNULL node arguments. Insertion uses distinct detached nodes; traversals use finite acyclic lists. Cycles, arbitrary insertion aliasing and unbounded capacity are outside this fixture contract. No bytes were inserted for padding and no target was shortened. The original game was not executed. Whole-executable identity remains pending.
