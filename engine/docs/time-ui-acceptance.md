# Timer and UI-state acceptance

Eight functions add 162 complete bytes and 14 verified address operands. All matched on their first implementation compile. Shared acceptance pending.

All 73,736 focused assertions are retained. Timer callback probes are explicitly named test-only source inclusions with immediately undefined macros; registered objects keep their real platform/elapsed callees. A separate connected matrix exercises real low-resolution and high-resolution platform timing, callback mutations, tick wraparound and bounded exact floating inputs. It restores platform function pointers, scales, selection and floating controls afterward.

UI cursor tests cover 64 output alias combinations, including aliases of source coordinates and shared output storage, with sequential expected writes computed before calls. Navigation pointer identity is checked before authored writes. Latch clear and joystick set preserve unrelated fields. Canonical bodies differ from focused candidates only by include, and prior308 registry/target entries remain unchanged.

No original program execution or whole-executable identity claim.

The supplemental modern build uses SSE integer-to-float conversion and rounds +/-16,777,217 before multiplication. Its expected table explicitly differs on those two rows; the original VC5 table and canonical function remain unchanged. The original-toolchain table preserves FILD integer precision. Both versions retain all 680 assertions, with distinct printed labels. Initial supplemental run34884415576 exposed this difference and remains recorded as failed; a fresh clean source/build checkpoint is required before acceptance.
