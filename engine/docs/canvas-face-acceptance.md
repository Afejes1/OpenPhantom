# Canvas and face allocation acceptance

Four campaign043 exact functions connect canvas allocation/initialization/free and face allocation. The40-byte canvas and20-byte raster view have asserted offsets; the76-byte face layout reuses the existing accepted header. The immutable2.0f operand binds to the original constant location.

Canvas fixtures retain literal center-bit and guarded raster oracles, then connect the real constructor/initializer/free wrapper for allocation success/failure, signed explicit rectangles, flag-selected raster dimensions, and captured root release. The external inner-cleanup observer is deliberately authored: its original five-byte body has four unresolved trailing bytes, so that dependency remains research and is not counted as an accepted function. Its separate focused free-wrapper observer also preserves callback mutation/order coverage.

Face construction connects actual accepted init/free/array-cleanup functions. Literal offset/word expectations retain unspecified fields and guards; authored allocated roots optionally acquire either of two owned array tokens before cleanup. The bounded callback plan verifies array order and final root release without deallocating real data.

No accepted target, original operand, comparison rule, warning waiver or full-span criterion changes. The original executable is never executed. These are function-level matches; final whole-executable/linker-layout identity remains pending.
