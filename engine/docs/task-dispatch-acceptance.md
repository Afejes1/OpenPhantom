# Task dispatch and state allocation

Two functions add274complete bytes and13verified address operands. [Acceptance 062](../research/acceptance-062/results.md) records successful shared checkpoint event 85. All112,392focused checks retained, withfull registry/pending/globals and output snapshots. Callback dispatch uses live current after callback retargeting, clears bit1 from the entire32-bit result and restores its entry context. Allocation wraps unsigned sizes correctly, zeros only the rounded span and publishes data using the live postallocator current.

Allocator495290 is an external dependency rather than an accepted reconstructed body; its observer is selected/restored in the shared fixture. The guarded pool is alwaysnonnull, including zero-size requests, and current is a valid owned record. Positive-size allocation failure, huge allocations and recursive callback behavior are outside this focused domain. No byte exclusions, warning waivers or shortened spans.
