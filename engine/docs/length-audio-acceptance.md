# Length and audio acceptance

Fourteen exact functions add 1062 complete bytes and 24 operands. All 238 prior target and registry entries remain unchanged. Canonical bodies change only their header include. Four estimator and ten audio fixtures retain their reviewed domains under separate local namespaces, with audio bindings defined once.

Connected estimator tests invoke all four canonical algorithms for each bounded dyadic input pair, checking independent ordering bounds, complete input preservation and FPU control restoration. Focused tests retain independent integer arithmetic exact-bit oracles and signed-zero cases; nonfinite, overflow and extended-ST0 behavior remain outside the fixture claims.

Connected audio tests invoke actual COM initialization, cooperative mode, frequency, distance bounds, spatial mode, listener commit, stop, buffer/spatial release and uninitialization wrappers using owned synthetic COM objects. Expected callback order, arguments, results, complete objects, vtables, vector, gates and listener state are checked after every step without reseeding. Stop's callback swaps vtables and clears the gate; release wrappers must skip until explicitly reenabled, then use the live swapped tables. No original game or OS COM/audio service executes.

Position and velocity wrappers remain excluded as research because their 45-byte candidates differ in extent from the 51-byte originals. No byte exclusions or new warning waivers. Whole-executable layout and identity remain pending.

The first supplemental compile rejected fixture helper locals shadowing global word/failure counters under modern /W4. Unique local names fix those warnings without changing any canonical source, fixture operation or warning policy. The failed same-source run is retained in the acceptance receipt.
