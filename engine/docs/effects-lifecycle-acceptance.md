# Effects lifecycle acceptance

Acceptance012 combines13 exact C27/C28 candidates,1119 complete bytes, into the existing69-function baseline. Ripple reset and overlay restore remain research because their complete extents do not match. Canonical actor flags/slot and halo layouts are shared; new zap records are120 bytes, material rows12 bytes, and staged effects blocks96 bytes. Source views assert accessed fields without inventing original opaque capacities.

Actual object teardown now calls both reconstructed halo and zap detach. The halo release callback observes the unchanged zap pool and may retarget the live shield slot; the actual stop and zap detach then produce checked full actor, shield and zap state. Inactive zap rows retain endpoint pointers. The former zap-stub mutation is removed because the real detach only clears matching active endpoints.

Actual object rendering now enters reconstructed halo_draw_actor. The existing432 draw cases set bit0x10 to reach their callback while preserving all tested bit0/signed-slot outcomes. Independent halo_draw_actor cases cover the missing-bit and NULL entry gates, duplicate owners, entry-only gating and dynamic later slots. Halo free-all shares the same pool and resource release callback scope.

Zap startup/shutdown, endpoint query/detach/reset and both draw wrappers share canonical globals. Resource and draw callbacks retain the original focused cases; a combined query/detach/reset sequence checks all records. Ripple material uses shared resource callbacks with exact VC5 argument order and captured return after cleanup callbacks alter its cache. Negative zap depths and ripple indices outside0..7 remain outside the source contract.

Effects load calls the actual overlay-read helper and shared serializer dispatcher. The96-byte read publishes six fog words before shield loading; the28-byte overlay reader stages its block and inverts reader status. The shared fixture checks both block identities, all gate outcomes, full block bytes and callback mutations. The linked-sphere loader remains an authored boundary.

All fixture scopes are explicit, owned storage is checked, and original game code is never executed. Standalone global pool checks are not adjacency-canary claims. Admission requires a clean source commit, full registered VC5 comparisons, isolated shared behavior,100 tooling tests,19 runner tests and full supplemental CI. No warning waivers, byte exclusions or full executable-layout claim.

A fixture-only compile diagnostic caught a collision between two namespaced endpoint-buffer variables before the full build. Their fixture-local names were corrected and the failed diagnostic retained; no candidate behavior changed.
