# Console and audio control acceptance

Six functions add441 complete bytes and36 address operands. [Acceptance059](../research/acceptance-059/results.md) records successful shared checkpoint event82. Every focused matrix is retained. The clear functions exercise the actual accepted setAttr source; callback mutation determines the live handle and restored default attribute. Box destruction reuses the existing heap observer dispatcher under a separately selected/restored test mode.

Cursor depth uses signed-positive show gating and unsigned live post-callback hide increment. Audio conversion tests use literal results for seventeen exact finite dyadic inputs, preserving raw volume bits before packing. Pan slot40 is recovered without shifting later vtable fields. Floating control state is saved/restored; invalid or out-of-range conversions are outside the bounded fixture domain.

The clear functions needed semantic local naming to reproduce VC5 stack placement. No synthetic locals, padding, byte exclusions or warning waivers were added. A nearby music duration function remains unattempted because its143-byte body has a trailingCC byte. Whole-executable identity remains unfinished.
