# Face and keyframe acceptance

Six campaign038 exacts retain full original spans. Face init/free/array cleanup are connected using owned guarded76-byte prefixes. Calls observe actual cleanup order, null paths, live second-array replacement/activation/clear, and the final captured-face release. Aliases test call observations without deallocating fixture storage. Initialization is independently snapshotted before a connected init/free chain. Unknown face payload fields and neighboring records remain intact.

Keyframe hooks use separate typed loader/unloader globals, exact previous-value returns and authored callbacks that must never be invoked by the setters. Header initialization clears the actual132-byte allocation, performs the original bounded copy via an authored backend, then forces byte31 NUL. The default resource text is authored. The upstream manifest duplicate load-hook label is reconciled by address and original unloader consumer.

These bounded tests do not claim null safety for face initialization/array cleanup or actual double-free safety. Whole-executable layout and original resource/data/CRT identity remain pending.
