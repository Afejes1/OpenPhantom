# Font state acceptance

Twelve campaign036 exact functions share one72-byte record definition and a16-record pool. Unknown word20/24 meanings stay explicit. The canonical pool excludes the research fixture trailing guard and occupies1152 bytes, ending exactly before the historical current-pointer address. Typed fields preserve observed offsets; complete resource semantics remain unknown.

Eleven prefixed focused fixtures retain their original independent oracles against the actual global pool. The backdrop fixture links the actual sprite acquire/release functions and dispatches only their lowest resource/format backends. It covers null/valid selection, null/non-null backdrop, release-side captured-slot replacement and retargeting, formatter-side retargeting, acquisition-side retargeting, null/name input, and failed/successful acquisition across all16 records. The original slot clears after backend mutation; later stores follow the live selection. Live formatted buffer identity/content is tested only during its lifetime. The canonical input qualifier is char*, matching the existing resource API; this changes no32-bit ABI or candidate instructions.

A connected state sequence selects each record, changes/reads colors, alignment, both scales, raw words and depth range, then invalidates selection and checks no-op behavior. Alias getters remain in focused fixtures. Z-range arithmetic evidence remains finite dyadic cases. No fake canonical sprite functions, warning waivers or shortened full spans are used. Query remains research with13 unresolved alignment bytes.
