# Console helper acceptance

Six wrappers add169 complete bytes and11 verified operands. [Acceptance058](../research/acceptance-058/results.md) records successful shared checkpoint event81. All96,000 focused assertions are retained, plus an actual linked sequence through the six wrappers. Console APIs remain authored stdcall observers. No original executable or real console API is executed.

The console information object has22bytes; the compiler reserves24 stack bytes. Only the22-byte object is initialized. Attribute writes preserve neighboring16-bit storage. Tests cover callback mutations, argument order, ignored return values and five coordinate destination aliases.

Canonical source differs only by include. No byte exclusions or warning waivers. Whole-executable identity remains unfinished.
