# Random table acceptance

Three functions contribute116 complete bytes and nine verified operands. Canonical bodies differ from focused candidates only by header include. All previous293 target and registry entries remain unchanged. Shared acceptance pending.

Sequential lookup increments the shared index and wraps only on equality with1117. Indexed lookup applies signed remainder only above1117, so equality reads the adjacent word. Shuffle calls the gameplay RNG and stores its unsigned remainder modulo1117; no return contract is inferred from the incidental quotient in EAX.

The retained matrices pass215,880 focused assertions with authored table words, index boundaries and RNG callbacks that mutate shared state. The table is owned for1118 elements in the harness to make the adjacent read safe and observable; this is not a claim about original production capacity. Negative or unowned table indexes are outside executed domains.

Gameplay RNG callbacks remain explicit test support. The initializer and resource accessor are parked research and are not part of this acceptance. No ignored bytes, padding, raw-byte insertion or warning waivers. Whole-executable placement and identity remain unfinished.
