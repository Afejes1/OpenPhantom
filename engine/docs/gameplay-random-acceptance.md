# Gameplay RNG acceptance

Three functions contribute245 complete bytes and eight verified operands. Canonical source differs from focused candidates only by include. All previous299 target and registry entries remain unchanged. Shared acceptance pending.

Next advances the signed Schrage state; seed normalizes input and returns the previous state; roll accumulates one plus unsigned remainder. The independent RNG oracle uses64-bit product/remainder, avoiding a duplicate of the candidate's32-bit algorithm. Input and arithmetic limits are explicit in the focused contracts.

The25,664 focused checks include arbitrary callback results and mutations. To preserve those controls after real RNG integration, shared tests include authored shuffle and roll source under explicit probe names with a local observer callee, then undefine the macros immediately. These probes are test-only source executions, not accepted artifacts or original-address aliases. Registered canonical objects retain their real symbols and callees. Separate connected matrices call those actual canonical functions and assert returned words, final RNG state, table index and complete table preservation. C075 next and indexed lookup still call their actual canonical bodies.

No original game execution, byte exclusions, manufactured frames or warning waivers. Final executable placement and identity remain unfinished.
