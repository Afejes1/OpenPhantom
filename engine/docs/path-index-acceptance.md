# Indexed directory and filename acceptance

Three functions add 370 complete bytes and six verified call operands. Canonical source differs from focused candidates only by header include. The prior 287 target and registry entries are preserved. Shared acceptance pending.

Directory offsets use one-based indexes and require a following backslash. Repeated backslashes are skipped between components; final filenames are rejected as directories. Zero and negative indexes consume the finite path until missing. Nth-directory copy clears output first, requires a positive separator length, copies that range and terminates it. Filename copy clears output before calling file-offset and uses the original intrinsic strcat. Exact alias therefore empties the path for either copy routine.

Three retained matrices pass 6,111,472 focused assertions. Independent component enumeration predicts every observed search pointer and copy argument; guarded input/output snapshots retain all surrounding bytes. The accepted path helper chain is linked. Separator remains explicitly unaccepted test support. INT_MIN signed decrement and partial overlap are outside the fixture contract.

Two original-compiler compatibility diagnostics are retained without waivers. No padding, raw-byte insertion or exclusions were introduced. Full executable placement and identity remain unfinished.
