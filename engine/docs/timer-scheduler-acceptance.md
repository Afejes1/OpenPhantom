# Timer initializer and task scheduler

Two functions add406 complete bytes and40verified address operands. Shared acceptance pending. All1,696,848focused checks retained. Timer tests use actual accepted clock/elapsed/conversion functions and preserve API/control state. Scheduler tests distinguish live current from live cursor, signed countdown decisions, count underflow and release-side mutations.

The scheduler identifies record.update as an int cdecl(void) callback. The original opaque void-pointer cast failed VC5C4055; the type was recovered without a warning waiver. Prior task_register changes only its equivalent pointer signature, and prior fixtures use four distinct authored callback identities that are never invoked. The complete shared comparison must verify prior345 bytes unchanged and retain all prior matrix counts. Historical focused snapshots are not rewritten.

No original execution, byte exclusions, padding or shortened spans. Whole-executable identity remains unfinished.

Preparation of the neighboring fixed-step routine establishes that address86871C stores floating interpolation alpha. The initializer keeps a raw32-bit zero-store view (legacy identifier op_time_mode) and does not interpret it as a mode. Addresses869294/868728 are also floating accumulators viewed as rawwords by existing reset helpers. Recover shared float declarations and associated fixtures before implementing the stepping routine. No stepping source was attempted in this window.
