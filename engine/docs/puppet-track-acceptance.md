# Puppet animation-track acceptance

Five exact functions share the asserted332-byte track, four-track puppet, and thing userdata views. Only proven fields are named; all other bytes remain opaque. Full spans total400 bytes with5 operands. The initializer now calls the actual reconstructed remover under one canonical symbol/address.

Four focused fixtures remain in the shared suite. The initializer observer is replaced with a connected initializer/remover/callback test covering all16 callback masks, both owned thing pointers and null thing when no callback can dereference it, callback mutation of the current track, a later/wrapped track, paused/thing and thing userdata. Every callback checks the full state before the remover performs its three selective clears; no mutation is copied from actual output into expected state. The connected flow then plays, fades in, fades out, removes and attempts inactive fade-out for all four tracks with complete state checks.

Original game and callback code are never executed. All fixture callbacks are authored. Fade tests make a finite-input claim; original unordered branches remain statically matched. Accepted history requires the clean full original-toolchain comparison and shared fixture; see research/acceptance-016/result.json.
