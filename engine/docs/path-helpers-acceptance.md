# Path helper acceptance

Four functions add390complete bytes: drive65, first-directory99, file-offset124 and extension102. File-offset is a raw match; the other three have one verified call operand each and zero resolved differences. Candidates change only their header include. The prior279target/registry entries remain identical.

Drive returns char in AL; upper EAX on invalid-drive paths is unspecified. Authored uppercasing observers validate signed input transport and low-byte returns, including synthetic high-byte inputs without invoking CRT on negative values. File-offset recognizes backslash/colon, not forward slash; extension chooses the first dot after basename and preserves the original trailing-dot behavior that can continue past the first NUL. Test paths live inside guarded96-byte arrays with explicit bounded post-NUL data.

Four retained focused matrices pass454,567assertions. The shared extension fixture links the real file-offset function. First-directory uses the separator's independently authored42-byte body as test support. Separator's full56-byte target retains14unresolved alignment bytes, remains research with no percentage, and is neither registered nor counted accepted. Its separate156,240-check dependency matrix is retained. Scoped character-search and uppercase routes preserve existing lexical fixtures.

Two fixture-generation failures (backslash escaping and neighboring multicharacter alphabet literals after formatting) are preserved. The final alphabet explicitly contains bytes97,92,58,46,47. Candidate code never changed, and no warning waiver or byte exclusion was introduced. [Acceptance 050](../research/acceptance-050/results.md) records the successful shared checkpoint in immutable event 73. Whole-executable identity remains unfinished.
