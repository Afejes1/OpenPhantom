# Save slot acceptance

Five exact functions add224 complete bytes and13 verified address operands. Prior188 target/registry entries and the focused source bodies remain unchanged except the shared header include. The disk-space helper remains research with six differing stack offsets and is not integrated.

Actual teardown/load/delete wrappers now call the actual slot-path helper and the existing shared variadic formatter observer. Four direct/connected modes check signed slot identifiers, fixed returned path identity, independent formatter/backend returns, lifecycle ordering, ignored returns, and callback mutations before later calls. The formatter dispatch reads the extra signed slot argument only for this fixture; all existing sprite/font formatter paths retain their original argument handling. Global path storage is authored with32 active bytes and8 trailing guards; it is a bounded fixture domain rather than whole-program capacity proof.

The free-labels helper uses its reviewed guarded99-row fixture and the single shared free observer. Callbacks replace current fields, alter future dispatch and insert earlier labels that must not be revisited; expected snapshots predict the candidate's post-callback null store. The8-byte row is only a verified partial view. No actual heap free, file deletion/load, original lifecycle or formatting content is executed or copied.

Manifest names for the load/delete wrappers conflict with their observed semantics. Eligibility remains bound to numeric team-not_done addresses; documentation uses the recovered behavior. No byte exclusions or VC5 warning waivers. Shared matches do not establish whole-executable identity.
